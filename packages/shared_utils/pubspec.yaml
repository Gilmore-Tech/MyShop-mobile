name: shared_utils
description: MyShop shared utilities — phone format, money helpers, validators
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  intl: ^0.19.0

dev_dependencies:
  test: ^1.25.0
