name: shared_ui
description: MyShop shared UI components, theme, and design tokens
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.0
  shimmer: ^3.0.0
  cached_network_image: ^3.4.0
  image_picker: ^1.1.0
  file_picker: ^8.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
