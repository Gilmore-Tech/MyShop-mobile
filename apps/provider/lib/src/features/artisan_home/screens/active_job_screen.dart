import 'package:flutter/material.dart';

/// Navigation (Mapbox), mark arrived/in_progress/complete, chat/call, emergency button, welfare check response
/// PRD Reference: PRD 5.3
class ActiveJobScreen extends StatelessWidget {
  const ActiveJobScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ActiveJobScreen')),
      body: const Center(
        child: Text('TODO: Implement ActiveJobScreen'),
      ),
    );
  }
}
