import 'package:flutter/material.dart';

/// Job summary: amount earned (after commission), client rating, tip. Wait for client dual confirmation
/// PRD Reference: PRD 5.3
class ArtisanJobCompleteScreen extends StatelessWidget {
  const ArtisanJobCompleteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ArtisanJobCompleteScreen')),
      body: const Center(
        child: Text('TODO: Implement ArtisanJobCompleteScreen'),
      ),
    );
  }
}
