import 'package:flutter/material.dart';

/// Phone number input for provider registration/login
/// PRD Reference: PRD 5.1
class ProviderPhoneInputScreen extends StatelessWidget {
  const ProviderPhoneInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProviderPhoneInputScreen')),
      body: const Center(
        child: Text('TODO: Implement ProviderPhoneInputScreen'),
      ),
    );
  }
}
