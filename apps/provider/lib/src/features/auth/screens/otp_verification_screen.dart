import 'package:flutter/material.dart';

/// OTP verification for providers
/// PRD Reference: PRD 5.1
class ProviderOtpVerificationScreen extends StatelessWidget {
  const ProviderOtpVerificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProviderOtpVerificationScreen')),
      body: const Center(
        child: Text('TODO: Implement ProviderOtpVerificationScreen'),
      ),
    );
  }
}
