import 'package:flutter/material.dart';

/// Edit: name, email, language, service radius (artisan), vehicle info (driver)
/// PRD Reference: PRD 5.4
class EditProviderProfileScreen extends StatelessWidget {
  const EditProviderProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EditProviderProfileScreen')),
      body: const Center(
        child: Text('TODO: Implement EditProviderProfileScreen'),
      ),
    );
  }
}
