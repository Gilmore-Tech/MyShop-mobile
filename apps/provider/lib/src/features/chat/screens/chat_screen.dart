import 'package:flutter/material.dart';

/// In-app chat with client during active ride/job
/// PRD Reference: PRD 4.7
class ProviderChatScreen extends StatelessWidget {
  const ProviderChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProviderChatScreen')),
      body: const Center(
        child: Text('TODO: Implement ProviderChatScreen'),
      ),
    );
  }
}
