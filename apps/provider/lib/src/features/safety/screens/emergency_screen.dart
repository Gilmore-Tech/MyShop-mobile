import 'package:flutter/material.dart';

/// Same two-step emergency as client. Available for both drivers and artisans
/// PRD Reference: PRD 9.1.1
class ProviderEmergencyScreen extends StatelessWidget {
  const ProviderEmergencyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProviderEmergencyScreen')),
      body: const Center(
        child: Text('TODO: Implement ProviderEmergencyScreen'),
      ),
    );
  }
}
