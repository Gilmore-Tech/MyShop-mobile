import 'package:flutter/material.dart';

/// Artisan onboarding: service categories, shop capacity (solo/multi), service radius
/// PRD Reference: PRD 5.1
class ArtisanRegistrationScreen extends StatelessWidget {
  const ArtisanRegistrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ArtisanRegistrationScreen')),
      body: const Center(
        child: Text('TODO: Implement ArtisanRegistrationScreen'),
      ),
    );
  }
}
