import 'package:flutter/material.dart';

/// Driver onboarding: personal info, vehicle details (make, model, year, plate, color)
/// PRD Reference: PRD 5.1
class DriverRegistrationScreen extends StatelessWidget {
  const DriverRegistrationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DriverRegistrationScreen')),
      body: const Center(
        child: Text('TODO: Implement DriverRegistrationScreen'),
      ),
    );
  }
}
