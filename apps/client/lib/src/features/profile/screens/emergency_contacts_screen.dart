import 'package:flutter/material.dart';

/// Add/edit/remove emergency contacts. Set primary contact
/// PRD Reference: PRD 9.3
class EmergencyContactsScreen extends StatelessWidget {
  const EmergencyContactsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('EmergencyContactsScreen')),
      body: const Center(
        child: Text('TODO: Implement EmergencyContactsScreen'),
      ),
    );
  }
}
