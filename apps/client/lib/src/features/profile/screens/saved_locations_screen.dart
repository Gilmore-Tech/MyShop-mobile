import 'package:flutter/material.dart';

/// Manage Home, Work, Favourites with map pin selection
/// PRD Reference: PRD 4.11
class SavedLocationsScreen extends StatelessWidget {
  const SavedLocationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SavedLocationsScreen')),
      body: const Center(
        child: Text('TODO: Implement SavedLocationsScreen'),
      ),
    );
  }
}
