import 'package:flutter/material.dart';

/// Points balance, transaction history (earned/redeemed), redeem flow
/// PRD Reference: PRD 4.9
class LoyaltyPointsScreen extends StatelessWidget {
  const LoyaltyPointsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LoyaltyPointsScreen')),
      body: const Center(
        child: Text('TODO: Implement LoyaltyPointsScreen'),
      ),
    );
  }
}
