import 'package:flutter/material.dart';

/// Dispute within 2 hours. Evidence upload, reason input
/// PRD Reference: PRD 7.5
class JobDisputeScreen extends StatelessWidget {
  const JobDisputeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('JobDisputeScreen')),
      body: const Center(
        child: Text('TODO: Implement JobDisputeScreen'),
      ),
    );
  }
}
