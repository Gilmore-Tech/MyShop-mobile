import 'package:flutter/material.dart';

/// Shows estimated fare, pickup time, route on map. Confirm Booking button
/// PRD Reference: PRD 4.3
class FareEstimateScreen extends StatelessWidget {
  const FareEstimateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FareEstimateScreen')),
      body: const Center(
        child: Text('TODO: Implement FareEstimateScreen'),
      ),
    );
  }
}
