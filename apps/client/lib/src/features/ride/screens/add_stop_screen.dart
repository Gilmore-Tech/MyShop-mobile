import 'package:flutter/material.dart';

/// Add a stop mid-ride with map pin. Shows Ashanti boundary warning if outside pilot region
/// PRD Reference: PRD 4.4
class AddStopScreen extends StatelessWidget {
  const AddStopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AddStopScreen')),
      body: const Center(
        child: Text('TODO: Implement AddStopScreen'),
      ),
    );
  }
}
