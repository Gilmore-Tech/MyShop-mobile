import 'package:flutter/material.dart';

/// Searching animation while system finds nearby drivers
/// PRD Reference: PRD 4.3
class DriverMatchingScreen extends StatelessWidget {
  const DriverMatchingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DriverMatchingScreen')),
      body: const Center(
        child: Text('TODO: Implement DriverMatchingScreen'),
      ),
    );
  }
}
