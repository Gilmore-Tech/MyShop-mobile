import 'package:flutter/material.dart';

/// Push notification history: ride updates, job updates, promos, account alerts
/// PRD Reference: PRD 10
class NotificationsListScreen extends StatelessWidget {
  const NotificationsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('NotificationsListScreen')),
      body: const Center(
        child: Text('TODO: Implement NotificationsListScreen'),
      ),
    );
  }
}
