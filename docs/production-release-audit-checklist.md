# MyShop Production Release Audit and TODO

> Living cross-system release tracker. This document records decisions, findings,
> evidence, and every gate that must close before production. It does not inherit
> a PASS from an older audit, specification, or unchecked implementation claim.

> **Post-release continuation (22 July 2026):** the emergency release tracked in
> the historical sections below has shipped. The active remaining-release and
> 100k-DAU checklist is
> [`post-release-100k-dau-roadmap.md`](post-release-100k-dau-roadmap.md). The
> older entries remain here as the decision/evidence ledger; their historical
> `NO-GO`, branch and percentage statements must not be read as the current
> runtime state.

## Release identity

| Field | Current value |
| --- | --- |
| Last updated | 2026-07-22 (GMT) |
| Release target | Client app, provider app, API, admin, and infrastructure |
| Scale target | 100,000 daily active users; peak concurrency/RPS model still to be approved |
| Release flow | feature branch -> `staging` -> tested staging -> `main` -> production |
| Mobile working branch | `feature/provider-request-overlay-redesign` at `d200d3dd5fbf0f6031c9930046c62cfc816e6cd5` |
| Mobile staging state | Fetched `origin/staging` is `f88f43c85e52ecf9f7d69b0f6e0a500665a0edcb`; the committed tree is byte-identical to mobile worktree HEAD `d200d3d…`, but the histories differ, so package the dirty candidate on a new branch from `f88f43c…` rather than merging the old feature-branch graph |
| Backend staging state | [Gilmore-Tech/myshop#96](https://github.com/Gilmore-Tech/myshop/pull/96) was merged by `Ayiks` at 2026-07-16 21:16:48 GMT; `staging` is now `1f1ada8b7ad3b9b8ab937fe8fab3dad611aae7bd` |
| Audited backend tree | PR head `3f27dd9825b47a82280a6ae6013d8988f8a0fae4`; its tree `e241fcfe198813274a897928b5671508d353ccb3e` is byte-identical to merge commit `1f1ada8b7ad3b9b8ab937fe8fab3dad611aae7bd` |
| Admin staging state | Fetched `origin/staging` and the local packaging base are both `78fabd15d8235e399dba45933bbe125b13c81077` |
| Staging API | Render `myshop-api-test`, Oregon, live and healthy |
| Production API | Render `myshop-api`, Frankfurt |
| Decision posture | Staging API **LIVE**, live-OTP testing **BLOCKED FOR CONTAINMENT**; production **NO-GO** |

## Current progress snapshot

The passing work below is local unless explicitly labelled deployed. None of the
new provider, ride, mobile, or admin fixes is yet authorized for production.
The emergency release candidate implementation and local automated audit are
approximately **99% complete** on three exact local staging-parent commits. The owner
approved the exact legal-consent document set, cutover, re-consent and
active-work continuity rules; the local cross-system implementation and its
automated evidence are complete. Packaging and staging-release readiness are
approximately **97% complete**, while production-release readiness is
approximately **72% complete** because
signed-device, deployed integration,
operational, legal-content, resilience and rollout evidence cannot be replaced
by local tests. These are evidence-weighted estimates, not a
production-capacity qualification or release approval, and production remains
**NO-GO**.

Exact-current backend evidence (2026-07-19): an exact pinned Node **22.23.0**
Linux run passes **201/201 suites and 4,002/4,002 tests**; local typecheck,
lint with zero errors and the five-package production API build also pass. A fresh
disposable PostgreSQL/Redis stack applies all **195
migrations**, passes approved-PostGIS-only schema drift, seed, bounded/read-only
preflights, ride-cutover refusal and online-index safety, then passes the
**79/79** lifecycle E2E gate. The E2E accounts carry the same exact immutable
Terms/Privacy evidence as production; the run also proved that final deletion
of the private phone-auth root may null only its audit pointer while every legal
evidence field remains immutable. Strict legal release readiness correctly
fails until the reviewed `1.4.1` content is published. The exact-current
production runtime image is **181,271,488 bytes** at
`sha256:16d9b3a088cbe37e5bbf27e2527037e1ec4b0d4f26a332b269cd9466257ded03`;
the repository verifier proves Node **22.23.0**, UID/GID 1001, an explicit
source/dev/secret-free runtime allowlist, read-only containment, all 195
migrations, seed, production refusal of eviction-enabled Redis, TLS Redis with
`noeviction`, geofence/readiness and SIGTERM handling. All **66**
manifest-controlled concurrent index builds plus **4** concurrent legacy-index
drops retain their write-continuity/catalog contracts. Production-shaped
timing, external dependency evidence, and the pinned staging artifact remain
required.

The release-critical scheduled-worker audit now fences the active-ride
disconnect escalation, scheduled-job no-show, bid expiry, job staleness,
directed-quote reminders, rating reveal, clawback write-off, reconciliation,
insufficient-balance alert and stale-payment verification paths with atomic
claims, compare-and-set transitions and/or durable notification idempotency.
Replica losers cannot repeat the protected state, money, provider-gateway or
notification effect in the new regression cases. Reconciliation discrepancies
now use the report as a PostgreSQL-leased durable alert outbox; five-minute
recovery plus per-admin dedupe closes crashes before or after inbox fan-out and
retains reports when no authorised payment admin exists. Candidate scans,
production-shaped two-replica faults and saturation remain operational
evidence, not locally assumed capacity proof.

The approved 120-second active-ride socket-loss path now joins the same durable
BR-33 degradation episode as stale-GPS detection. It persists permission-scoped
admin, exact-role provider and client inbox alerts, retries when no authorised
operator exists, uses the shared episode timestamp for cross-worker dedupe, and
marks the episode complete only after durable delivery. Queued BR-53 successors
remain excluded until activation, and the active ride continues throughout.

Signature-verified Paystack webhooks that exhaust automatic processing no
longer depend on an application-log `ADMIN_ALERT`. The webhook row is now a
PostgreSQL-leased alert outbox with token-owned acknowledgement, bounded retry,
zero-recipient retention and per-admin event dedupe. Global payment-authorised
operators receive a privacy-safe inbox alert and a new read-only Admin queue;
raw webhook bodies, account destinations, payment/transfer references and
provider error prose are never returned. The queue adds no automatic retry,
refund, transfer, payout or reconciliation mutation.

The current gate includes exact-opt-in/default-off non-OTP SMS and new
scheduled artisan jobs, provider-user immutability for approved profile photos,
server-authoritative BR-66 document replacement, every authenticated
non-deleted role's SOS access without mandatory booking/GPS, null-safe Admin SOS
rendering, and durable GMT document notices at upload confirmation, seven days,
72 hours, 24 hours, two hours and invalidity. Exact legal consent now uses two
independent unchecked controls, role-specific Terms plus shared Privacy,
OTP-bound exact document snapshots, an immutable role-account ledger,
re-consent, new-work fences and active-work continuity. Mobile passes
**428/428** and all seven analyzers; Admin passes **32/32** contracts,
TypeScript and its 48-route production build, with lint at zero errors and
**101 warnings**. Exact local candidates now exist at mobile source
`b0d05d0df53a34102c5d38ad442ad0d951d3c7b2`, backend
`0c40e30b3602c7da6862d2651f8c83c7f99739c2`, and Admin
`f65df4fa8e7ad5400fd70e5c7b1056e9bcaa96e2`. Each is parented directly to its
pinned staging base, clean-checkout gates pass, and no branch has been pushed.
The candidate now needs second-developer review, push/merge, deployed staging
and installed-device proof.

| Surface | Fresh evidence | What remains before release |
| --- | --- | --- |
| Mobile | The combined current tree contains durable BR-37 booking recovery, mandatory-update containment, delayed-OTP truth, exact-role chat identity parsing, authoritative provider Offline/vehicle selection, stable eligibility and precise GPS error copy, document alerts, real ride/job dispute submission, fail-closed media/refund-destination consumers, fail-closed server-owned provider location epochs with monotonic sample sequences, the approved 15-second/30-metre ride/job lifecycle GPS proof, exact-role BR-32 online-intent recovery, one-time exact-session recovery capabilities that are never inferred or persisted, exact-role BR-63 deleted-account OTP filing with request-key replay safety and sibling-isolation copy, cancellation/lifecycle authority that preserves active work unless the write response or read-back proves the transition, app-owned safe API copy, and payment/payout UI that cannot create success from a client tap or malformed response. Every client SOS entry now routes through the approved three-second hold, opens only the external emergency dialer, uses current 191/112 numbers, and makes no automatic-call, handset-delivery, or recording claim. Raw FCM/socket/payment payload values are no longer logged on the audited paths. Support surfaces hide unverified phone/WhatsApp destinations, help-article ticket actions navigate correctly, and visible release UI receives the approved marketing version from the signed-build resolver. The exact-current tree passes **428/428 tests** across all six test-bearing packages (client 78, provider 153, API client 154, shared UI 30, shared models 7, overlay 6), all seven package analyzers with fatal infos, the release-version contract, and `git diff --check`. No legacy media-confirm fallback is authorized. Existing signed Android/iOS artifacts still predate the candidate and are not release evidence. | Read the highest private client/provider build numbers, choose codes above both stores and the local `+20` floor, retain the exact verified Android/iOS listing URLs, rebuild signed version `1.4.1` artifacts, and prove installed old/new builds plus physical-device foreground/background/terminated/locked/reconnect/permission/lost-response/cancellation/payment/payout/media/OTP/refund/location-session/lifecycle-GPS/BR-32/BR-63/SOS journeys before the controlled staging rollout. |
| Provider eligibility/API | The local candidate combines exact-role eligibility, dispatch, payment/refund, media, liveness, admission, health, metrics, support-reference, log-containment, durable role-session, session-recovery and truthful fail-closed SOS controls. BR-63 now adds OTP-bound exact-role filing, stable request-key replay, device binding, global client approval, named regional Admin provider intake, Coordinator→RM provider completion, sibling isolation, and no rejection route. BR-66 closes still-valid provider replacements and uses server/DB time to reopen expired or rejected evidence without weakening Go Online. Exact-commit pinned Node 22 evidence passes **201/201 suites and 4,002/4,002 tests** plus the five-package production build. The fresh stack applies all **195 migrations**, passes approved-PostGIS-only drift, seed, bounded/read-only release preflights, cutover refusal and online-index safety, then passes **79/79** lifecycle E2E tests with exact role-scoped legal acceptance and real reconciliation/webhook-alert crash recovery. The high-risk scheduled workers now have per-item claims, database compare-and-set authority and/or durable notification dedupe, including stale payment verification, active-ride disconnection escalation and Paystack dead-letter operator delivery. The 120-second socket-loss detector and stale-GPS worker share one durable degradation episode, while unactivated queued rides are excluded. The strict legal release gate correctly reports the known publication blockers; zero violations remain required. Production bootstrap and readiness now reject any Redis policy other than `noeviction`, and the exact-commit runtime image proves both eviction refusal and the accepted TLS/no-eviction path. All **66** indexes on pre-existing populated tables remain one-statement concurrent builds and four legacy removals remain concurrent drops with write-continuity/catalog proof. | Obtain second-developer review, push/merge the exact local commit, and repeat the production-shaped clone/staging preflights; require zero legal violations; configure external metrics/alerts; prove two-replica Redis/network/process failure behavior, private storage, encryption custody, live carrier/payment paths, full load/soak, rollback, and one pinned staging artifact. Replace cross-region eviction-enabled production Redis; the candidate will deliberately refuse to start against it. Keep device-session recovery and deleted-role recovery OFF until their distinct controlled staging gates pass. Automated police checks, emergency recording, email, USSD, support/dispute attachments, automated payouts, rewards, cancellation consequences, and BR-53 also remain OFF. Production remains **NO-GO**. |
| Ride loss | The combined V2 candidate has durable booking recovery, a PostgreSQL-final new-booking cutover gate, PostgreSQL offer/receipt authority, per-driver 45-second decisions after authenticated receipt, sequential assignment, current online-session authority, one-open-offer invariants, bounded worker leases/rings, five-minute dispatch lifetime, authoritative offer-time document/vehicle/category/capacity/location/legal-consent checks, no-starvation KNN matching, server-owned monotonic driver/artisan location sessions, exact lifecycle GPS proof, invalid-token role fencing, durable exact-role auth-session fencing, and a database-final V2 pricing/booking contract that rejects returned legacy writers. The exact-current pinned Node 22 **201-suite / 4,002-test backend gate** passes locally. The approved 120-second active-ride socket-loss path now produces durable deduplicated operator/provider/client evidence without terminating the trip or selecting an unactivated queued successor. The exact-current **195-migration** lifecycle gate passes **79/79** on a fresh stack, including exact legal-consent admission, exact-session notification-registration/logout races, fail-closed USSD, email, automated police checks and emergency-recording exposure, truthful SOS persistence, BR-63 exact-role recovery, reconciliation/webhook-alert replica/crash recovery, reopened-gate invalid-writer refusal, and pre-cutover legacy-state zero-mutation cutover proof. | Keep V2 bookings closed until client/provider private build numbers and signed artifacts are pinned; retain the exact verified store URLs; repeat query plans and the complete BR-38 sequence on the production-shaped clone and exact staging artifact; execute the stopped-old-worker receipt reset; and prove delayed/duplicate/lost socket+push, invalid-token replacement races on devices, Redis/network/process, multi-replica, physical-device, load, canary, and rollback behavior. BR-53 stays disabled. |
| Admin | The current 48-route candidate keeps device-session recovery separate from deleted-role recovery; enforces global-only client approval and non-inheriting named regional Admin provider intake; exposes no recovery rejection; and preserves exact role-account routes, durable session-recovery `resolving`/retry visibility, vehicle/category Coordinator→Regional Manager lifecycle, immutable approved vehicle revisions, fail-closed private document upload, safe artisan capacity editing, read-only suspended batch payout controls, and a privacy-safe read-only Paystack webhook-failure queue. Its API wrapper uses app-owned code/status copy and validated opaque support references. Fresh evidence: TypeScript passes, **32/32 contract tests** pass, lint exits with **0 errors and 101 warnings**, and the full 48-route Next.js production build passes. | Add browser/E2E/concurrency/IDOR and timezone-boundary coverage; run real BR-63 client approval and Admin→Coordinator→RM provider journeys, session-recovery crash/resume, legacy remediation, signed-document, error-reference, SMS-failure, PDF-failure and Paystack dead-letter operator-visibility staging journeys; verify external log access/retention/redaction; review warning debt; run final diff checks; and pin/deploy the exact artifact. |
| Staging | Deployed backend SHA `1f1ada8…` is live/healthy with the protected Ghana boundary and isolated Redis prefixes. Exact local staging-parent candidates and clean-commit gates now exist for all three repositories. | The deployed service does **not** contain the candidates above. Complete second-developer review, push/merge, OTP/integration isolation and migration/data preflights; then deploy the exact SHAs and execute acceptance canaries. |
| Production | **NO-GO**. | Close every unwaived P0/P1, replace cross-region eviction-enabled Redis, prove 100k-DAU capacity/resilience, monitoring, rollback, support, and approvals. |

### Exact emergency release cut

This is the short authoritative cut for the current two-developer release. The
long findings register below retains historical evidence and future enablement
conditions; it must not make a securely disabled feature appear to block this
emergency build.

| Non-deferable gate | Current evidence/status | Fastest safe closure |
| --- | --- | --- |
| Three reviewed release commits | Exact local staging-parent candidates now exist: mobile source `b0d05d0…`, backend `0c40e30…`, and Admin `f65df4f…`. Clean-checkout analyzers/tests/builds, pinned Node 22, 195 migrations, 79 E2E tests and runtime verification pass. They are not pushed and have not received the required second-developer review. | Push the three local release branches, open focused PRs, have the second developer compare the path/deletion/disabled-feature inventory, then merge only the reviewed exact SHAs with auto-deploy still off. |
| Correct legal publications and versioned consent | The owner-approved exact consent mechanics are implemented locally: separate unchecked Terms/Privacy controls, role-specific Terms plus shared Privacy, OTP-bound exact snapshots, transactional immutable acceptance, no legacy fabrication, authenticated re-consent, active-work continuity, new-work/matching fences and immutable publication. Production still serves draft/stale documents, and source text is intentionally `publish: false`. | Qualified Ghana counsel/Product must supply and approve the actual corrected `1.4.1` text, operator/contact/effective-date facts and hashes. Publish the four exact rows through the audited seed/API path, require `ASSERT_RELEASE_READY=true` to report zero, verify public hashes, then prove installed-app registration/re-consent. |
| Durable same-region production Redis | Known production Redis is Cape Town, eviction-enabled, while the API is Frankfurt. This cannot be treated as durable OTP, dispatch, rate-limit or socket authority at 100k DAU. | Provision Frankfurt Redis with no-eviction, TLS, HA/backup/capacity and alerts; rehearse failover/restore and validate exact prefixes before cutover. |
| Reviewed staging deploy and data preflights | Exact local commits pass pinned Node 22/runtime-image proof, **195-migration** replay, drift/seed/preflight/index checks and **79/79** lifecycle E2E; deployed staging is the older SHA. | After second-developer review/merge, run aggregate/PII-free staging impact preflights, preserve rollback/PITR, deploy the exact backend/Admin candidates with every deferred flag closed, and verify health/geofence/prefix/version markers before installing mobile builds. |
| Private build numbers, signing and installed artifacts | Marketing version 1.4.1 is approved; highest private build numbers are unknown and iOS distribution signing is unavailable. | Read all four private maxima, choose higher codes, restore Apple Distribution authority, build/sign/install client and provider on Android/iOS, and verify canonical store destinations. |
| Core installed-device journeys | Local contracts are green but cannot prove OS delivery, permissions, background execution or app-kill recovery. | On the two developer devices, run OTP/signup, provider verification/vehicle/Go Online, sequential ride receipt/45-second decision, foreground/background/locked/reconnect, payment/refund/dispute and SOS plus rollback journeys. |
| Live carrier/payment failure canaries | Local policy, retry and idempotency tests pass; real Arkesel/WhatsApp/Paystack callback, timeout, reversal and cost evidence is absent. | Use the approved private two-number OTP allowlist and bounded low-value payments; retain callbacks, latency/cost/error evidence and prove the kill switches and reconciliation. |
| Monitoring, rollback and bounded capacity canary | Runtime health/metrics and safe load tooling exist locally. High-risk scheduled state/gateway/notification effects are now atomically fenced in source, but no retained production-shaped multi-replica result, scan/pool saturation result or paging ownership exists. | Configure dashboards/alerts and named owners, run bounded multi-replica delay/duplicate/Redis/process faults plus a canary, reconcile invariants, and rehearse rollback. Full 100k-DAU qualification remains required before broad production scale. |

Safe next-release scope is now source- and Blueprint-contained: new scheduled
artisan jobs; aggregate batch payouts; promo/loyalty/referral mutation;
Smile/police automation; USSD; general email/SMS/WhatsApp; support/dispute
attachments; emergency recording; automatic purge; BR-53 active-trip fallback;
and automatic cancellation consequences. These remain P0/P1 before any future
enablement, but do not block this emergency build while their exact release
authorities stay false and user/legal/store claims remain absent.

Best-case engineering time from push/merge authorization to a tested
store-submission candidate is **3–6 hours** if credentials, private build
numbers, legal approvals/content and replacement Redis are already available.
External legal review, infrastructure provisioning and store review are not
inside that estimate.

### Newly verified live legal-content gate

- [ ] **P0 / production currently serves draft and materially stale Terms:**
  public reads on 2026-07-19 confirmed that the client, driver, and artisan
  `/v1/legal/terms` responses all return version `1.0.0`, no `effectiveAt`, and
  body text marked `DRAFT`. The live client Terms claim USSD, Flutterwave, a
  fixed 20% commission, and a two-hour dispute window. The live driver and
  artisan Terms claim fixed 20% commission, Flutterwave, and instant/batch
  payout behavior; the artisan Terms also state a two-hour dispute window.
  These contradict approved v1 containment and the implemented 24-hour
  dispute rule. The local candidate now refuses automatic publication of source
  drafts, requires explicit `publish: true` plus an already-effective timestamp,
  refuses launch placeholders, rejects silent same-version edits, and
  release-gates the approved four-document set: client, driver and artisan
  Terms plus shared Privacy. The owner-approved consent path is now implemented
  locally with independent unchecked controls, exact OTP-bound document
  snapshots, an immutable role-account ledger, no timestamp backfill,
  authenticated re-consent, active-work continuity and server-side new-work
  fences. The exact-current local gate passes **201/201 suites, 4,002/4,002
  tests, all 195 migrations and 79/79 lifecycle E2E**. Strict
  `ASSERT_RELEASE_READY=true` correctly refuses the current drafts. Do not
  silently overwrite version
  `1.0.0`: use the
  [v1.4.1 legal correction brief](legal-v1.4.1-release-correction-brief.md),
  prepare a new version, remove unsupported promises, obtain Product/Legal
  approval and an effective date, publish through the audited legal-document
  path, verify every required audience and capture installed-app acceptance.
  Correct factual release copy and legal approval may run in parallel with
  staging/device tests, but this gate cannot be shifted to a later release
  while the stale text remains public.

The exact remaining operator sequence is maintained in the
[staging release candidate runbook](staging-release-candidate-runbook.md). The
three-repository bases, local source fingerprints, exclusions, fail-closed
feature set and packaging order are pinned in the
[staging release-candidate manifest](staging-release-candidate-manifest.md).

## 24-hour emergency release posture

The owner reported a 24-hour delivery constraint on 2026-07-18. This does not
convert missing evidence into a PASS. A full 100k-DAU production qualification
is not credible inside that window; the only defensible 24-hour outcome is an
emergency candidate in which every unresolved P0 is either fixed and proved or
disabled by a tested server-side kill switch.

| Window (GMT elapsed) | Mandatory outcome | Release consequence |
| --- | --- | --- |
| 0-4h | Freeze scope and finish the active media-provenance, payout-concurrency, ride-session, provider-eligibility, OTP, signup, and stable-error P0 slices. | Any unfinished money, dispatch, promotion, or upload path must remain disabled. |
| 4-8h | Integrate exact backend/mobile/admin trees; validate migrations on an isolated clone; run build, typecheck, static analysis, focused security, and full automated suites. | Any failed or unrepeatable gate blocks the affected feature. |
| 8-14h | Run cross-role/IDOR, payment race, ride loss/duplicate/delay, OTP failure, expiry, vehicle, and rollback fault tests against one pinned staging SHA. | No local-only result may be treated as staging evidence. |
| 14-18h | Prove both apps on physical Android/iOS devices in foreground, background, terminated, locked, reconnect, denied-location, and lost-response states. | Missing platform/journey evidence blocks that platform or feature. |
| 18-21h | Run a bounded capacity/failure canary and verify alerts, database/Redis saturation, worker ownership, rollback, and data reconciliation. | This is emergency containment evidence, not proof of the full 100k-DAU target. |
| 21-24h | Produce signed artifacts, verify exact versions/store links, obtain release approvals, and begin a small monitored staged rollout with automatic halt thresholds. | Store review time is external and cannot be guaranteed inside the 24-hour engineering window. |

- [x] Owner approved temporary suspension of automated batch payouts and
  promo/points redemption for the emergency release on 2026-07-18. Earnings,
  balances, and existing records must be preserved; the disabled paths must
  fail closed with stable unavailable copy and must not silently consume value.
- [x] Local promo/points containment now fails closed unless two new explicit
  redemption flags are `true`; validation plus promo-bearing ride/job creation
  stop before persistence, and points redemption stops before balance access.
  The four focused backend suites pass 142/142. Client static promo adverts are
  removed and the loyalty screen keeps balance/history visible while showing a
  disabled, truthful redemption state; client analysis is clean. Exact-tree
  integration, staging configuration, deployment, and smoke proof remain open.
- [x] **BR-61 containment approved:** disable every unconverted upload/attachment
  consumer while retaining safe text/status/read-only behavior. Dispatch is a
  core release gate and remains closed unless its evidence passes.
- [ ] Assign one human release commander and one human staging/device tester;
  adding four to six experienced payment/backend, mobile, database/SRE, and QA
  engineers is the minimum credible way to parallelize human review and device
  evidence. The current four-agent software workstreams do not replace those
  operational approvals.
- [ ] Freeze unrelated merges and record one exact SHA/artifact per surface;
  every late change restarts the affected gate.

### Emergency scope/containment matrix

| Surface | Required 24-hour state | Containment if its gate misses | Owner decision |
| --- | --- | --- | --- |
| Authentication/OTP/signup redirect | Must pass on installed client and provider builds using the capped two-number staging policy, then with production-safe limits. | Keep the affected login/signup entry point closed; never report a code as sent without bounded carrier acceptance. | No waiver can make an unverified authentication path safe. |
| Provider eligibility and Go Online | Must derive one stable decision from RM approval, independently approved current documents, selected eligible vehicle, notification reachability, and fresh accurate location. | Keep Go Online closed for identities/vehicles the server cannot prove eligible; use stable actionable reason codes. | Core release gate; no broad verification bypass. |
| Ride booking and sequential dispatch | Must pass durable booking recovery, current-session receipt fencing, 10-second receipt, per-driver 45-second window, logout/offline fencing, and delayed/duplicate delivery tests. | Keep new ride creation/dispatch closed with the existing server-side maintenance gate while active rides remain finishable. | Core release gate; reopening requires evidence. |
| Payment collection, escrow, tips, refunds | Must prove verify-first recovery, conditional transitions, idempotency, and durable webhook receipt. | Block the specific unproved money mutation while preserving status/reconciliation access. | No financial-state waiver without named owner, reconciliation plan, and rollback trigger. |
| Automated scheduled/batch payout | Must gain a durable settlement claim proving that concurrent replicas cannot transfer the same earnings twice. | Temporarily disable automated batch payout; retain earnings and use a separately reviewed/manual reconciliation procedure. | **APPROVED 2026-07-18:** suspend for this emergency release; implementation and deployed proof remain open. |
| Promotions/points redemption | Must implement the approved ride-bound reservation/consume/release ledger and prove cap/idempotency races. | Temporarily reject new redemption with stable unavailable copy; do not silently ignore or over-consume a promotion. | **APPROVED 2026-07-18:** local fail-closed backend plus truthful client containment passes 142 focused tests and client analysis; integrated staging/deployed proof remains open. |
| Media/evidence consumers | Every enabled consumer must use a role-bound durable upload intent and provider-verified object metadata; real object-store proof must pass. | Disable unconverted upload/attachment consumers; keep safe text/status/read-only paths available. | **APPROVED / BR-61:** containment may ship only after route inventory and tests prove every unsafe writer is actually closed. |
| 100k-DAU infrastructure claim | Same-region no-eviction Redis, multi-replica worker fencing, measured load, saturation alerts, recovery, and rollback must pass. | Use a small monitored staged rollout with hard halt thresholds; make no 100k-DAU readiness claim. | A bounded rollout is not approval for full-scale promotion. |

No row may move from “contain” to “enabled” because a deadline expired; it moves
only after the exact integrated artifact has repeatable evidence.

## Next release-critical sequence (CI remediation remains deferred)

- [ ] Resolve the remaining capacity-state, document-replacement, OTP-cap, encryption-custody, media-retention/privacy, geofence-override, and other decisions in section 4; do not encode defaults by assumption. Durable provider auth-session generations are locally implemented and now require integrated migration/staging proof rather than a business-rule decision.
- [ ] Independently review and freeze the combined durable offer/booking/account-session candidate, then rehearse the BR-38 stop-old/start-V2-closed cutover and prove complete migration ordering, PostgreSQL concurrency, Redis-loss recovery, exact receipt/decision timing, and one-driver reservation.
- [ ] Integrate the local minimum-build guard and non-dismissible mobile update state; pin exact release build numbers against the now-verified client/provider store URLs and live versions; gate ride creation, affected media calls, receipt registration, and every actual provider Go Online path; reset old receipt-capability rows only inside the stopped-worker cutover; and prove installed old/new builds before reopening rides.
- [ ] Review and rehearse the local vehicle entity/selection/snapshot model, independently bound roadworthiness/insurance, two-step vehicle/category approval, and online/matching evidence authority; preflight and manually remediate every legacy driver without inferring approval.
- [ ] Rehearse the local seven-day notice/replacement lifecycle, expiry scheduler/outbox, active-work finish/post-work Offline fence, and stable reason codes; complete evidence-backed staging provider repair without running the destructive production migration.
- [ ] Finish the background availability contract: retain the local false-disconnect containment, backend-Offline mobile reconciliation, 30-second/50-metre matching gate, deployment-leased epoch-fenced liveness worker, exact server-owned driver/artisan epochs plus monotonic sample sequences across direct/socket/batch ingestion, invalid-final-token role force-offline, and 15-second/30-metre lifecycle-transition GPS authority; complete provider-token age/rotation policy, production-shaped query/load/failure proof, and installed-device proof.
- [ ] Contain every paid OTP route with the two-number allowlist, Redis caps, kill switch, bounded provider acceptance, authenticated delivery callbacks, and honest UI/error states before tester traffic resumes; auth and payout-method issuance are locally covered, but real Redis/carrier and deployed staging proof remain absent.
- [ ] Preflight encryption without printing or replacing any secret: verify each environment independently reports a present 64-hex-character key, record a non-secret fingerprint, escrow/recovery proof, and pass an existing-ciphertext decrypt plus round-trip canary before the strict validation patch can deploy.
- [ ] Integrate the reviewed backend, mobile, and admin work into one exact staging candidate; run migration/data preflight, cross-role/IDOR and lifecycle E2E, physical-device background/locked journeys, and rollback canaries.
- [ ] Move production Redis beside the Frankfurt API with eviction disabled, then execute the approved 100k-DAU capacity, soak, failure, observability, support, and release-approval gates.

## How to update this file

- `[x]` means the item has fresh evidence for the exact release candidate.
- `[ ]` means pending or not yet re-verified. A historical PASS is not enough.
- `BLOCKED` means another action must complete first.
- `DECISION REQUIRED` means implementation must wait for an explicit business answer.
- Add the date, exact commit/environment, result, and evidence link beside every PASS.
- Never mark a specification, TODO, or code comment as implementation evidence.
- A P0 or P1 waiver needs a named owner, approver, mitigation, expiry, and rollback trigger.
- All times and scheduled payout calculations use GMT unless a rule below says otherwise.

## 0. Current release gates

| Gate | Status | Why | Exit criteria |
| --- | --- | --- | --- |
| Backend PR #96 / `staging` merge | **MERGED; CI REMEDIATION DEFERRED** | PR #96 is merged at `1f1ada8…`. On 2026-07-16 the owner explicitly prioritized restoring isolated staging and core release defects over CI repair. This is not a CI PASS or authorization to release production. | Restore and test staging first; repair E2E/schema/container gates before production promotion. |
| Start the staging API | **PASS — LIVE** | On 2026-07-17 the hardened image skipped boot migrations, verified protected Ghana fingerprint `ece9c…`, connected to DB and staging-prefixed Redis/Socket.IO, and started in `staging`. Independent `/v1/health` returned HTTP 200 with DB/Redis `ok`; fetched `origin/staging` remains `1f1ada8…`. | Continue monitoring; retain the exact Render event/build identity and rollback reference. |
| Reopen staging testing | **BLOCKED FOR LIVE OTP CONTAINMENT** | The owner approved live Arkesel SMS and WhatsApp for two developers, but the deployed public OTP endpoint cannot enforce that scope: there is no phone allowlist, global/provider cap, or kill switch, and initial SMS returns success before provider acceptance. Core public canaries passing does not contain billing/abuse. | Deploy and verify the allowlist, Redis-backed cap, timeout, honest result/error handling, and delivery telemetry; then restrict testing to the two privately configured numbers. |
| Merge `staging` to `main` | **BLOCKED** | Full mobile/backend/admin audit, bug closure, load proof, security/privacy review, and staged acceptance are incomplete. | No unwaived P0/P1; all production gates and approvals pass. |
| Production release | **BLOCKED** | Current production Redis is cross-region and eviction-enabled; 100k-DAU and disaster-recovery evidence is absent. | Production infrastructure, capacity, resilience, canary, monitoring, rollback, and support gates pass. |

Important repository control finding: the current GitHub plan does not make branch
protection available for this private backend repository. PR #96 was therefore
merged even though it had no review and its hosted checks were red. Paid
GitHub-hosted Actions are not an approved option. Until a dedicated self-hosted
gate exists, the release owner must manually enforce the exact-SHA evidence rule;
merging does not authorize deployment.

## 1. Immediate action queue

### 1A. Contain and re-gate the merged backend change

- [x] Owner policy recorded: do not pay for GitHub-hosted Actions.
- [ ] **P0 / BLOCKED** Pin, review, and land one exact backend SHA. The current uncommitted integrated tree passes every local workflow equivalent, but it is not yet a reviewed release commit:
  - [x] Lint & Typecheck PASS on the exact tree in clean Node 20 Linux: deployment guards 12/12, formatting PASS, lint 0 errors/48 warnings, and typecheck 10/10.
  - [x] Current-tree migration replay and seed PASS on disposable PostgreSQL for all 109 migrations. The harness now holds one bound loopback listener per suite instead of relying on per-request Supertest server open/close. The latest gate includes a real PostgreSQL liveness-worker scenario: 51/51 pass once and five complete cross-suite runs pass 255/255 with test cron jobs proven unmounted. The prior 102-migration run also passed the protected-config trigger integration test and `prisma migrate status`.
  - [x] Current-tree API unit gate PASS: 171 suites/3,507 tests; API/E2E typecheck, API/database build, Prisma validation, changed-file lint with zero errors, and diff checks PASS.
  - [x] Current-tree Ride/Artisan Lifecycle E2E PASS: both suites and 51/51 tests pass after repairing stale fixtures, lifecycle assertions, HTTP listener ownership, and the migration-backed liveness path; E2E typecheck also passes.
  - [x] Docker Build Verification PASS from pristine merge commit `1f1ada8…`; clean context 6.86 MB and local image manifest list digest `sha256:e156ce16432955981b365abdc8b12a8ed4bfded6d69fe8f1c0a48c00c35e4611`. Extra audit smoke: the Node 22 image started in isolated test mode and `/v1/health` returned HTTP 200 with DB/Redis `ok`.
- [x] Exact audited head verified as `3f27dd9825b47a82280a6ae6013d8988f8a0fae4` in a clean detached checkout before generated artifacts; its tree is identical to merge commit `1f1ada8b7ad3b9b8ab937fe8fab3dad611aae7bd`.
- [ ] Store durable command logs, timestamps, tool/container versions, clean-worktree proof, and PASS/FAIL evidence outside this conversational audit.
- [ ] For recurring releases, provision a dedicated disposable Linux self-hosted runner; do not install a persistent runner on a workstation holding production credentials.
- [ ] Resolve review policy: recommended minimum is one independent approval; record an explicit owner risk acceptance if operating solo.
- [x] GitHub state captured: PR #96 had no recorded reviews and was merged by `Ayiks` at 2026-07-16 21:16:48 GMT.
- [x] Render `myshop-api-test` auto-deploy is OFF (owner dashboard confirmation, 2026-07-16).
- [x] Merge commit pinned: `staging` is `1f1ada8b7ad3b9b8ab937fe8fab3dad611aae7bd`; its push-triggered hosted CI run failed at startup, and no deploy workflow run was listed for that commit.
- [ ] Land the local E2E fixture/lifecycle repairs in the reviewed release change without treating the shared `User` referral model as approval of BR-03.
- [x] The current integrated tree has a separate E2E typecheck and it passes; retain it in the release gate so fixture drift cannot hide behind the normal test exclusions.
- [ ] Rerun the complete approved local/self-hosted gate on the resulting exact `staging` commit and record all-green evidence before the next release-candidate deployment or any `main` promotion.
- [x] The owner explicitly deferred CI remediation and manually restored staging after the geofence/database safety gates passed. This exception does not authorize merging `staging` to `main` or releasing production.

### 1B. Prepare the first isolated staging start

- [x] Render's critical GitHub deploy incident began 2026-07-16 22:42 GMT and was later resolved; the official API returned no unresolved incidents after the successful 2026-07-17 deployment.
- [x] A resumed/stale container was identified from its logs: it contained only 68 migrations and the pre-hardening entrypoint, attempted boot migration and seeding, and therefore was not merged staging SHA `1f1ada8…`. The protected boundary trigger correctly rejected its worldwide seed write; the preceding category upserts are create-if-missing/idempotent.
- [x] Runtime proof confirms `RUN_MIGRATIONS_ON_BOOT=false`; no seed ran in the successful deployment. Retain a dashboard check for duplicate linked-group values as configuration evidence.
- [x] Confirmed the failing Render logs belong to `myshop-api-test` (staging), not `myshop-api` (production).
- [x] Image build succeeds for merged staging SHA `1f1ada8…`; the observed failure is during entrypoint geofence verification, not compilation or image publication.
- [x] Canonical PostgreSQL JSONB fingerprint calculated independently for the approved Ghana-wide staging polygon: `ece9c4133725ed4c5131bbab7b900205` (2026-07-16). This value is staging-only and must never replace the production Ashanti fingerprint.
- [x] Live staging DB check (owner-executed, 2026-07-16): `pilot_region_boundary` is an unsafe almost-worldwide dev/test polygon with fingerprint `6f77f914d5374fd7463b14ad8c931d67`, not the approved Ghana-wide staging value. The merged seed writes this value whenever `NODE_ENV !== 'production'`; do not seed deployed staging.
- [x] Live staging state established: Ghana migrations `20260708000000` and `20260711000000` are successfully applied; protection migration `20260716000000` has no row, `protected_platform_config` does not exist, and no protection triggers were returned. This proves the worldwide seed overwrite happened after the force-Ghana migration and before protection.
- [x] Corrected the unprotected live staging boundary with a guarded transaction after confirming migration/artifact state; owner-returned verification at 2026-07-16 22:40:57 GMT shows the approved Ghana staging value and exact fingerprint `ece9c4133725ed4c5131bbab7b900205`.
- [x] Applied migration `20260716000000_protect_pilot_region_boundary` through the staging direct endpoint on 2026-07-16. Post-migration verifier returned `ece9c4133725ed4c5131bbab7b900205`, protected-trigger integration smoke passed, and Prisma reported all 69 migrations up to date.
- [ ] **P2 / dependency compatibility:** explicitly use `sslmode=verify-full` before the next `pg-connection-string`/`pg` major upgrade; current packages warned that future `sslmode=require` semantics will weaken. This warning did not fail the migration or current verification.
- [x] Protection migration is applied through the direct non-pooler staging endpoint; boot migrations remain disabled and the direct URL remains outside Render.
- [x] Successful startup proves Render has the approved `EXPECTED_PILOT_REGION_BOUNDARY_MD5`, explicit geofence enforcement, disabled boot migration, and protected DB state.
- [x] Independent check at 2026-07-17 05:53:47 GMT: `/v1/health` returned HTTP 200, `status=healthy`, database `ok`, and Redis `ok`.
- [x] Public staging canaries at 2026-07-17 05:55 GMT: OTP channel discovery, ride categories, and current surge endpoints all returned HTTP 200.
- [x] **Limited staging exception:** owner approved live Arkesel SMS and WhatsApp for the current two-developer test phase (2026-07-17). This does not authorize production release or broader staging access.
- [ ] **P0 / live-channel containment:** the deployed service cannot enforce “two developers only.” The exact-current local backend defaults paid OTP delivery to off, supports an exact E.164 `allowlist` or explicit `live` mode, and uses Redis server time plus atomic sliding windows for deployment-wide global and phone/channel reservations. Phone numbers never appear in OTP Redis keys; auth, paid-send, provider-circuit, payout-destination, and refund-destination multi-key controls share explicit Redis Cluster hash slots. Redis errors or malformed authority responses fail closed. New-code issuance, same-code resend/cooldown, OTP state, failed-attempt limits, single-use consumption, bounded provider HTTP, sanitized failures, and a deployment-wide circuit with one leased half-open probe are implemented. Real Redis E2E proves quota scope, physical-key privacy, shared circuit open/half-open/close behavior, resend authority, three-attempt invalidation, and exactly one winner for concurrent correct verification. Exact-current evidence passes **180/180 suites and 3,767/3,767 tests**, API/E2E typechecks, build, lint with zero errors, diff check, all **187 migrations**, and five complete **68/68** fresh-stack E2E runs (**340/340** total). Keep this P0 open: the owner must approve separate staging and production cap values and privately configure the two-number staging allowlist; then deploy one reviewed SHA and prove carrier callbacks, kill switch, cost/delivery alerts, installed-device behavior, immediate disable, and credential rotation before paid staging traffic resumes.
- [ ] **P0 / misleading OTP success:** deployed staging still dispatches initial Arkesel SMS fire-and-forget and can respond as though a code was sent before provider acceptance. The local candidate now awaits a bounded provider result and returns a stable retryable failure while retaining the ambiguous code for fallback; provider acceptance still must not be called handset delivery. Review, integrate, deploy, and prove the installed-app navigation/error policy before closure.
- [ ] Complete authenticated/correlated Arkesel delivery evidence and OTP accepted -> delivered/failed -> verified latency telemetry without logging raw recipients/codes. The local auth-SMS candidate now attaches a unique 256-bit expiring capability per send, binds the returned carrier message ID, accepts the documented `sms_id`/`status` query contract over GET or POST, rejects expired/mismatched attempts, and applies idempotent monotonic transitions without a reusable callback secret. Non-sandbox paid Arkesel mode requires a credential-free HTTPS callback URL. This is unit-tested only: exercise callback-before-response, duplicates, mismatch, expiry, Redis outage, and real carrier delivery on isolated staging; link verification latency to the issued code; and separately replace or disable the still-mismatched legacy notification-SMS callback.
- [ ] **OTP provider evaluation (no speed claim yet):** run an instrumented Ghana-carrier bake-off against Arkesel and one secondary route. Official Arkesel V2 supports message IDs, delivery webhooks/status lookup, and its OTP response means processing/queued—not handset delivery—so first correct the current telemetry. Hubtel is the recommended local pilot candidate because its official API exposes OTP send/verify/resend plus a USSD fallback and states direct connectivity to Ghana networks; confirm contracted production throughput because the public developer documentation advertises a very low default rate limit. Infobip 2FA is a second global candidate with delivery logs/webhooks. Twilio requires registered Ghana sender IDs and states that unregistered IDs are blocked on affected networks from 8 July 2026, so it is not a drop-in emergency switch. Compare p50/p95/p99 accepted-to-delivered and accepted-to-verified by MTN/Telecel/AT, failure rate, cost, support SLA, and sender-registration lead time before choosing routing priority.
- [ ] Confirm the exact Meta-approved WhatsApp authentication-template shape. Current code assumes four body parameters and one dynamic URL button; `one_tap` does not supply the Android package/signature-hash fields needed for actual one-tap autofill.
- [x] **P0 / emergency-recording containment:** recording is deferred from v1,
  defaults OFF, is explicitly pinned OFF in the deployment Blueprint, and
  production configuration refuses startup if it is enabled. SOS remains
  available without recording; the API and apps make no recording claim.
  Reintroducing recording requires a separate privacy/legal/storage/access and
  signed-device release gate.
- [x] **P1 / email containment:** email is deferred from the emergency release.
  It now requires exact `FF_EMAIL_NOTIFICATIONS=true`, the production
  configuration refuses that value, the Blueprint pins it false, and retained
  SES credentials cannot silently reactivate the channel. The dormant weekly
  template no longer promises the suspended 18:00 payout schedule. Re-enabling
  email requires reviewed templates/sender identity, bounce/complaint handling,
  operating ownership and end-to-end delivery evidence in a later release.

- [ ] Record staging and production resource fingerprints without exposing secrets.
- [ ] Prove the staging Neon branch is not the production branch/endpoint.
- [ ] Determine whether the staging Neon branch contains cloned production PII; replace with synthetic data or complete approved sanitization if it does.
- [ ] Confirm runtime `DATABASE_URL` is the pooled staging URL and contains `-pooler`.
- [x] Desired guard confirmed: `DIRECT_DATABASE_URL` is absent from the long-running Render service (owner dashboard confirmation, 2026-07-16).
- [ ] Inject the staging direct non-pooler URL as `MIGRATE_DIRECT_DATABASE_URL` only into the approved migration executor: a GitHub Environment for a dedicated self-hosted workflow, or an isolated secrets manager/one-shot local environment. Never add it to the long-running Render service.
- [ ] Protect `RENDER_DEPLOY_HOOK` through the same executor-specific mechanism and rotate any exposed copy.
- [x] Reviewed and deployed the Ghana-wide staging polygon with canonical DB fingerprint `ece9c4133725ed4c5131bbab7b900205`; post-migration verification and protected-trigger smoke passed.
- [ ] Verify every staging external integration is isolated: Paystack test mode/callbacks, Firebase, storage/Cloudinary, OTP callbacks, WhatsApp, Maps, email, and TURN. For deferred Smile automation, prove credentials are absent and both old initiation/callback paths return 404.
- [ ] Decide and implement a controlled bootstrap migration path that does not depend on a deploy hook succeeding while the service is suspended.
- [x] Choose one safe first-start path:
  - [x] Render dashboard successfully deployed the hardened latest `staging` head with the saved environment after the incident resolved; retain the Render event/build ID as durable proof.
  - [ ] Create a replacement staging service with isolated configuration, migrate first, deploy the exact reviewed commit, validate it, then retire the old suspended service.
- [ ] Take a staging DB snapshot and verify restoration metadata before migrations.
- [x] Applied all migrations through the direct staging endpoint and passed the post-migration boundary, trigger smoke, and zero-pending checks for exact staging SHA `1f1ada8…` (2026-07-16).
- [x] Deployed the hardened latest `staging` head; fetched `origin/staging` is `1f1ada8b7ad3b9b8ab937fe8fab3dad611aae7bd`, and startup contains its fail-closed entrypoint/geofence behavior. Capture the Render event SHA/image digest to remove the remaining provenance inference.
- [x] `/v1/health` returned HTTP 200 with both DB and Redis `ok`.
- [ ] Complete every isolation canary in section 9 before adding testers beyond the two approved developers.

### 1C. Contain the verified provider-reset and ride-loss defects

- [ ] **P0 / staging data impact:** run the read-only [staging provider-impact audit](provider-verification-staging-impact.sql) against the Neon `staging` branch and record only the row count/provider IDs needed for remediation; do not paste phone numbers, document URLs, or other PII into this tracker.
- [ ] Take/identify a pre-migration staging PITR point or temporary clone from before `20260703010000_requeue_pending_provider_documents`. The migration overwrote prior verification/stage/online fields without before-images, so current rows alone cannot prove the correct previous status.
- [ ] Restore affected staging providers from a pre-migration source when available. Otherwise perform evidence-backed individual review; never bulk-mark the audit-query output approved, and leave availability offline until each provider explicitly goes online.
- [ ] **P0 / production migration containment:** after a verified production snapshot, run the read-only [production provider preflight](provider-verification-production-preflight.sql). The approved migration executor must then run `npx prisma migrate resolve --applied 20260703010000_requeue_pending_provider_documents` against production before `migrate deploy`, capturing reviewer, exact output, migration status, and rollback/PITR reference. Do not execute its DML, and do not edit or delete the already-applied migration.
- [ ] Land the provider-eligibility backend patch only after review. Latest evidence on 2026-07-17: 107 API suites and 2431/2431 tests pass; the active-role user suites pass 83/83; API typecheck/build, config build, relevant-file formatting, lint with 0 errors/48 existing warnings, and diff check pass. The local `/users/me` projection now queries only the authenticated role and fails closed when that role is missing or soft-deleted. The incompatible provider-document uniqueness migration, legacy-data preflight, migration smoke, cross-system review, and staging journeys remain blockers.
- [ ] **P0 / durable ride and booking candidate implemented locally; staging proof still open.** BR-36 is represented by a PostgreSQL `ride_offers` ledger with capability-scoped socket+FCM delivery, authenticated receipt, database-clock 45-second decisions, idempotent actions, unique open-holder constraints, bounded leased workers, atomic dispatch readiness, persisted rings, Redis-failure recovery, cross-role denial, and cancel/accept race protection. BR-37 adds a client-scoped UUID booking key, canonical SHA-256 request binding, a unique PostgreSQL invariant, authenticated live-state recovery, same-key replay, conflict rejection, and bounded two-minute cancellation only when dispatch never became ready and no offer exists. BR-39 prevents a new delivery attempt after five minutes while retaining a timely receipt's full 45 seconds. Latest ride-branch evidence is 99 suites/2341 tests, focused ride tests, API/database build and typecheck, target lint, diff check, and Prisma generate. The separate minimum-version candidate passes 22 focused tests and 98 suites/2301 baseline API tests; the mobile candidate passes client 40/40, provider 76/76, API client 98/98, and shared UI 23/23 with fatal analysis. It remains P0 until exact patches are reviewed/integrated, exact builds/store URLs are pinned, stale token capability is replaced with current authenticated online-session authority, migrations and query plans pass on isolated real PostgreSQL, real concurrency/restart/failure/device/load evidence passes, and the controlled canary/rollback is approved.
- [ ] **P0 / lost ride-create response candidate pending database/device proof.** The local backend now makes the client-generated booking UUID durable and unique per client, binds it to the request hash, exposes `GET /rides/booking-attempts/:key`, returns live terminal/nonterminal state, and never uses Redis as idempotency authority. The local client persists only UUID/fingerprint/time, resolves before every retry or changed request, keeps the key across ambiguous errors, and serializes double taps. Focused and full affected-package tests pass. Still prove response loss at every post-commit boundary, concurrent POST/lookup/cleanup races on PostgreSQL, app kill/reinstall/logout/account-switch behavior, and the mandatory-client-update rollout before closure.
- [ ] **P0 / BR-38 controlled cutover locally fenced, not production-shaped or staging rehearsed.** The owner chose mandatory client and provider updates plus pause/drain/stop-all-old/migrate/start-V2-closed/reopen; mixed workers, old mobile builds, and ordinary rolling deployment are forbidden. Both the API and a `BEFORE INSERT` PostgreSQL trigger now enforce `rides_enabled`, so a legacy process or stale config-cache read cannot insert after the gate closes; the API maps the database refusal to stable `503 RIDE_BOOKING_PAUSED`. On fresh PostgreSQL, a direct insert while closed returned SQLSTATE `23514`, both exact session/vehicle source migrations refused an active ride before their destructive resets, and driver/session/token/ride/offer sentinels remained unchanged. The [cutover runbook](ride-v2-controlled-cutover.md) preserves active-ride APIs, requires zero legacy work twice, explicitly fences stale Redis config, and defines a no-mixed-protocol rollback boundary. It remains P0 until exact builds/store URLs, receipt reset/re-registration, production-shaped clone and staging rehearsals, installed old/new device proof, and authorized execution close.
- [ ] **P0 / ride migration ordering contained locally; production-shaped refusal/race proof pending:** `20260718130000_add_ride_online_sessions` now requires the database `rides_enabled=false` gate and the approved stopped-old-worker cutover, refuses active work before mutation, bounds metadata-lock waits, installs populated-table CHECK constraints through `NOT VALID` plus write-compatible validation, and defers all populated-table indexes to reviewed standalone concurrent migrations. It contains no explicit table-wide lock. The complete 183-migration replay, migration contracts, real online-DDL write-continuity proof, and five E2E runs pass. Still prove active/racing states and zero mutation on refusal on an authorized production-shaped clone and exact staging artifact; old workers must remain stopped through the full chain.
- [ ] **P0 / promo migration preflight ordering contained locally; unsafe-state proof pending:** the required preflight originally queried snapshot columns created only by the later promo migration. It is now schema-aware: before additive DDL every active promo ride is reported unresolved; after DDL it checks null frozen terms. The clean path is included in the fresh 108-migration replay. Still prove unsafe pre-migration states refuse before mutation on an isolated production-shaped database.
- [ ] **P0 / role-lifecycle migration source hazards contained; scale/refusal proof pending:** `20260718221000_role_account_lifecycle` rejects unsupported legacy status before DDL, bounds lock/statement waits, maps all three lifecycle indexes exactly in Prisma, and builds those populated-table indexes in standalone concurrent migrations. The complete 183-migration replay, online write-continuity proof, and contracts pass. It still updates legacy role rows; inventory multi-role suspended/deleted rows and prove bounded runtime, refusal-before-mutation, retry, and application behavior on a production-shaped clone before closure.
- [x] **P1 / provider-document index drift closed:** `ProviderDocument` maps the non-partial vehicle index to `provider_documents_vehicle_current_idx`, while the two replacement-grace and two current-authority partial indexes are explicitly SQL-only because Prisma cannot represent their predicates. On a fresh PostGIS database all **108 migrations** applied; the provider migration contract passed **5/5**; and `prisma migrate diff` proposed no provider-document index create/drop, so it would not duplicate or replace these indexes. The broader non-provider-document schema drift remains open separately below.
- [ ] **P0 / vehicle-cutover race contained locally; legacy go-live remediation pending:** `20260718140000_provider_vehicle_eligibility` requires `rides_enabled=false` plus the approved stopped-old-worker cutover, bounds metadata-lock waits, refuses active work before mutation, installs ownership triggers before forcing legacy rows Offline, validates new populated-table foreign keys without holding a write-blocking scan, and defers required indexes and legacy-index removal to standalone concurrent migrations. It contains no explicit table-wide lock, and the complete 183-migration replay passes. It intentionally fabricates no Vehicle, document, or approval for legacy drivers; every affected driver remains unable to go live until individually inventoried, evidence-backed, and approved Coordinator→RM. Run the PII-free impact preflight, prove active/racing refusal with zero mutation on the production-shaped clone, complete operator remediation, and prove previously verified providers can select an eligible vehicle and Go Online before release.
- [ ] **P1 / populated-table online DDL implemented locally; production-shaped timing pending:** all **65** indexes targeting tables that predate the pending rollout now live in manifest-controlled standalone, exactly-one-statement `CREATE [UNIQUE] INDEX CONCURRENTLY` migrations; four required legacy removals are separate one-statement `DROP INDEX CONCURRENTLY IF EXISTS` migrations. Ordinary index builds remain only on tables first created within the same pending rollout. Source migrations contain no invalid-remnant cleanup, ordinary index drop, explicit `LOCK TABLE`, or `ACCESS EXCLUSIVE`; dependent postflights run only after the online builds and verify exact name/readiness/validity/table binding. Pinned Prisma 7.6 applied all **183 migrations** twice; the permanent PostgreSQL proof allowed an ordinary writer within 750 ms during every build/drop, verified all catalog states, and proved invalid builds fail closed until the authorized recovery script removes only invalid/not-ready remnants. Migration/static contracts pass in the full **3,697/3,697** API gate, and five E2E runs pass **280/280**. The seven release preflights are now DB-enforced read-only, bounded, aggregate-only, and included in the real E2E leakage proof. Keep P1 open until those exact preflights, data-volume timing, failure/resolve/retry, and the exact pinned artifact pass on an authorized production-shaped clone and staging; no production execution or downtime approval is implied.
- [ ] **P0 / mobile release codes still unknown:** REL-03 fixes both marketing versions at `1.4.1`, resolving the public provider `1.4.0` collision. Both pubspecs now declare `1.4.1+20`, but production builds reject the local build-number floor and require an explicit console-verified `RELEASE_BUILD_NUMBER`; do not choose it until the highest private build numbers are read from App Store Connect and Play Console. Then rebuild and prove signed artifacts.
- [ ] **P0 / iOS distribution signing unavailable:** both current-source archives compile, and the client archive embeds a valid app privacy manifest declaring precise location, Government ID, and sensitive information, but App Store IPA export fails with `No Accounts` / no `iOS Distribution` certificate. Previous IPAs predate the final privacy declarations and must not be uploaded. Restore an authorized Apple Distribution account/certificate, then rebuild, export, Transporter-validate, and device-test both exact release artifacts.
- [ ] Current exact-tree mobile evidence on 2026-07-18: `melos run test` passes all six test-bearing packages, including client 47/47 and provider 100/100; `melos run analyze` passes all seven packages with `--fatal-infos` and no issues. This proves source-level regression and static-analysis gates only. No newly signed Android/iOS release artifact or installed-device journey yet proves BR-37, the 375-second BR-39 ceiling, the intent-only confirmation contract, background location, or mandatory-update handling.
- [ ] Commit and review mobile changes only after the backend contract is pinned; run the full provider/API-client suite, Android/iOS build smoke, and real-device staging journeys before merge.

## 2. Environment inventory

These checkmarks mean the owner-supplied facts are recorded, not that dashboard
configuration or runtime isolation has been independently verified.

### Staging

- [x] Neon project name recorded: `myshop`.
- [x] Neon branch recorded: `staging`.
- [x] Neon region recorded: AWS `eu-central-1` (Frankfurt).
- [x] Runtime Neon URL reported as pooled (`-pooler`).
- [x] Render service recorded: `myshop-api-test`, Oregon, `https://myshop-api-test.onrender.com`, live and healthy as of 2026-07-17 05:53 GMT.
- [x] Upstash database recorded: `myshop-redis-staging`, AWS Oregon / `us-west-2`, Pay as you go, eviction disabled, no replicas/import.
- [x] Render values reported saved-only: staging `REDIS_URL`, `REDIS_KEY_PREFIX=myshop:staging:`, `REDIS_SOCKET_IO_CHANNEL_PREFIX=myshop:staging:socket.io`, `RUN_MIGRATIONS_ON_BOOT=false`, and `SEED_DATABASE=false`.
- [x] Render staging Auto-Deploy is OFF (owner dashboard confirmation, 2026-07-16).
- [x] Desired guard confirmed: Render staging has no `DIRECT_DATABASE_URL` (owner dashboard confirmation, 2026-07-16).
- [ ] Verify saved `DATABASE_URL` points to the staging Neon branch, not production.
- [x] Runtime startup proves `PILOT_REGION_CHECK_DISABLED=false`; the hardened entrypoint would otherwise refuse Render startup.
- [x] Runtime verified `EXPECTED_PILOT_REGION_BOUNDARY_MD5=ece9c4133725ed4c5131bbab7b900205` against the protected staging DB.
- [ ] Verify no duplicate/colliding variables arrive from a linked Render environment group.
- [ ] Reassess staging topology: API/Redis are in Oregon while Neon is in Frankfurt; move or explicitly accept the latency/egress limitation before performance testing.

### Production

- [x] Render service recorded: `myshop-api`, Frankfurt.
- [x] Existing Redis recorded: `myshop-redis`, Cape Town, Pay as you go, eviction enabled.
- [x] Production service boundary decision recorded: Ashanti Region.
- [ ] Fingerprint the production Neon project/branch/endpoints and record its region.
- [ ] Provision and cut over to a dedicated production Redis near the Frankfurt API with eviction disabled, monitored capacity, and an approved migration/drain plan.
- [ ] Do not toggle the existing production Redis eviction policy in place or change its key/channel prefix during live work without a coordinated drain/restart plan.
- [ ] Verify production `REDIS_KEY_PREFIX=myshop:` for the first compatible rollout and `REDIS_SOCKET_IO_CHANNEL_PREFIX=socket.io` until every replica can switch together.
- [ ] Verify production auto-deploy is OFF, boot migrations/seed are OFF, geofence bypass is false, and the approved Ashanti fingerprint is set.

## 3. Confirmed business decisions

The decision is checked only because the owner confirmed it. Each corresponding
implementation and test remains unchecked elsewhere in this document.

- [x] **BR-01** Every provider document is approved or rejected independently.
- [x] **BR-02** Artisan eligibility requires Ghana Card plus exactly one of Business Registration or Trade Certificate.
- [x] **BR-03** One phone may have at most one client, one driver, and one artisan account; each is a separate single-role entity with no cross-account data leakage.
- [x] **BR-04** A provider awaiting replacement-document review may remain enabled only until the earlier of: seven days after upload confirmation, or the old document's invalidity boundary. Notify appropriately and auto-disable at that deadline.
- [x] **BR-05** A printed expiry date is inclusive in GMT: a document expiring on 17 July remains valid through 23:59:59 GMT on 17 July and becomes invalid at 00:00 GMT on 18 July. Send an advance notice seven days before expiry.
- [x] **BR-06** Providers need background location permission to receive dispatches while backgrounded. The app may request/educate, but the operating system controls the grant.
- [x] **BR-07** Ride offers are strictly sequential: A gets 45 seconds; only decline/expiry closes A and gives B a fresh 45 seconds.
- [x] **BR-08** A solo artisan may handle one request; a non-solo artisan may handle at most three.
- [x] **BR-09** A client may have one active ride and one active artisan job at the same time, but never two active rides or two active jobs; enforce server-side.
- [x] **BR-10** Driver acceptance automatically transitions the ride to `en_route`; no second driver action is required.
- [x] **BR-11** Arrive/start/end are proximity-gated; outside the geofence, warn and offer an explicit, audited “continue anyway” path.
- [x] **BR-12** Cancellation before the relevant waiting period affects the driver; after it elapses it affects the client. The client consequence is GH₵5 added to the next ride.
- [x] **BR-13** Logout and account deletion are forbidden while that account has an active ride, active job, or unresolved payment.
- [x] **BR-14** Tips support card and MoMo, carry zero commission, belong fully to the provider, and appear in provider earnings.
- [x] **BR-15** Commission is admin-controlled and fetched dynamically from the database; no hardcoded 20% may affect quotes, settlement, copy, or reports.
- [x] **BR-16** On-demand and scheduled payouts are supported; scheduled payout calculations use GMT.
- [x] **BR-17** The dispute window remains 24 hours. Both the artisan dispute clock and artisan two-hour payout hold start when the client confirms completion, recorded authoritatively as `artisanJob.clientConfirmedCompleteAt`; ride clocks start at `ride.completedAt`. An approved refund is repaid digitally to the client and the provider amount is clawed back or recovered with an auditable ledger path when funds have already left escrow.
- [x] **BR-18** One redeemed loyalty point is GH₵0.50; elected redemption reduces the amount payable on a later request.
- [x] **BR-19** Operational/security notifications are mandatory for online offers, ride/job state, payment/payout, safety/SOS, verification expiry/suspension, and security/session changes. Marketing is optional. Admin 2FA is mandatory; provider biometric unlock is optional and device-local.
- [x] **BR-20** SOS uses a three-second hold, visible countdown, haptics, and cancellation during the hold; activation then occurs immediately without a second confirmation.
- [x] **BR-21** Deleted role accounts are soft-deleted and retained for 90
  exact 24-hour periods (`2,160` hours) from `deletedAt`; the private phone-auth
  identity and sibling role accounts remain untouched. Recovery follows BR-63
  and post-retention disposition follows BR-64. Neither workflow may be
  enabled before its complete authority, storage, audit, and failure contracts
  pass release evidence.
- [x] **BR-22** Rejected/suspended providers may enter the dashboard and see the rejection/suspension note verbatim; this does not grant dispatchability.
- [x] **BR-23** A driver may register multiple vehicles but must select exactly one when going online; only one vehicle can be active per online session.
- [x] **BR-24** Production is Ashanti-only; staging is Ghana-wide on isolated infrastructure.
- [x] **BR-25** The replacement-review grace clock starts only after upload confirmation. An admin may send notices, but a notice does not start or reset that clock.
- [x] **BR-26** Provider approval is a mandatory two-person pipeline: the matching coordinator approves and forwards; only a Regional Manager's final approval permits first go-live.
- [x] **BR-27** Driver mandatory evidence is Ghana Card, driver's licence, profile photo, roadworthiness certificate, and insurance certificate. Roadworthiness and insurance are separate records attached to the relevant vehicle.
- [x] **BR-28** Driver's licence, roadworthiness certificate, and insurance certificate are all expiry-enforced using the inclusive-GMT rule. Expired evidence on an inactive vehicle blocks only that vehicle; it does not block an otherwise eligible driver using another eligible selected vehicle.
- [x] **BR-29** After the exact 10 km driver-search ring is exhausted without an eligible available driver, cancel the request and show the client: “All drivers are busy. Please try again.”
- [x] **BR-30** Location authority thresholds are approved: a provider fix used for Go Online or matching must be no older than 30 seconds and have reported accuracy of 50 metres or better; arrive/start/end geofence actions require a fix no older than 15 seconds and accuracy of 30 metres or better.
- [x] **BR-31** A provider cannot go online without notification permission and at least one usable registered device token; the UI must explain how to restore access.
- [x] **BR-32** A prior online intent may be restored after process death/relaunch only after the server revalidates full eligibility and the device supplies a fresh location meeting BR-30. Failed revalidation leaves the provider offline with a stable actionable reason.
- [x] **BR-33** If location permission, GPS, or a fresh fix is lost during active work, the active ride/job may continue but the provider becomes non-dispatchable for new offers. Show a persistent provider alert and a temporary-location-unavailable client notice, escalate to admin/safety after 120 seconds, clear the degraded state on recovery, and force Offline after completion until a fresh BR-30 fix is revalidated.
- [x] **BR-34** Reviewer bypass is disabled during normal production operation. Prefer an isolated review environment; if a production review window is unavoidable, it must be time-boxed to a restricted test identity with no real PII, money movement, live provider work, or SOS access, plus rate limits, alerts, a kill switch, and immediate post-review disable/rotation.
- [x] **BR-35** Smile Identity automation is deferred beyond v1. V1 identity and document verification is manual: each document is decided independently, the matching coordinator approves and forwards, and only the Regional Manager's final approval permits go-live. Preserve dormant Smile implementation for a future reviewed release, but expose no v1 initiation or callback route and make no Smile-verification claim in user-facing copy.
- [x] **BR-36** Ride-offer delivery uses a configurable ten-second receipt window. Send the same idempotent offer through socket and FCM together; only an authenticated device receipt may activate that driver's fresh authoritative 45-second decision window. No receipt advances to the next driver without penalizing the skipped driver. Late/duplicate receipts and decisions must be idempotent and can never create two active holders.
- [x] **BR-37** Each rider booking action carries a durable client-generated idempotency key. If `POST /rides` commits but its response is lost, every retry with the same authenticated client and key must recover and return that same ride and current state; it must never create or dispatch a second ride. A ride that never becomes dispatch-ready may be auto-cancelled after two minutes, with the key continuing to resolve to that terminal result.
- [x] **BR-38** V2 ride-offer activation requires a mandatory provider-app update and a controlled maintenance cutover: pause new bookings, drain or explicitly resolve every actionable legacy request, prove zero legacy work, stop all old workers, migrate/deploy/verify V2, and only then reopen. Mixed old/new ride-dispatch workers and an ordinary rolling deploy are forbidden.
- [x] **BR-39** The authoritative matching clock starts when the ride becomes dispatch-ready. Do not create a new driver delivery attempt after five minutes, and do not start one unless its full ten-second receipt window fits before that cutoff. A driver who authenticates receipt before the cutoff keeps the full fresh 45-second decision window; after that final decision ends, cancel unmatched work with the approved BR-29 notice.
- [x] **BR-41** If required driver or selected-vehicle evidence expires during an active trip, the accepted trip continues with GPS tracking, status transitions, and completion enabled. The driver becomes immediately ineligible for every new offer, receives a persistent clear alert naming the expired evidence and required action, and is forced Offline after the active trip completes until a valid replacement is independently approved.
- [x] **BR-42** Drivers create their own vehicle records and may edit them only before approval. Once a vehicle is approved, it is immutable to the driver; only an authorized administrator may edit it. A driver may request removal of a vehicle, but only an authorized administrator may actually retire/remove it.
- [x] **BR-43** Every new vehicle follows the same mandatory two-person approval pipeline as provider go-live: a coordinator approves and forwards it, and only a Regional Manager's final approval makes that vehicle selectable for an online session.
- [x] **BR-44** An authorized admin edit to an approved vehicle creates a new pending vehicle version and repeats coordinator then Regional Manager approval. The old approved version remains authoritative while the edit is reviewed; an immediate safety block must be a separate explicit audited admin action, not an inferred consequence of an ordinary edit.
- [x] **BR-45** A physical vehicle/normalized registration plate may be registered to one driver account only; duplicate active registration across drivers is forbidden.
- [x] **BR-46** Ride categories are assigned and approved separately for each vehicle, not generally at driver level. Matching and accepted-ride snapshots must use the selected vehicle's approved categories.
- [x] **BR-47** Every vehicle-category assignment or change requires two different reviewers: a coordinator approves and forwards it, and a Regional Manager gives final approval before that category can authorize matching.
- [x] **BR-48** A promotion's immutable booking-time type, value, and maximum discount are applied to the final metered pre-promotion fare. The final discount cannot exceed either that fare or the approved promotion cap; quote fields, final pre-promotion fare, discount, and final client charge remain distinct.
- [x] **BR-49** The dynamic commission rate is snapshotted at booking and applied to the final pre-promotion fare. Providers do not fund a platform promotion through a reduced commission base; tips remain outside every commission base.
- [x] **BR-50** Cancellation fee/window terms are snapshotted at booking and later admin changes affect new bookings only. Cancellation must remain possible if the snapshot is missing or invalid; no automatic fee is charged in that case and the anomaly is sent for operational review.
- [x] **BR-51** One private phone-auth identity may anchor exactly one client, one driver, and one artisan account, each with its own non-exposed role-account ID. Profiles and all business data are role-scoped; deleting one role leaves siblings untouched, while BR-21 applies to the deleted role. Sessions for different roles may coexist only when every authorization check is bound to the JWT role.
- [x] **BR-52** Loyalty balances and ledgers, referral codes/history/rewards, and emergency contacts are independently owned by each role account. None may be read, mutated, credited, or deleted through a sibling role.
- [x] **BR-53** Active-trip driver fallback is allowed only after all eligible idle drivers are exhausted. The active driver must be within ten minutes of completing the current trip, the new pickup must be within two kilometres of that trip's final drop-off, and the driver may hold at most one queued next ride. The current trip finishes first, the new rider sees the adjusted ETA, and BR-36's ten-second receipt plus fresh 45-second decision window still applies.
- [x] **BR-54** Ride time pricing uses trusted exact seconds converted to fractional minutes: 20 seconds is one-third of a minute. The configured minimum fare alone protects the monetary floor. If trusted duration is missing, add no invented duration or time charge.
- [x] **BR-55** Legacy vehicle authority is never inferred. Before the vehicle cutover, run the PII-free production preflight and manually remediate every affected driver with explicit vehicle ownership, selection, evidence binding, category approval, Coordinator approval, and Regional Manager final approval. No migration may fabricate an approval or expiry.
  - Approved implementation clarification: when an authorized admin explicitly binds an existing valid roadworthiness or insurance row to the confirmed physical vehicle, preserve that document's independent approval decision and printed expiry date and audit the binding. This preservation never approves the vehicle or a ride category; both remain pending through their required Coordinator then Regional Manager decisions. Missing, expired, conflicting, or unreadable evidence still requires normal remediation.
- [x] **BR-56** Deterministically migrate legacy loyalty, referral, and emergency-contact ownership only when the private phone identity has exactly one role; quarantine every multi-role ambiguous record for manual assignment without copying or guessing. For the emergency release, pause all loyalty earning/redemption/mutations, referral linking/codes/rewards, and admin reward mutations while preserving balances/ledgers/refunds. Emergency contacts remain enabled and exact-role scoped.
- [x] **BR-57** Normal logout blocks active interactive work and payment collection, but not long-running provider settlement after completed work. Role deletion blocks active work and every unresolved exact-role financial liability. Unaccepted offers/bids/directed quotes are atomically withdrawn or requeued rather than blocking. A lost/stolen-device force logout is restricted to global Super Admin/Operations, requires reason/audit/alerts, preserves work for reattachment, and never permits soft-delete bypass.
- [x] **BR-58** Session-recovery list/detail/dismiss/revoke authority is restricted to global Super Admin/Operations; regional/category-scoped coordinators and managers have no access. Recovery must not expose sibling-role linkage through the shared phone and remains disabled until exact role-account plus target-SID compare-and-revoke, concurrency, legacy redaction, and failure-recovery proof pass.
- [x] **BR-59** Going Offline clears the selected vehicle. Every later Go Online requires a fresh explicit eligible-vehicle selection. Vehicle switching is forbidden while online, while holding an actionable offer, or during active work; no pending/active work may silently move to another vehicle.
- [x] **BR-60** For the emergency release, cancellation itself remains available but every automatic ride/job cancellation fee, debt carry-forward, rating/star/counter penalty, and cancellation-triggered financial transfer is suspended. Existing records are preserved and the UI/API must state the containment truthfully.
- [x] **BR-61** A carrier timeout keeps the same still-authoritative OTP; the user may explicitly request another configured channel, but no automatic paid fallback is permitted. Any upload/attachment consumer that has not completed the role-bound durable-intent and provider-verified object contract is disabled while safe text/status/read-only functionality remains available.
- [x] **BR-62** A client receiving a refund for a cash-origin payment must enter the refund MoMo number and network and OTP-verify that destination. Snapshot the verified destination on the refund, execute the digital refund idempotently, and create the provider clawback only after the refund is authoritatively successful. Missing/expired verification, destination mismatch, unknown transfer outcome, or duplicate retry must fail closed without creating a clawback.
- [x] **BR-63** A deleted role may be restored during its 90-day retention
  period, ending exactly `2,160` hours after `deletedAt`. The user must initiate
  a phone-OTP-verified request; direct
  phone-only restoration is forbidden. A retained same-role registration is
  blocked with truthful support guidance rather than creating or silently
  restoring an account. User-initiated and admin-initiated deletion follow the
  same recovery policy. Provider restoration returns the role to pending
  verification, revalidates its documents, and requires the category-responsible
  Coordinator followed by Regional Manager final approval before Go Online.
  Client recovery remains pending after OTP until global Operations/Super Admin
  approval, then restores only the client role. Provider recovery follows
  intake by a named regional `Admin`, category-responsible Coordinator review,
  and Regional Manager final approval. Intake restores only the requested
  provider role as pending verification and Offline, resets its current and
  replacement documents to review, clears its session/location/vehicle
  authority, and leaves both sibling roles untouched. This release exposes no
  recovery rejection or dismissal action: an unresolved request remains pending
  until its approved transition or exact deadline expiry; no unapproved
  rejection/resubmission policy may be inferred.
- [x] **BR-64** Once the exact BR-21 recovery deadline passes and no applicable
  legal hold, open dispute, unsettled payment/refund, or clawback exists, purge
  the role through a transactional, idempotent, retryable, and audited job.
  Preserve only a non-personal tombstone (random role ID, role, `purgedAt`, and
  policy version) so retained ledgers keep valid references; unlink it from the
  private phone-auth identity and delete profile/personal fields, documents,
  role-owned notifications, sessions/devices, rewards/referrals/emergency
  contacts, and other role-owned personal state. Retain the minimum
  transaction/accounting facts for at least six years, or longer while law or
  a hold requires them. Remove unnecessary exact addresses, GPS routes,
  messages, photos, and other embedded personal evidence unless an active hold
  requires it, then erase it when that hold ends. Delete role-owned objects
  within 24 hours and verify by seven days; backups expire within 30 days and
  any restore must reapply completed purges. Keep the private phone-auth
  identity while any sibling role exists; remove it after the final role is
  purged and no hold requires it. Automatic purge remains disabled for this
  release until this complete contract is implemented, legally reviewed, and
  proved. A hold may be released only by the responsible Legal/Safety owner and
  a different Operations/Super Admin approver. Exact post-closure retention
  periods for non-tax dispute, safety, security, and audit records remain a
  Legal decision, so no automatic permanent deletion is authorized yet.
- [x] **BR-65** `INSTANT_TRANSFERS_ENABLED_AT` is the exact backend deployment-
  live timestamp for the reviewed payout-outbox release. Payments created
  before it remain retained for exact manual reconciliation and may never fall
  back to the legacy direct-transfer path.
- [x] **BR-66** Provider-controlled replacement of a still-valid approved
  document is closed. The upload control reopens only at 00:00 GMT after the
  printed expiry date; expiry blocks Go Online but must never block submission
  of a replacement once that boundary has passed. Approved non-expiring
  evidence remains provider-locked and requires authorized support/admin
  handling. Rejected evidence remains resubmittable. The API, not the handset
  clock, is authoritative for the open/closed state.
- [x] **BR-67** Once a provider profile photo is independently approved, the
  provider cannot replace it. Only an authorized administrator may change it
  through the separately audited admin route.
- [x] **BR-68** Required expiring driver evidence uses durable role-scoped
  notifications at upload confirmation, the existing seven-day notice, 72
  hours, 24 hours, two hours, and the exact GMT invalidity boundary. In-app and
  push are used at every stage; SMS is added only at 24 hours and invalidity.
- [x] **BR-69** New scheduled artisan-job creation is suspended for this
  emergency release. Existing scheduled commitments remain readable and may
  continue through their normal confirmation, reminder, cancellation, and
  lifecycle paths so accepted work is not orphaned.
- [x] **BR-70** SOS is available to every authenticated, non-deleted client,
  driver, and artisan role account. A missing active booking or GPS fix must not
  suppress platform recording or dialer access. When a booking is supplied it
  must be active and owned by the exact authenticated role; no sibling role is
  inferred. Booking and available device-location context are attached when
  present.
- [x] **REL-01** Mobile, backend, and admin `staging` branches are release candidates; `main` is production.
- [x] **REL-02** During the current two-developer staging test phase, live Arkesel SMS and WhatsApp channels may be used for real-carrier OTP testing. This exception is staging-only and must be reconsidered before adding testers or releasing production.
- [x] **REL-03** Use marketing version `1.4.1` for both client and provider apps. Build codes remain undecided until the highest private build numbers are read from App Store Connect and Play Console; each new code must be greater than its store maximum.

## 4. Business decisions still required

No implementation may silently choose these rules. Resolve them in reviewed
batches and add the answer to the decision log.

### Documents and provider eligibility

- [x] **RESOLVED by BR-66:** provider upload stays closed while approved
  evidence is valid and reopens only after the exact inclusive-GMT expiry
  boundary. Expiry affects Go Online eligibility, not the reopened submission
  path. Rejected evidence remains resubmittable.
- [x] **BR-02 artisan profile-photo alignment:** only Ghana Card plus exactly one of Business Registration or Trade Certificate is a document prerequisite for artisan go-live. A profile photo may be uploaded and independently reviewed, but it is optional for dispatch eligibility and must not make mobile stricter than backend authority.
- [x] **RESOLVED by BR-67:** an approved provider profile photo is admin-only;
  the user cannot initiate a replacement or silently alter dispatch evidence.
- [x] **RESOLVED by BR-68:** upload confirmation, seven-day, 72-hour, 24-hour,
  two-hour and invalidity notifications; SMS only at 24 hours and invalidity.
- [x] Driver's licence, roadworthiness, and insurance are all expiry-enforced (BR-28).
- [x] Expired evidence on an inactive vehicle blocks only that vehicle (BR-28).
- [x] Smile Identity is not a v1 prerequisite and must remain disabled/deferred (BR-35).
- [ ] **DECISION REQUIRED** Confirm whether a clear police check is a mandatory prerequisite to coordinator/RM approval and first go-live.

### Vehicle lifecycle

- [x] Drivers add their own vehicles and may edit them before approval; only an authorized administrator may edit an approved vehicle (BR-42).
- [x] A driver may request removal, but only an authorized administrator may retire/remove the vehicle (BR-42).
- [x] Every new/additional vehicle requires coordinator approval and Regional Manager final approval before selection (BR-43).
- [x] Drivers must explicitly select one eligible vehicle when going online; do not silently auto-select even when only one is stored (BR-23).
- [x] **BR-59 vehicle switching:** going Offline clears selection; every later Go Online requires a fresh explicit selection. Switching is forbidden while online, holding an actionable offer, or performing active work; no offer/ride may silently move vehicles.
- [x] Selected-vehicle or driver evidence that expires during an active trip follows BR-41: finish the accepted trip, block new offers, keep a persistent actionable alert, then force Offline after completion.
- [x] One physical vehicle/normalized plate may belong to only one driver account (BR-45).
- [x] Ride categories are assigned and approved separately to each vehicle (BR-46).
- [x] Vehicle-category assignment follows BR-47: one coordinator approves and forwards it, then a different Regional Manager gives final approval. A driver-level category approval never authorizes another vehicle.
- [x] **BR-55 legacy remediation:** run the PII-free production preflight and manually remediate every affected driver; never fabricate vehicle ownership, approval/category, document binding, or expiry.
- [x] An admin edit of an approved vehicle creates a pending immutable version, preserves the old approved authority during review, and requires the full two-step approval again (BR-44).
- [x] Selected vehicle is cleared on Offline and must be explicitly selected at the next Go Online (BR-59).

### Account identity and separation

- [x] BR-51 permits one private phone-auth identity with three separate, non-exposed role-account IDs beneath it. The auth identity is never presented as a client, driver, or artisan account entity.
- [x] Internal phone-auth linkage is permitted only as private authentication/security infrastructure; no sibling role profile or business data may be exposed or authorized through it.
- [ ] **DECISION REQUIRED** Must account type be chosen before OTP verification?
- [x] Client, driver, and artisan sessions may coexist on separate apps/devices only when every endpoint, socket, device registration, and resource authorization is strictly bound to the JWT role and role-account ID.
- [x] Deleting one role account leaves both sibling accounts untouched; only that role enters the BR-21 soft-delete/purge lifecycle.
- [x] BR-52 makes loyalty, referrals, and emergency contacts separate per role account, including balances, ledgers, codes, history, rewards, reads, writes, and deletion.
- [x] **BR-56 legacy role ownership:** deterministically migrate only single-role records; quarantine every multi-role ambiguous loyalty/referral/emergency record for manual assignment without copying or guessing.
- [x] **IMPLEMENTED LOCALLY / BR-13:** normal logout, self-delete, admin delete, and emergency force-logout now share the exact-role database exit guard. Active work/payment blockers, unaccepted offer/bid withdrawal, role-local lifecycle checks, and sibling isolation are covered. Client ride/job writers share a durable SID/generation row fence with exit; both PostgreSQL race orderings passed. Driver and artisan auth sessions now use the same durable exact-role SID/generation authority.
- [x] **BR-57 work matrix:** active interactive work includes accepted queued BR-53 successors, `artisan_marked_complete`, and `pending_payment`; unaccepted offers/bids/directed quotes are atomically revoked, withdrawn, or requeued rather than blocking exit.
- [x] **BR-57 finance split:** normal logout blocks active work and payment collection only; role deletion additionally blocks every unresolved exact-role liability. Failed/no-money attempts never create permanent blockers.
- [x] **BR-57 security recovery:** lost/stolen-device force logout is global Super Admin/Operations only, with mandatory reason, audit, alerts, and work-preserving reattachment. Admin soft-delete never bypasses blockers.
- [x] **IMPLEMENTED LOCALLY / exact-role exit serialization:** client ride and artisan-job creation take the exact client SID/generation lock as their first transaction statement; logout/delete/force-logout take the conflicting role lock, clear the SID, and advance generation. The migration applied twice on PostgreSQL 18; work-first committed and made logout observe the blocker, while logout-first made the late work insert commit zero rows. Redis/socket/device cleanup remains post-commit and idempotent; migration `20260718263000_client_booking_session_fence` must be applied before the API because boot migrations are OFF.
- [x] **P1 / provider legacy-session residual closed locally:** driver and artisan rows now carry durable active auth SID and generation authority. Login, refresh, legacy migration, HTTP authorization, role exit, and recovery all enforce the exact role row; a Redis cleanup failure cannot restore database-invalid access, and pre-fence sessions bind only after Redis proves the exact SID. The migration and forced-logout behavior still require the pinned staging artifact, multi-replica Redis-loss proof, and installed provider reauthentication evidence before release.
- [x] **IMPLEMENTED LOCALLY / HTTP and WebSocket role-session enforcement:** HTTP, location, chat, and call paths now require the exact active role-account ID plus current SID; sensitive socket events revalidate after awaits, socket takeover/revocation covers every namespace, sibling-role sessions remain isolated, and peer events expose public role-account IDs rather than the private auth identity. Focused socket evidence plus the full 161-suite API gate pass. Keep the P0 operationally open until installed-client, multi-replica Redis, disconnect/reconnect, and deployed force-logout proof passes.
- [ ] **IMPLEMENTED LOCALLY / disabled pending integrated staging proof:** new session-recovery requests are explicitly bound to an immutable exact role-account ID and target SID; a replacement durable SID returns `RECOVERY_SESSION_CHANGED`. Legacy phone/device-only rows are redacted and non-actionable, dismiss/revoke share the recovery-row lock, lower-role grants are removed, and backend/admin gates default OFF. The durable resolving saga and current admin status surface pass local unit, typecheck, build, migration, and real database/Redis E2E gates; installed-app and pinned multi-replica staging proof remain open.
- [x] **Exact-SID recovery correction:** the local candidate now persists the immutable target SID and role-account ID and compares that SID before revocation, so an old request cannot deliberately revoke a newer session.
- [x] **P0 / recovery crash consistency closed locally:** approval first commits a PostgreSQL exact-role SID/generation fence and immutable leased `resolving` operation before Redis/socket/token cleanup. Cleanup is compare-and-revoke, failures retain safe retry state with bounded backoff, expired leases are claimed with `FOR UPDATE ... SKIP LOCKED`, finalization re-proves the durable fence, resolved replay is idempotent, and purge excludes `resolving`. Real PostgreSQL/Redis E2E crashes after the durable fence, proves the old token is already rejected while Redis still contains the old SID, resumes from another reconciler claim, and proves exactly one final audit. Keep `FF_SESSION_RECOVERY=false` until the exact integrated staging artifact passes multi-replica process/Redis fault injection, installed-app, operator, and audit evidence.
- [x] **BR-58 recovery authority:** only global Super Admin/Operations may list, inspect, dismiss, or revoke recovery; scoped coordinators/RMs have no access. Keep the feature disabled until technical proof passes.
- [x] **P0 / public recovery challenge closed locally:** when `FF_SESSION_RECOVERY=true`, the exact `ALREADY_LOGGED_IN_ELSEWHERE` conflict issues a random 256-bit capability with a five-minute TTL, stored only by hash and bound to phone hash, user, exact role account, target SID, and requesting device. Filing atomically consumes it before touching the victim's phone quota; expiry, replay, role/device/phone mismatch, changed session, malformed state, and Redis failure fail closed. When recovery is disabled, conflicts expose no capability and both apps hide the unavailable support action. Real HTTP/PostgreSQL/Redis E2E proves disabled containment, one exact request, replay `410`, no duplicate row, durable crash recovery, and idempotent resolved replay; the exact gate passes **65/65** and five complete fresh-stack runs pass **325/325**. Keep recovery OFF pending integrated staging and installed-app evidence.
- [x] **BR-58 recovery privacy:** do not expose sibling-role linkage through the shared phone. Legacy role-unknown rows redact inferred role/session/device data and allow dismissal only; immediate mobile copy must not claim support notification when no request was filed. Requester notification channels after resolution remain disabled pending a separately approved channel contract.

### Capacity and dispatch

- [ ] **DECISION REQUIRED** Confirm the final artisan-capacity state set and lower bound. The safe local candidate counts `confirmed`, `artisan_en_route`, `arrived`, and `in_progress`; a directed `admin_assigned` request also reserves a slot without double-counting when accepted. Ordinary pending marketplace bids do not yet reserve a slot, and scheduled reservation timing remains separate. Solo is exactly 1; non-solo is capped at 3, but confirm whether administrators may configure non-solo as 1–3 or only 2–3.
- [x] BR-53 limits the exceptional driver capacity to two rides total: one active trip and one queued next ride.
- [x] BR-53 defines proximity as: active driver within ten minutes of current completion and new pickup within two kilometres of the final drop-off.
- [x] BR-53 requires exhaustion of every otherwise eligible idle driver before active-trip candidates may enter matching.
- [ ] **IMPLEMENTED BUT DISABLED / BR-53:** the local backend candidate is seeded OFF and fail-closed. It enforces idle-ring exhaustion, current `in_progress` predecessor, fresh backend ETA, final-drop-off proximity, one successor/predecessor, exact eligibility, and the normal receipt/decision window under database locks. It must remain OFF for this release until the following decisions, mobile state, and durability gates close.
- [ ] **IMPLEMENTED LOCALLY / BR-53 migration rehearsal pending:** the follow-up moves refusal checks ahead of mutation under a write fence, makes core DDL retry-aware, preserves/rebuilds concurrent indexes safely, forcibly upserts the flag to `false` before and after index work, and verifies every required index plus the OFF flag. Focused contracts pass 11/11. Keep it out of the emergency release until apply/failure/operator-resolve/retry is proven on isolated PostgreSQL; a lock-timeout refusal requires a quieter maintenance retry.
- [ ] **DECISION REQUIRED / BR-53 lifecycle:** define predecessor terminal/cancellation/admin-force-completion behavior, whether pre-`in_progress` states qualify, queued-rider cancellation/waiting time, queued-driver cancellation consequences, ETA drift handling, and destination/stop edits.
- [ ] **DECISION REQUIRED / BR-53 ranking and disclosure:** approve active-driver ranking and whether adjusted ETA is immutable or live with delay alerts.
- [x] **DEFERRED / P0 before BR-53 enablement — durability and eligibility:**
  `ride_active_trip_fallback_enabled` is database-seeded false and its migration
  postflight refuses a non-false value. Before a later enablement, add a durable
  activation/recovery worker or outbox, resolve non-completion terminal
  predecessors, revalidate every eligibility/session/location authority at
  activation, and make activation notification durable.
- [x] **DEFERRED / P0 before BR-53 enablement — mobile safety:** the feature
  remains unavailable in this release. A later release must build an explicit
  queued-successor model/UI on both apps that preserves active navigation and
  prove locked/background/duplicate/late journeys before changing the database
  flag.
- [x] **DEFERRED / P0 before BR-53 enablement — cancellation and privacy:** the
  feature remains unavailable in this release. A later release must implement
  the approved queued cancellation policy, hide predecessor location/identity,
  and add database transition constraints before enablement.
- [x] **DEFERRED / P1 before BR-53 enablement — scale:** fence ETA-worker
  leases, avoid route refreshes without fallback demand, reject missing/zero
  duration, and load/cost-test the route-call model before enablement.
- [x] **DEFERRED by BR-69:** new scheduled artisan jobs are unavailable in this
  release, so scheduled capacity reservation is a later-release decision.
- [x] Two-stage offer delivery is approved: persist the attempt, send socket and push together, activate only after authenticated device receipt, then give that driver a fresh 45 seconds (BR-36).
- [x] The configurable delivery-receipt timeout is ten seconds; unacknowledged attempts advance without driver penalty and late acknowledgements are rejected idempotently (BR-36).
- [x] Lost-response booking behavior approved: recover the same durable ride for the same authenticated client booking key; auto-cancel only never-dispatch-ready initialization after two minutes (BR-37).
- [x] V2 activation approved: mandatory provider update plus controlled maintenance drain/stop-old/start-new cutover; no mixed-version rolling deployment (BR-38).
- [x] The client app is mandatory at the same BR-38 cutover as the provider app. The minimum-version gate must prevent old clients without BR-37 booking keys from reopening V2 bookings.
- [x] Matching lifetime approved: no new attempt after five minutes from dispatch-ready, require its full ten-second receipt window to fit, and preserve a timely recipient's full 45-second decision window (BR-39).
- [x] After the exact 10 km ring is exhausted, cancel and show “All drivers are busy. Please try again.” (BR-29). The exact client event/push copy is implemented and tested in the local narrow ride patch; it is not deployed.
- [x] A driver may hold only one actionable offer at a time. Under BR-53, accepting the exceptional offer creates the sole queued next ride; no further offer may be delivered until capacity returns.
- [ ] **IMPLEMENTATION COMPLETE LOCALLY / EVIDENCE PARTIAL / BR-30:** mobile Go Online plus server driver/artisan candidate selection enforce age <=30 seconds and accuracy <=50 metres. Every direct/socket/background-batch driver write and direct artisan write now requires the exact server-owned Online epoch, a strictly increasing sample sequence, and a row-locked strictly newer PostgreSQL capture time before any Redis/liveness/trail/broadcast effect. Duplicate sequences, old epochs, future/out-of-order fixes, and Offline authority all fail closed without secondary effects; Offline clears both roles' session and sequence authority. Ride and artisan arrive/start/end now require a device fix no older than 15 seconds and no worse than 30 metres; one proof cannot skip multiple lifecycle checkpoints, and missing/stale/inaccurate proof fails with stable codes before mutation. Installed-device, clock-skew, multi-replica, network/Redis-failure, lifecycle-geofence-radius/override, and load proof still require completion.
- [x] **BR-30 location authority approved:** issue a new server-owned location session at each successful Go Online/recovery, require a monotonic sample sequence inside it, and permanently fence every older session so delayed batches cannot rewind current location.
- [ ] **DECISION REQUIRED** Set ride-offer and delivery-outbox audit retention.
- [ ] **DECISION REQUIRED** When no initial driver exists, persist then cancel the request for audit, or retain the current immediate error contract?

### Background availability and request reachability

- [ ] **IMPLEMENTATION PARTIAL / BR-31:** mobile provides contextual permission/token recovery; backend blocks Offline-to-Online without an active token; driver offer creation requires current-session receipt capability; and driver/artisan matching requires an active role token. A permanent FCM invalid-token result deactivates only the exact failed token and role; if it was the final eligible role token, the provider is forced Offline, session/location authority and Redis presence are cleared, exact-role sockets receive `availability:forced_offline`, sibling roles remain untouched, and active ride/job completion remains allowed. Registration and invalidation serialize on the user row and the exact FCM value prevents a stale send failure from invalidating a replacement token. Provider-token age/rotation policy, full artisan receipt assurance, and installed locked/background proof remain open.
- [x] Active-work location loss follows BR-33: allow the current work to finish, block all new offers, alert both sides, escalate after 120 seconds, and force Offline after completion until a fresh fix passes BR-30.
- [ ] **IMPLEMENTATION COMPLETE LOCALLY / EVIDENCE PARTIAL / BR-32:** a successful explicit Go Online stores only an exact driver/artisan role-account intent. Relaunch restoration waits for Firebase readiness, requires the retained selected driver vehicle where applicable, uses the ordinary server eligibility endpoint with a fresh BR-30 location, and does not silently trigger OS permission or Settings prompts. Role/account mismatch fails closed; active work stays with ride/job recovery. A failed revalidation consumes the stale intent, requests authoritative Offline, and shows safe actionable copy derived only from stable reason codes; if Offline confirmation also fails, the UI truthfully says Offline could not be confirmed. Provider recovery/store/notice/error tests pass in the current **135/135** provider gate. Installed foreground/background/terminated/locked, permission, network-loss, Firebase-delay, exact-role coexistence, and staging-server evidence remain open.
- [ ] **DECISION REQUIRED** Approve truthful support wording for backgrounded/locked, OS-terminated, user-force-stopped, and rebooted states; current code does not support all of them equivalently.
- [ ] **DECISION REQUIRED** Approve idle versus active-work sampling cadence, required precision, and battery budget.

### OTP delivery and encryption custody

- [x] Current staging carrier tests are restricted to the two developers' privately configured E.164 numbers; a public request must never trigger a paid SMS, WhatsApp message, or voice call outside that allowlist (REL-02).
- [ ] **DECISION REQUIRED** Approve the staging and production global paid-send cap and per-phone/per-channel cap. The local containment defaults of 20 total attempts/minute and 5 attempts/phone/channel/10 minutes are safety placeholders, not a production capacity decision.
- [x] **BR-61 carrier timeout:** keep the same still-authoritative code and truthfully offer explicit retry/another configured channel; do not invalidate a code that may still arrive.
- [x] **BR-61 no automatic paid fallback:** channel changes are user-triggered only; no automatic SMS/WhatsApp/voice ordering may silently incur another send.
- [ ] **DECISION REQUIRED** Are Ghana Card numbers needed after successful identity/document approval? If not, define verified deletion/anonymization rather than indefinite encrypted retention.
- [ ] **DECISION REQUIRED** Should the currently unused driver/artisan encrypted Ghana Card number columns remain, or should provider identity rely only on independently reviewed document evidence?
- [ ] **DECISION REQUIRED** During encryption-key rotation, may encrypted iOS Live Activity tokens be purged and re-registered with standard push fallback, or is uninterrupted migration required?
- [ ] **DECISION REQUIRED** Name the encryption-key escrow authority, approved secret/recovery store, rotation cadence, old-key overlap, and recovery-drill owner.
- [ ] **DECISION REQUIRED** For undecryptable Ghana Card data, choose user re-submission/re-verification versus a security incident with no automatic account-state change.
- [ ] **DECISION REQUIRED** Confirm whole-API fail-closed startup when the encryption canary cannot authenticate (recommended), or explicitly design and approve a degraded mode.
- [ ] **DECISION REQUIRED** May an account/profile merge copy encrypted fields? Record-bound authenticated context intentionally requires an authorized decrypt/re-encrypt operation rather than copying ciphertext.

### Cancellation

- [x] **BR-60 emergency containment:** cancellation remains available, but every automatic ride/job fee, debt carry-forward, rating/star/counter penalty, and cancellation-triggered financial transfer is suspended for this release. The unresolved permanent policy below is post-release and must not leak into the contained path.

- [ ] **DECISION REQUIRED** Define which clock “waiting time” means and approve its exact duration; three minutes exists in code but was not approved here.
- [ ] **DECISION REQUIRED** Define whether GH₵5 applies after an acceptance free window, a pickup no-show wait, or both.
- [ ] **DECISION REQUIRED** Decide who receives GH₵5: waiting driver, platform, or a split.
- [ ] **DECISION REQUIRED** Define the exact driver consequence before the wait elapses.
- [ ] **DECISION REQUIRED** Decide whether debt carries forward after another cancellation, whether it is commissionable, and how cash/no-authorization collection works.
- [ ] **DECISION REQUIRED** Define an equivalent artisan-job cancellation policy, if any.

### Tips, commission, payouts, and loyalty

- [ ] **DECISION REQUIRED / fare snapshot:** Confirm that base/per-km/per-minute/minimum fare, wait charge/window, surge, quote-floor/cap inputs, category, and other price-affecting rules are snapshotted when a ride becomes a priced booking and remain immutable through completion. Current completion code re-reads mutable admin/category values and can change an in-progress ride's final charge.
- [x] **BR-54 minimum time charge:** price trusted exact seconds as fractional minutes, rely on the configured minimum fare for the monetary floor, and add no invented time when trusted duration is missing. The existing forced one-minute completion charge must be removed and regression-tested before release.
- [x] **Cancellation snapshot decided (BR-50):** cancellation-free window and fee are immutable booking terms for new rides. Missing or corrupt terms never trap cancellation or invent a charge: cancel for free and create an operational-review anomaly.
- [ ] **DECISION REQUIRED** Tips: immediate transfer or inclusion in on-demand/scheduled earnings payout?
- [ ] **DECISION REQUIRED** Who pays tip processing fees, refunds, and chargebacks?
- [ ] **DECISION REQUIRED** Is commission global or scoped by provider type, category, region, or service?
- [x] **Commission base decided (BR-49):** snapshot the dynamic commission rate at booking and apply it to final metered pre-promotion fare; the platform funds promotions and tips are excluded.
- [x] **Promotion settlement decided (BR-48):** freeze type/value/cap at booking, apply it to final metered pre-promotion fare, cap the discount at both fare and approved promo cap, and preserve distinct quote/final/discount/client-charge fields.
- [x] **Commission snapshot lifecycle decided (BR-49):** booking time.
- [ ] **DECISION REQUIRED** Approve scheduled payout cadence/time, cutoff, minimum, weekends/holidays, retries, and partial on-demand payouts. Current 18:00 GMT code is not approval.
- [ ] **DECISION REQUIRED** Does loyalty redemption apply to rides, artisan jobs, or both?
- [ ] **DECISION REQUIRED** Approve point earning rate, redemption cap, funding source, cancellation/payment-failure restoration, promotion/debt/tip combinations, and expiry.
- [x] **BR-40 promotion lifecycle approved:** atomically reserve at ride booking; consume exactly once when the ride reaches `completed`, regardless of whether immediate payment collection succeeds; release only when the ride never completes because of initialization failure, exhausted/no-driver or another system cancellation, driver cancellation, or client cancellation. Every retryable collection failure retains the same consumed reservation, with no payment retry deadline or payment-failure release. Cancellation abuse is controlled separately and never by silently confiscating the promotion.
- [ ] **DECISION REQUIRED / promotion scope:** confirm whether admins may create ride-only, artisan-job-only, and both-service promotions. Source currently disagrees between `ride`/`artisan_job` and `rides`/`jobs`; do not normalize production data until the canonical values and legacy repair are approved.

### Geofencing, disputes, SOS, retention, and suspension

- [ ] **DECISION REQUIRED** Approve arrive/start/end radii and whether override requires a reason, photo, admin review, or audit record only.
- [x] Reviewer geofence/device/OTP bypass is not permitted as a standing normal-production capability; apply the isolated/time-boxed controls in BR-34.
- [x] App-review accounts follow BR-34: isolated review environment preferred; any unavoidable production window is restricted, monitored, kill-switch controlled, and disabled/rotated immediately afterward.
- [ ] **FACT CONFIRMATION / BR-34:** establish from retained production configuration/audit evidence whether the old `REVIEWER_TEST_PHONE`/fixed-OTP bypass was ever enabled in production. If it cannot be disproved, revoke every reviewer client/driver/artisan session, refresh/role-selection token, device token, and outstanding OTP; force providers Offline; and rotate the fixed OTP before release.
- [ ] **DECISION REQUIRED / BR-34:** set the maximum reviewer-bypass window. “Any future timestamp” is not a meaningful time box.
- [ ] **DECISION REQUIRED / media privacy:** classify profile, provider-document, job, portfolio, chat, support, dispute, and safety evidence as public or private, define authorized readers and signed-URL lifetime, and set confirmed/staging/orphan retention plus malware/quarantine requirements.
- [x] **FINANCIAL DECISION / dispute clock and scope:** ride disputes start at `ride.completedAt`; artisan-work disputes and the artisan payout hold start only when the client confirms completion, persisted as `artisanJob.clientConfirmedCompleteAt`; both use the approved 24-hour admission window and two-hour payout hold. Approved refunds are repaid digitally to the client, with provider funds clawed back or recovered through an auditable ledger path.
- [x] **FINANCIAL DECISION / hold versus dispute:** retain the 24-hour dispute window, hold provider payout for two hours, and use an auditable recovery/clawback path for disputes raised during hours 2–24 after funds have left escrow.
- [x] **FINANCIAL DECISION / partial refund economics:** recompute provider earnings and commission from the retained post-refund amount. The implementation must use the immutable commission-rate snapshot, preserve original charge/refund audit facts, pay only the recomputed provider entitlement before payout, and claw back only actual provider overpayment after payout; missing legacy rate snapshots must fail closed for manual review.
- [x] **RESOLVED by BR-70:** any authenticated, non-deleted role may raise SOS;
  active booking and GPS are optional context, never prerequisites.
- [x] **RETENTION DECISION / BR-64:** apply the full tombstone/selective-erasure
  contract recorded in BR-64. Minimum transaction/accounting retention is six
  years; object deletion is due within 24 hours and verified within seven days;
  backups expire within 30 days and restored backups reapply completed purges.
  Exact addresses, GPS, messages, photos, and other embedded personal evidence
  are not retained merely because the parent business record remains.
- [x] **BR-64 hold release governance:** a hold may be released only by the
  responsible Legal or Safety owner plus a different global Operations/Super
  Admin approver. Placement, evidence, scope, reason, both identities and both
  timestamps are immutable audit facts; one actor may not satisfy both stages.
- [ ] **DECISION/LEGAL REVIEW REQUIRED / BR-64 retention:** set exact retention
  periods for non-tax dispute, safety, security, and audit facts after closure.
  Until those values, the implementation, legal review, and storage/backup proof
  pass, the purge worker remains fail-closed.
- [x] **BR-64 retained identity:** preserve a minimal non-personal tombstone
  role ID for restrictive foreign-key references. Do not physically delete the
  referenced role row or retain all embedded personal fields indefinitely.
- [x] **RECOVERY DECISION / BR-63:** recovery is user-initiated and phone-OTP
  verified during the 90-day retention period; retained same-role registration
  is blocked with support guidance; user/admin deletion share the policy; and
  providers return to pending document/Coordinator/RM review rather than
  Online eligibility. The old unauthenticated phone-only `/auth/recover` route
  remains unregistered because it does not satisfy this workflow.
- [x] **RECOVERY DECISION / BR-63 clock:** the recovery deadline is exactly
  `deletedAt + 90*24 hours` (`2,160` hours), not the end of a GMT calendar day.
- [x] **RECOVERY DECISION / BR-63 client approval:** after phone OTP, client
  recovery requires one global Operations/Super Admin approval and restores
  only the client role. Provider recovery instead follows intake by a named
  `Admin` in the provider region, category Coordinator approval/forwarding,
  then Regional Manager final approval.
- [x] **RECOVERY DECISION / BR-63 provider deadline:** the user must file and
  the named regional Admin must accept intake before the 2,160-hour deadline.
  Timely intake restores only that provider role into pending verification;
  Coordinator/RM revalidation may finish after the deadline, with the provider
  remaining Offline until RM final approval. No recovery rejection or automatic
  post-intake expiry is inferred.
- [ ] **DECISION REQUIRED** Define which dashboard actions remain available to rejected/suspended providers.
- [ ] **DECISION REQUIRED** Define precedence when suspension, document expiry, and replacement review overlap.

## 5. Initial verified findings to track

- [x] **P0 / staging geofence incident contained:** staging previously contained seed-only worldwide fingerprint `6f77f914d5374fd7463b14ad8c931d67`. It now contains approved Ghana fingerprint `ece9c4133725ed4c5131bbab7b900205`; protection migration, fingerprint verification, trigger smoke, disabled deployed seeding, and fail-closed startup all passed. The approximate staging geometry and controlled future rotation remain separate open work.
- [ ] **P1 / protected configuration API containment implemented locally; rotation authorization pending:** `pilot_region_boundary` is no longer in the general PATCH allowlist and receives stable `CONFIG_KEY_PROTECTED` before any database/cache access; the existing fingerprint trigger remains defense in depth. Focused config evidence passes **46/46** and API typecheck/diff checks pass. Keep P1 open because ordinary config writes still use the read-named `view_config` permission, and boundary rotation still needs owner-approved distinct write authorization, typed GeoJSON validation, dual approval/audit, fingerprint rotation, and staging rehearsal.
- [ ] **P1 / geofence completeness — provider-path bypasses closed locally:** driver and artisan direct Online plus cached-location Online now share the current fail-closed boundary authority; direct fixes must be valid, no more than 30 seconds old, and no worse than 50 metres; idle providers may always go Offline. A real PostgreSQL/PostGIS endpoint test proves outside-boundary artisan Online refusal leaves both location and availability unchanged. Keep P1 open because the configured staging shape is only an approximate Ghana bounding rectangle, the exact production geometry and border policy are not approved/installed, and region resolution still ignores coordinates and selects the first active region. Approve authoritative geometry and multi-region ownership, then prove Ghana/neighbor/border points on the exact pinned artifact.
- [ ] **P0 / CI control before production:** paid GitHub-hosted Actions are excluded by owner policy. Every current-tree local workflow equivalent now passes, including migration-backed E2E, but the combined changes remain uncommitted/unreviewed and lack durable exact-SHA evidence. Pin the reviewed SHA and repeat the approved local/self-hosted gate before `main` promotion or production.
- [x] **Local E2E baseline repaired:** on a disposable PostgreSQL/Redis environment, both lifecycle suites and all 50 tests now pass after correcting stale artisan-capacity, driver-vehicle, welfare-alert, share-expiry, payment-dispute, and forward-recovery fixtures/assertions. E2E typecheck passes, all 104 current migrations apply in order, seed completes, and teardown cleans the disposable services. This is source-level local evidence, not staging or exact-release-SHA proof.
- [ ] **P0 / deployment control:** `.github/workflows/deploy.yml` is incompatible with the no-paid-Actions policy: it uses `ubuntu-latest` and refuses deployment without a successful GitHub push CI record for the exact SHA. Local evidence cannot satisfy it unchanged. Adapt and review a dedicated self-hosted or audited local migration/deploy path without weakening the gate.
- [x] **P1 / real schema drift closed locally:** the Prisma model now matches all modelable objects from a fresh **112-migration** PostGIS replay, including foreign-key names/actions, native text/varchar types, database defaults, relations, and ordinary indexes. The only remaining diff is `idx_artisans_current_location_all`, an intentionally SQL-only GIST index over Prisma's unsupported PostGIS geometry field. `db:verify-schema-drift` accepts only that exact two-line diff (or zero drift) and fails every additional/changed operation; its guard passes **6/6**, and the actual verifier now passes inside the isolated migration-backed E2E gate before seed/runtime tests. The pinned Prisma 7.6 runtime also proved failed migrations roll back as one transaction, while an isolated probe proves one-statement concurrent-index migrations apply successfully. Financial migrations contain no inner `BEGIN/COMMIT` that could split schema changes from migration-history recording. Retain this verifier in the audited local/self-hosted release path; the deferred paid-GitHub-Actions work is tracked separately.
- [ ] **P1 / container runtime containment closed locally; reviewed supply-chain attestation pending:** the root `.dockerignore` excludes Git/editor metadata, every `.env` form, common credential patterns, dependency/build caches, tests, docs, logs, and reports. The Dockerfile now pins Node **22.23.0** to the exact multi-platform digest, installs production dependencies only, rebuilds the required native/Prisma runtime packages, copies an explicit runtime allowlist instead of the builder tree, and runs root-owned files as exact UID/GID 1001. The final local image is **181,187,690 bytes** at `sha256:8fb88d38e8f65778e309ddaf5689c021f6abc3b8dd08ab70293381cde719f64e`; inspection proves API source, tests, docs, Git/env files, Jest, ESLint, TypeScript ESLint, and Nest CLI are absent. A local SPDX 2.2 SBOM contains 828 packages and has SHA-256 `9926a643c132940ac8c7c952ab36bc2008dc0a9239da1a6bc650ab4af5032790`. Keep P1 open until the same image is reproducibly built from one reviewed SHA, its SBOM/license exceptions are formally reviewed, and an approved vulnerability scan is retained. Docker Scout was not run because it can upload private image/package metadata; that external disclosure requires explicit owner approval or an approved local-only scanner.
- [ ] **P0 / Ghana Card encryption key:** the API intends to fail production startup without a 32-byte AES key, but the audited configuration accepted any string of at least 64 characters and the service checked length without proving hexadecimal bytes. A second deployment hazard is now verified: `render.yaml` used `generateValue: true`, which Render documents as a base64-encoded 256-bit value, while this API's encryption contract requires exactly 64 hexadecimal characters. The local patch enforces exact hex in config/runtime, changes the Blueprint to retained manual `sync: false`, removes the unused/misleading CBC setting, and documents the actual AES-256-GCM runtime; the current complete evidence is 107 suites/2431 API tests plus config build, API typecheck/build, relevant-file formatting, lint with 0 errors/48 existing warnings, and diff checks. **Do not deploy this patch or replace either existing key yet:** independently verify only presence/length/hex validity and a non-secret fingerprint, escrow the current key, prove an existing-ciphertext decrypt plus round-trip canary, and design a versioned keyring/rotation path so a valid-but-wrong key cannot boot and silently strand old Ghana Card or ActivityKit-token ciphertext.
- [ ] **P0 / reviewer bypass candidate contained but BR-34 not complete:** local config requires an explicit enable flag, exact identity/OTP, and future expiry; it rechecks expiry at runtime, omits credentials from the Render Blueprint, and both configuration and startup refuse activation in production. Fixed OTP and dual-role selection state are bound to the exact review expiry and rechecked immediately before session mint. Reviewer+auth tests and the current full 161-suite/3,215-test API gate pass. Independent review still found that previously minted ordinary sessions survive flag disable/expiry for up to 365 days, there is no approved maximum duration, distributed reviewer rate limit, immutable reviewer marker, endpoint deny matrix, durable audit/alert, or live session-revocation kill switch, and the seeded documents violate the approved artisan XOR/driver-insurance set. This remains P0 until production history is proved or all reviewer state is revoked/rotated, the synthetic account and seed are repaired, and the full restricted-environment controls are demonstrated.
- [x] **P1 / runtime parity closed locally:** `.nvmrc`, package engines, local workflow definitions, deploy workflow, builder, and runner now align on Node **22.23.0**; the base digest is pinned, runtime files stay root-owned, and the unprivileged process uses exact UID/GID 1001. The entrypoint invokes the bundled Prisma/`tsx` executables and never fetches a runtime CLI. The repeatable runtime verifier applies all **188 migrations** and seed from the exact image, proves Render boot-migration and unspecified-geofence fail-closed behavior, connects to disposable PostGIS plus TLS `rediss://` Redis with no eviction, verifies Ghana fingerprint and isolated Redis/Socket.IO prefixes, receives HTTP 200 with DB/Redis `ok` from `/v1/health/ready`, and observes PostgreSQL/Redis disconnect hooks after SIGTERM. The full API gate then passes **184/184 suites and 3,787/3,787 tests** in the pinned Node 22 Linux builder. Deployment of the reviewed image and its exact version/commit marker remains a separate staging/provenance gate.
- [ ] **P1 / merge control:** private-repo branch protection is unavailable on the current GitHub plan; establish an enforceable alternative or upgrade.
- [ ] **P0 / scale evidence; local harness smoke closed, capacity proof absent:** the old k6 suite could falsely pass obsolete contracts, self-signed legacy JWTs, direct database fixtures, 401/403 responses, a stale Flutterwave webhook, and the static USSD menu. The current backend tree forbids known production hosts and live Paystack secrets; binds the canonical target URL to the exact healthy deployed commit with a deterministic SHA-256 fingerprint; requires unique exact-role sessions, selected approved vehicles, current Online epochs/sequences, and receipt-capable device rows; authenticates every fixture against live target role/account, session, vehicle, category, and current-work authority before load; uses current driver/artisan GPS, ride idempotency/recovery, real Socket.IO receipt/decline, artisan bid, and Paystack inbox contracts; combines each driver's GPS stream and socket under one exact session; keeps each artisan location session fresh throughout job/bid load; and permits full/stress/webhook load only on a disposable clone. The guarded companion provisioner can mint synthetic tokens and insert authority only after exact commit/target binding, a non-production Ghana-wide geofence, clean database, dedicated Redis prefix and matching database/Redis/operator nonce all pass; it refuses shared staging, production, the Ashanti production fingerprint, real-user data, repository output and existing output files. Load contracts pass **16/16** locally and under pinned Node 22. The first k6 1.3.0 smoke exposed and failed on an unisolated routing dependency; after explicit 18-route synthetic-cache isolation, a fresh clone passed exact-role preflight and smoke at 9/9 checks, 0/7 HTTP failures and p99 133.23 ms, with one terminal ride and zero active work after reconciliation. The rewritten report and runbook require target/resource identity, approved traffic model, metrics, abort owners, multi-replica faults, reconciliation, rollback, and 2x headroom. Keep P0 open: the harness has not run on a reviewed production-shaped same-region multi-replica clone; the approved 100k-DAU concurrency/RPS model, retained normal/2x/soak/fault result, provider metrics, alert evidence and capacity sign-off are still absent.
- [ ] **P1 / money:** BR-49 approves a booking-time dynamic-rate snapshot applied to final pre-promotion fare, with tips excluded. The app already renders the backend settlement rate; staging displaying 20% is expected while `commission_rate_percent=20`. A later audit found that the ride response quoted the booking rate but the ride row did not retain it, so completion re-read mutable config. The backend candidate now persists an explicit ride snapshot, uses it for both completion and payment initiation, and fails closed instead of inventing a legacy rate. It remains open until the new migration is rehearsed, a changed-rate-during-trip staging canary passes, and legal/versioned-consent plus Payment/earnings/refund/report reconciliation are complete.
- [x] **P0 / mutable ride pricing — local code and database contract closed; integrated cutover proof tracked separately:** every new ride freezes versioned category rates, surge, cancellation terms, waiting charge, quote-protection thresholds, telemetry trust floor, duration cap, and automatic fare cap before persistence. Completion, admin completion, cancellation, and multi-stop recalculation use that snapshot; missing/corrupt required policy fails before booking and no completion-time default is invented. The database booking trigger now rejects protocol-0, missing-idempotency, missing/invalid-snapshot, and invalid-surge inserts even after `rides_enabled` reopens. An eighth aggregate-only preflight blocks active legacy rides without valid booking-time policy. Evidence: focused **96/96**, full API **183/183 suites and 3,783/3,783 tests**, API typecheck, Prisma validation, and a fresh **188-migration** PostgreSQL/Redis gate with drift, seed, **8/8** preflights, reopened-writer refusal, zero-mutation cutover proof, online-index safety, and lifecycle E2E **68/68**. Keep production NO-GO until the exact reviewed artifact runs this preflight/rehearsal on a production-shaped clone and pinned staging; legacy terms must never be fabricated from current admin rates.
- [x] **DEFERRED / P0 source defect closed before promotion enablement:** BR-48
  now preserves distinct quoted, actual pre-promotion, discount and final-charge
  facts and settles commission/refund/earnings from immutable booking-time
  terms. Promotion redemption remains false in the production Blueprint, so no
  new promo value is consumed in this emergency release. Legacy-data preflight
  and staging money reconciliation remain required before a later enablement.
- [x] **DEFERRED / P0 source defect closed before promotion enablement:** BR-40
  uses a ride-bound atomic reservation with scope/global/per-client caps,
  idempotent booking identity, exact-once release before completion and
  exact-once consumption at completion. Collection retry retains the consumed
  reservation. `FF_PROMO_REDEMPTION=false` remains the release authority; a
  later enablement still requires production-shaped concurrency and accounting
  canaries.
- [ ] **P0 / account isolation — local sessions/exit/emergency/recovery ownership contained; shared rewards and staging proof remain:** client, driver, and artisan now have separate role-account IDs plus durable exact-role SID/generation fences; concurrent sibling sessions, normal logout, self-delete, admin delete, emergency force logout, and BR-63 OTP recovery act only on the selected role. Emergency contacts are role-owned and legacy ambiguous rows are quarantined. Deletion blocks active work and exact-role financial liabilities, including the immutable 24-hour dispute boundary anchored only to `ride.completedAt` or `artisanJob.clientConfirmedCompleteAt`; tests prove the private auth root and both siblings remain untouched. Keep P0 open because loyalty/referral ownership is still rooted in shared `User` while those features remain OFF, BR-64 automatic purge is intentionally unimplemented/disabled pending exact Legal periods, and production-shaped migration plus cross-role IDOR/delete/recovery/purge evidence is absent. Hold-release governance is approved but not yet implemented. Do not assign legacy shared rewards by assumption.
- [x] **P0 / admin sibling-account actions — local code closed; integrated proof tracked separately:** active admin mutations require explicit `client|driver|artisan` plus public role-account ID; role detail/actions show the exact target; suspend, reinstate, delete, force logout, and provider verification/vehicle workflows cannot fall through to the private auth root or the sibling provider role. Backend route/service regressions and **27/27** admin contracts cover the exact-role shapes and reject private root IDs. Keep production NO-GO until the pinned staging artifact passes browser IDOR/concurrency and sibling-session preservation journeys.
- [ ] **P1 / platform-wide Super Admin authorization sweep pending; verification slice closed locally:** the root `product_owner` plus legacy `super_admin`/`manage_admins` markers may now perform every ordered provider-verification stage across region/category scopes, while Admin, category Coordinator and RM restrictions remain unchanged for non-root accounts. Admin login now returns the named role and complete region/category profile required by the dashboard. Focused verification evidence passes **3 suites / 534 tests**; the full backend passes **202 suites / 4,047 tests**, typecheck and build; and the Admin passes TypeScript, **33/33** contracts and its 48-route production build. After staging deploy, re-login and prove Admin check → Coordinator validation → RM finalization from the Super Admin account. Keep this item open for the explicitly deferred route-by-route platform permission/scope sweep; do not infer that unaudited modules already implement root override consistently.
- [x] **P0 / shared identity fallback leakage — local code closed; integrated staging proof still required elsewhere:** payment/Paystack, ride offer/snapshot, tips, marketplace bids/jobs, refund destination, dispute/admin finance/activity/map/reporting, clawback, and email paths now resolve only the exact client/driver/artisan legal/display/business name and exact-role email, falling back only to `Client`/`Driver`/`Artisan`. Active admin responses no longer nest the private auth entity or expose its ID; clawbacks return the operational phone directly instead of a private `userId`. Adversarial regressions inject `SHARED AUTH NAME` and prove it cannot surface. A permanent source contract rejects shared name/email joins in these active paths. Evidence: focused backend **9/9 suites and 586/586 tests**, full backend **183/183 suites and 3,783/3,783 tests**, API typecheck, admin TypeScript, **27/27** admin contracts, and a successful production admin build. The remaining account-isolation, installed-artifact, and deployed staging gates are tracked separately and remain open.
- [ ] **P0 / dispatch candidate unlanded:** the local candidate now includes the durable receipt protocol, exact sequential rings, PostgreSQL offer/ring authority, restart recovery, old-build transport isolation, race guards, and the approved exhausted-search notice. It is still not deployed and cannot close BUG-01 until reviewed migrations, multi-replica/Redis-loss/device/staging/load evidence, observability, rollout, and rollback gates pass.
- [ ] **P1 / topology:** staging API/Redis are Oregon while staging Neon is Frankfurt; this cannot provide representative latency evidence for Frankfurt production.
- [ ] **P0 / production durability fails closed locally; infrastructure replacement pending:** production Redis is Cape Town with eviction enabled while the API is Frankfurt. The exact-current API now checks Redis `INFO memory` during production bootstrap and on readiness once its 60-second result cache expires, accepts only `maxmemory-policy=noeviction`, collapses concurrent probes, bounds the provider call, and reports no Redis provider detail on failure. The runtime verifier proves the production container exits against `allkeys-lru` and becomes ready against TLS `noeviction`. This prevents an unsafe deploy but does not fix the infrastructure: provision same-region Frankfurt Redis with no eviction, HA/capacity/backup/alerts and prove failover before production.
- [ ] **P1 / documentation drift:** historical security, DPA, architecture, project-status, EDD, and API-contract documents conflict with the current Render/Neon/Upstash/Paystack/Arkesel system. Re-audit current code and designate canonical owners.
- [ ] **P0 / recovery support code contained locally; operational reachability
  proof remains:** client and provider use one app-owned support-email constant
  consistently across retained-role recovery, support screens and client PDF
  receipts. Placeholder/malformed phone and WhatsApp destinations are removed;
  the shared sheet renders WhatsApp only for exact E.164 digits, and both help-
  article “Open a ticket” actions now navigate to the ticket form instead of
  popping the page. Direct and full mobile regressions pass. Before shipping,
  confirm `support@gilmoretechnologiesgh.com` and `support@myshop.com.gh` are
  live and monitored by named owners, then prove unauthenticated recovery email
  and authenticated ticket taps reach an attended queue on physical devices.
- [ ] **DECISION REQUIRED / P0 USSD release scope; runtime contained locally:** the retained `/v1/ussd/callback` remains a static TODO stub, but the release candidate now defaults `FF_USSD_CHANNEL=false`, explicitly disables it in the Blueprint, refuses `true` at production startup, accepts only the exact boolean/string value `true` at the request boundary, returns stable 404 otherwise, remains subject to normal throttling, omits the controller/tag from Swagger, and logs no telco session or phone fragment. A real E2E caught and closed the raw string `"false"` truthiness bypass. The owner must still confirm whether USSD is deferred or belongs to a later release. Existing project-status/changelog and seeded client Terms/Acceptable Use claims remain contradictory and require legally reviewed correction before consent. If USSD is later included, implement authenticated telco ingress, the complete server-owned state machine, exact role/account/payment authority, privacy/retention, E2E, load, monitoring, and support evidence. Do not count the contained placeholder as a passing feature.
- [ ] **P1 / recovery implemented locally; retention/legal alignment and staging
  proof remain:** legacy `/auth/recover` stays 404. The feature-gated BR-63
  routes now OTP-bind the exact deleted role, device, immutable deletion facts,
  region/category and stable request key. Client approval is global
  Operations/Super Admin; provider recovery is regional named Admin intake then
  category Coordinator and RM final approval. No recovery rejection/dismiss
  action exists. Soft delete reports `recoveryEnabled` from the same exact
  `FF_ROLE_ACCOUNT_RECOVERY=true` gate, preserves siblings/private auth, and
  retains the role for exactly 2,160 hours. Timely provider Admin intake moves
  the role into ordinary pending verification that may finish after the
  deadline while remaining Offline. Suspension cannot revive it. The purge
  remains a deliberate no-op. Local backend, database/E2E, mobile and admin
  evidence is recorded in checkpoint 1062. Keep P1 open for controlled staging
  proof, corrected/versioned legal texts, exact Legal retention periods for
  non-tax records, purge and
  hold implementation, object/notification deletion, and backup proof.
- [ ] **P1 / admin QA:** the current 48-route admin candidate has **32/32**
  automated role-account, vehicle and API-error contracts; TypeScript, lint
  with zero errors, the production build and diff check pass. Keep P1 open for
  browser/E2E, concurrency/IDOR, timezone-boundary, exact reviewer-chain,
  legacy remediation and pinned staging-artifact proof.
- [ ] **P1 / mobile E2E:** no Flutter `integration_test/` suite is present; critical journeys need automated contract/integration tests plus physical-device evidence.
- [ ] **P0 / destructive provider requeue:** migration `20260703010000_requeue_pending_provider_documents` resets any pending/approved/rejected provider with any current `pending_review` document to pending/pending-documents/offline, including optional-document and soft-deleted cases. It records no audit event or before-image. It is already on staging and must be marked applied without DML on production after snapshot/preflight/review.
- [x] **P0 source defect closed locally / replacement-document outage:** upload
  preparation now creates a non-current version and leaves existing authority
  untouched; confirmation serializes and atomically swaps only an allowed
  rejected/expired candidate. BR-66 closes provider replacement while approved
  evidence remains valid, abandoned uploads cannot demote it, vehicle-scoped
  current constraints are present, and lifecycle/grace events are durable.
  Exact staging storage/race proof remains under the common media and provider
  acceptance gates.
- [x] **P0 source defect closed locally / expiry lifecycle:** the inclusive GMT
  boundary is enforced by eligibility/dispatch and durable lifecycle events.
  Notices are staged at upload confirmation, seven days, 72 hours, 24 hours,
  two hours and invalidity; active work may finish, invalid evidence prevents a
  new Online session, and the provider UI consumes server expiry/replacement
  authority. Deployed scheduler/channel proof remains open.
- [x] **P0 source defect closed locally / admin mandatory-document mismatch:**
  driver licence, vehicle-bound roadworthiness and vehicle-bound insurance use
  required expiry; artisan Ghana Card plus the strict credential XOR and
  provider/vehicle ownership are enforced. The 48-route Admin build and 32/32
  contracts pass. Legacy-data remediation and staging reviewer journeys remain
  open.
- [x] **P0 source defect closed locally / eligibility split-brain:** every
  provider Online and dispatch boundary now shares server-owned provider,
  independent-document, RM, category, selected-vehicle, location, liveness and
  notification-reachability authority with stable refusal codes. The exact
  current local gate passes; staging/device proof and legacy remediation remain
  open.
- [x] **P0 source defect closed locally / independent review:** final provider
  approval no longer bulk-approves evidence. Each required current document is
  independently approved; artisans require Ghana Card plus exactly one trade
  credential, and drivers require the confirmed provider/vehicle set before RM
  completion. Deployed reviewer-chain proof remains open.
- [x] **P0 source defect closed locally / exact-role isolation:** `/users/me`,
  mutations, logout/delete/recovery, sessions, devices, support tickets,
  emergency contacts, notifications and active-work fences use the exact role
  account while the private phone-auth root stays non-public. Sibling roles are
  preserved. Rewards remain disabled rather than assigning ambiguous legacy
  ownership; cross-role staging IDOR evidence remains open.
- [x] **P0 source defect closed locally / multiple vehicles:** drivers may own
  multiple vehicles, must choose exactly one eligible vehicle for each Online
  session, and cannot edit or remove an approved vehicle. Evidence is separately
  vehicle-bound, approval is Coordinator then RM, acceptance revalidates the
  selected vehicle, and rides preserve an immutable vehicle snapshot. Legacy
  driver inventory/remediation and installed-device proof remain open.
- [ ] **P0 / false active-ride disconnection locally fenced; device/fault proof pending:** the provider app intentionally closes Socket.IO whenever it becomes paused/inactive/hidden, including when the phone is merely locked during an active ride. The audited backend baseline treated that close as connectivity loss and, after about 120 seconds, could alert despite healthy background REST GPS. The local API now ignores sibling-role closes and remaining driver sockets, records server disconnect time, renews grace only from a newer server-timestamped heartbeat, and clears late reconnects. The escalation cron takes an atomic per-ride Redis claim, rechecks reconnect and newer background heartbeats after winning, releases only its own claim and preserves the pending sentinel after a crash for bounded retry; Redis authority failure is fail-closed. After the approved 120 seconds it joins the durable BR-33 episode, creates permission-scoped admin and exact-role provider/client inbox evidence, retries zero-recipient/failure cases, deduplicates the independent stale-GPS worker and excludes unactivated queued rides. Focused disconnection evidence passes **26/26**, the broader location set passes **114/114**, and the exact backend gate passes **4,002/4,002**. Physical locked/background-device, Redis/network/process and production-shaped two-replica proof is still absent, so the operational P0 remains open.
- [ ] **P0 / availability split-brain materially contained locally; staging/device proof pending:** the deployed staging baseline remains stale, but the local mobile patch now fetches authoritative availability on authentication, foreground resume, socket reconnect, and `availability:forced_offline`; backend Offline demotes idle local Online while Busy/active work is preserved. The backend now force-offlines the exact provider role when its final eligible FCM token is permanently invalid, without cancelling active work or touching sibling roles. Registration/invalidation fencing prevents a stale send result from killing a replacement token. Keep P0 open for provider-token age/rotation, full artisan receipt assurance, multi-replica/Redis faults, locked/background installed-device behavior, and pinned staging evidence.
- [ ] **P0 / stale and rewindable location authority materially contained locally; staging/device proof pending:** deployed code remains unsafe. The local mobile and backend share exact server-owned driver/artisan Online epochs and monotonic sample sequences across direct, socket, and background-batch ingestion. PostgreSQL applies the epoch, sequence, capture-time, location, and liveness transition atomically under a row lock; only an accepted write may refresh Redis GEO/heartbeat, degradation recovery, ride trail, eligibility, or broadcasts. Offline clears both roles' epoch authority, and a cutover migration refuses with `PROVIDER_LOCATION_SESSION_CUTOVER_BLOCKED` before DDL while any provider is Online. Ride and artisan lifecycle checkpoints now require an independent <=15-second/<=30-metre proof and cannot be skipped with one fix. Real PostgreSQL/PostGIS E2E proves old/duplicate locations have zero secondary effects and active work can finish after final-token force-offline. Keep P0 open for lifecycle-geofence radii/override evidence, server-receipt/device-clock telemetry, multi-replica races, network/Redis failure, load, pinned staging, and installed-device proof.
- [x] **P0 source defect closed locally / active-work location degradation:**
  current work is preserved while new offers are suppressed, degradation and
  recovery are server-owned for drivers and artisans, stale/rewound samples
  cannot refresh authority, durable notices are emitted, and completion leaves
  the provider Offline until BR-30 passes. Multi-replica/Redis/device/fault
  behavior remains a staging and capacity gate.
- [ ] **P0 / provider liveness implementation fixed locally; full capacity/failure proof pending:** the former every-replica O(all-online) N+1 cron is replaced by one deployment-wide Redis lease, partial stale-online database indexes, bounded cursor pages, batched heartbeat reads, per-provider expiry claims, and monotonic database liveness epochs. A returning heartbeat invalidates an in-flight stale claim; an already-expired database session fails with stable `PROVIDER_SESSION_EXPIRED` instead of silently resurrecting Online state. A later 100,000-provider disposable PostgreSQL benchmark found that the original ID-first cursor touched roughly 18,000–22,000 shared buffers and took 5–15 ms to return 2,000 stale rows. The candidate now keysets by stale time plus UUID over a matching functional partial index, reducing that same scan to about 2,000 buffers and under 1 ms. The deployment lease is owner-renewed before scans, each processing chunk, and cursor commits; loss or Redis renewal failure stops the old worker before further mutation. The query and lease renewal are exercised through the real worker in the fresh 109-migration E2E gate; the full API passes 171 suites/3,507 tests, E2E passes 51/51 and five repeated gates pass 255/255. Keep P0 open until a production-shaped clone confirms the plan and batch/pool budgets and real multi-replica Redis loss, long in-flight chunk expiry, worker crash/restart, delayed-heartbeat, active-work races, and full application load/soak pass on one pinned staging artifact.
- [ ] **P1 / background permission and continuity gap:** Android/iOS permissions and background modes exist, and the local provider tree adds a native iOS While In Use -> Always bridge invoked only after explicit Go Online plus disclosure. Current-source archives compile, but distributable IPAs are blocked by the missing Apple Distribution identity and physical-device permission behavior remains unproven. Reduced/approximate accuracy, notification/token reachability, OS process death/force-stop, reboot recovery, and battery-safe idle versus active sampling are not handled or proven. Store/disclosure wording must match the actual supported states.
- [ ] **P1 / location privacy and credential hygiene:** exact coordinates and selected high-risk provider/error details were present in production-capable logs. Local containment drops HTTP query/body values, fail-closed masks Ghana phone formats, sanitizes audited Paystack/Smile/media/admin/refund paths, and retains bounded status/error-class metadata; the current full 161-suite/3,215-test API gate and source checks pass. The repo-wide log/telemetry and HTTP error-serialization audit remains open across notification, payout-method OTP, call, push, places/routes, pending-transfer, welfare, dispute/reconciliation, tip, VoIP, and WhatsApp paths. The provider Android Maps key was historically tracked; restrict/rotate it and keep the value out of this tracker.
- [x] **P0 source defect closed locally / media upload authority:** durable
  upload intents and confirmations bind exact user, role, purpose, staging/final
  keys, MIME, bytes, expiry and fingerprint. Confirmation ignores client URL
  authority, verifies stored bytes, preserves role in `ConfirmedUpload`, and
  same-owner/role response-loss replay returns the original server result.
  Every active consumer is role/purpose-bound; unaudited purposes and deferred
  support/dispute attachments fail closed. Mandatory minimum versions provide
  the approved no-legacy-fallback cutover. Real private-store, malware, quota,
  cleanup, retention, installed-build and migration proof remains open in the
  following BR-61 gate.
- [ ] **P0 / BR-61 media-consumer follow-up:** the later combined local candidate now requires exact user + JWT role + purpose + non-null durable intent + verified MIME/size before persisting client profile photos, client Ghana Card images, artisan-job photos, or payment-dispute evidence; support attachments already apply the same owner/role/purpose proof. Client Ghana Card now has a dedicated purpose instead of reusing profile-photo authority. General media upload purposes with no audited writer (`chat_attachment`, `portfolio_photo`) are closed; provider portfolio evidence continues only through the separate reviewed `/verification/*` contract. Focused evidence passes 5 backend suites/134 tests and 2 API-client suites/12 tests; the provider no-GPS Offline/vehicle follow-up analyzes cleanly. Keep P0 open until the full shared tree is frozen and regated, old/new installed builds pass the mandatory cutover, the migration is rehearsed, and real private S3/Cloudinary access, replay, cleanup, quota, retention, malware, and cross-role behavior are proven.
- [ ] **P0 / exact-role admin media containment implemented locally; real-store proof pending:** both privileged admin writers are default-off and take exact `role + roleAccountId`; neither resolves a shared phone-auth user or sibling provider. Profile photos verify actual JPEG/PNG/WebP bytes and size, store under the exact role-account path, and attach the server-owned result in the same audited database finalisation; ordinary profile PATCH DTOs reject `profilePhotoUrl`. Provider documents additionally verify private stored metadata and downloaded bytes, hash the stored content, re-lock exact provider/vehicle ownership, bind vehicle evidence, preserve replacement-grace/active-trip behavior, and delete uncommitted objects after verification or database failure. Fresh focused evidence passes **3 backend suites / 370 tests**, admin exact-role contracts **12/12**, backend/admin TypeScript, and diff checks. Keep P0 open until pinned staging proves private object access, exact-role IDOR denial, upload/finalisation races, orphan deletion, malware/quota/retention policy, and real S3/Cloudinary failure behavior.
- [ ] **P0 / redispatched offer deadline:** after A's 45 seconds, the socket payload for B used the ride's original creation time and no canonical offer deadline, so B could auto-expire immediately while the foreground FCM was suppressed. The pending ride branch sends a fresh absolute deadline and 45-second window for each offer; physical-device and multi-replica evidence is pending.
- [ ] **P0 / candidate starvation:** matching applied `LIMIT 10` before excluding busy/already-notified drivers, so an eligible driver #11 was invisible. The pending ride branch moves those exclusions into SQL before the limit and tests the behavior; query-plan/load evidence remains pending.
- [ ] **P0 / dispatch durability candidate pending proof:** the unlanded implementation stores immutable sequential offers, exact search rings, and client booking identity in PostgreSQL; uses partial unique offer and booking-key invariants, database-clock receipt/decision deadlines, bounded `SKIP LOCKED` claims, leased restart recovery, idempotent decisions, atomic dispatch readiness, and an indexed prior-driver anti-join. Redis eviction/failure no longer removes offer, booking, progress, or committed-transition authority. The slow-POST, lost-response retry, all-replica recovery herd, canceled-card replay, late-expiry rollback, phantom count, cross-role action, and oversized push defects have local fixes and tests. Remaining: isolated apply/revert; real PostgreSQL booking/offer/worker races and crash reclaim; response-loss fault injection; delivery outcomes/latency; query plans; pool/cron budgets; exact-SHA review; and load/soak/canary/rollback evidence.
- [ ] **P0 / offer delivery and driver reservation candidate pending device proof:** socket and FCM carry the same v2 identity; only capability-advertising sockets/tokens receive it; a minimal non-PII local handoff precedes authenticated receipt; the backend then returns its exact deadline plus DB clock, and mobile projects remaining time onto its own clock while the server remains decision authority. No receipt within ten seconds advances without driver penalty; delayed/duplicate receipts, explicit socket-ack/REST acceptance fallback, and process handoff have focused tests. Remaining: real Android/iOS foreground/background/terminated/locked/Doze/Low Power/poor-network tests; iOS silent-wake/force-quit behavior; notification actions and process-kill recovery; latency telemetry; and the approved rollout policy below.
- [ ] **P0 / BR-38 dispatch rollout approved and database-fenced, but not production-shaped or staging rehearsed:** both client and provider updates are mandatory, and activation must follow the [controlled V2 cutover](ride-v2-controlled-cutover.md): block old mobile versions, pause new bookings without blocking active lifecycle APIs, drain/resolve all protocol-0 work, prove zero twice, stop every old replica/worker, migrate, start only V2 closed, canary, then reopen. The migration forces `rides_enabled=false`, and a database `BEFORE INSERT` trigger is now the final authority even for stale/legacy writers. Local PostgreSQL proves stable closed-gate refusal and zero active-trip mutation when both destructive cutovers refuse. Stale Redis config must still be fenced, and no mixed-version backend or mobile rollout is allowed.
- [ ] **P0 / mandatory-update source integrated and exact store authority pinned; rollout proof absent:** the combined backend has dormant-by-default per-app/per-platform minimum builds, stable `APP_UPDATE_REQUIRED` 426 responses, and guards for ride creation, `/media/*`, provider availability, receipt-capability registration, and every foreground/background driver/artisan GPS Go Online path. Both apps send app/platform/build metadata and latch a non-dismissible update state. Configuration now accepts only the four exact reviewed MyShop listing URLs and refuses activating only Android or only iOS for one app, because legacy builds without platform metadata would otherwise receive a blocking 426 without the other platform's store destination. Official public checks returned all four exact package/bundle identities; public versions are Android client/provider `1.3.11`, iOS client `1.3.9`, and iOS provider `1.4.0`, below the approved `1.4.1`. Exact-current evidence passes focused guard **17/17**, config/API typechecks, pinned Node 22.23.0 backend **190/190 suites and 3,863/3,863 tests**, the five-package production build, mobile **413/413**, all analyzers, and diff checks. This remains P0 because private build numbers, signed artifacts, stopped-worker receipt reset/current-session re-registration, exact commits, installed old/new-build proof, and the BR-38 staging cutover rehearsal are still absent.
- [ ] **P0 / iOS receipt evidence:** v2 APNs delivery is a short-lived silent/background push so an alert is never shown before authenticated receipt. If iOS does not wake the app, the correct server behavior is to advance after ten seconds, but this can reduce offers seen by an online iOS provider. Prove on physical devices and decide whether that operational hit is acceptable; do not infer reliability from Firebase acceptance.
- [x] **DEFERRED / P0 before Smile enablement:** BR-35 is contained in the exact
  local candidate. No automated initiation or public Smile callback route is
  registered, the production Blueprint pins `SMILE_IDENTITY_ENABLED=false`,
  and source/mobile release claims identify manual review. Reviewed-commit and
  deployed-configuration proof are covered by the common packaging/staging
  gates. Corrected published legal text remains a non-deferable release blocker;
  Smile credentials and automated claims must stay absent until a separately
  reviewed future release.
- [ ] **P1 / ride-query scale:** the local migration adds partial functional GiST geography indexes matching the exact driver and artisan `ST_DWithin(...::geography)` predicates plus a partial requested-ride recovery index. Still prove `EXPLAIN (ANALYZE, BUFFERS)` on representative volume, add the business-approved `location_updated_at` freshness rule/index, deterministic distance/driver tie ordering, a partial active-ride driver index, bounded worker/connection-pool budgets, and 100k-DAU load evidence.
- [x] **P1 / session and push race closed locally; staging/device proof pending:** mobile awaits exact-role authentication persistence before publishing authenticated state and FCM registration. Backend FCM, VoIP, ActivityKit push-to-start, and ActivityKit update-token registration now lock and revalidate the exact durable role SID at their final write boundary against the same user+role rows locked by logout. Registration-first is deactivated by logout; logout-first rejects the delayed writer with stable `ROLE_SESSION_CHANGED`; sibling roles remain untouched. Focused notification tests pass **51/51**, the current full backend passes **187/187 suites and 3,821/3,821 tests**, and real PostgreSQL race scenarios pass inside the exact-current **72/72** E2E gate. Keep operational proof open for installed locked/background devices, delayed registration, multi-replica behavior, and the pinned staging artifact.
- [x] **P0 / active-work logout bypass closed locally:** BR-13/BR-57 now route normal logout, role deletion, admin deletion, and emergency force logout through the exact-role database exit guard. Active interactive work/payment blockers are enforced; unaccepted offers/bids/quotes are atomically withdrawn; client booking/job creation is serialized against exit with SID/generation fencing; sibling roles are untouched; and the two PostgreSQL race orders pass. Provider legacy refresh-session residual and deployed/device proof remain tracked separately rather than reopening this source defect.
- [ ] **P1 / S3 verification confirmation:** a presigned S3 PUT returns no public URL, so mobile skipped `/verification/confirm` and left documents in `uploaded`. The working tree now confirms with the backend storage key and has S3 plus Cloudinary regression tests; staging storage-path proof is pending.
- [ ] **P1 / admin workflow and safety:** one audited admin implementation lacks complete automated tests and has unsafe suspension/reinstatement/scoping paths; another admin tree appears queue/read-only. Pin the actual production admin artifact, repair broken actions/pagination/RBAC, and test the complete independent-review workflow before release.

## 6. Reported live-bug closure matrix

Each bug needs a production symptom, deterministic reproduction, root cause,
fix, regression test, staging proof, telemetry/alert, rollback, and support workaround.

- [ ] **BUG-01 / P0** Ride/request losses: the unlanded candidate now addresses the original stale redispatch deadline, candidate starvation, unsafe fan-out/config override, missing exact rings, Redis-authoritative offers/ring progress, duplicate holders, restart recovery, authenticated delivery receipt, late/duplicate decisions, old-build delivery, and cancel/accept overwrite race. Unit/full-suite evidence is recorded above, but closure still requires exact migration/query-plan review, multi-replica and Redis-loss integration tests, physical Android/iOS proof, rollout approval, staging telemetry/canaries, 100k-DAU load/soak, rollback, and support evidence.
- [ ] **BUG-02 / P0 local legacy-vehicle cutover repaired; staging proof pending.** Existing car data was retained in the legacy driver columns, but the explicit-vehicle cutover deliberately refused to infer ownership and the shipped admin queue could review only vehicles that already existed. That left verified legacy drivers Offline with no supported conversion path. The backend now exposes a region-scoped, bounded migration queue and one-driver serializable write: an authorized admin must compare the old vehicle data and explicitly confirm every current unbound roadworthiness/insurance document. When no explicit vehicle exists, the operator chooses vehicle-scoped ride categories and creates only `pending_coordinator` authority; when the driver already submitted an explicit vehicle, the operator must select that exact target and only bind the retained documents, without changing vehicle/category authority. Existing independent document status/expiry is preserved without rewriting it; the binding, actor, vehicle snapshot and selected evidence are audited. Plate conflicts, duplicate creation, changed evidence, missing targets and ambiguous duplicate current documents fail closed. Coordinator then RM approval and separate category approval remain mandatory. The app no longer mislabels a real pending vehicle as an unfinished migration, and a genuinely new driver with no legacy record receives the normal vehicle-selection state instead of an admin-migration notice. Focused backend evidence passes 4 suites/50 tests; the full API passes 202/202 suites and 4,018/4,018 tests; API typecheck/build and changed-file lint pass. Admin TypeScript, changed-file lint and production build pass. The pinned Node 22 builder applied all 196 migrations to fresh disposable PostgreSQL; rollback-only view probes returned `backfill_required=false` for a new driver, `true` for genuine legacy fields, `false` after an explicit vehicle exists, and `true` again for simulated retained unbound evidence. Keep P0 open until migration `20260720000000_correct_vehicle_backfill_state` is applied on staging, every authorized queue row is remediated, aggregate `backfill_required=0`, wrong-scope/permission/race cases are proven, and a migrated installed driver refreshes, selects the approved vehicle and goes Online.
- [ ] **BUG-03 / P0** Verified providers asked for already-approved documents: mobile required both mutually exclusive artisan credentials, backend masked independent approvals behind aggregate status, and final review bulk-approved rows. Mobile XOR/status fixes and a backend independent-review patch have focused tests; replacement/grace semantics, data reconciliation, admin UI, and staging proof remain open.
- [x] **BUG-04 / P1 local cross-system containment closed; operational proof open.** Mobile maps known failures to app-owned actionable copy and suppresses unknown API prose across the audited ride, job, bid, payment, payout, refund, dispute, verification/upload, rating, call, emergency, and account paths; audited FCM/socket/payment logs no longer print payload values. Backend HTTP errors now carry a server-generated opaque UUID in both `x-support-reference` and the canonical error envelope, logs reuse the same reference and framework route template, and caller-supplied request IDs are never trusted or echoed. Unexpected exceptions remain generic; request query/body values, external provider bodies, runtime messages/stacks, token prefixes, and raw persisted gateway failure text are contained by a permanent log-privacy contract. Admin ignores backend prose, maps stable code/status copy, preserves validated references through its proxy, branches on codes instead of messages, and contains its SMS/PDF routes. Evidence: mobile focused **24/24** and exact **394/394**; backend focused correlation/log proof **43/43** within exact **180/180 suites and 3,767/3,767 tests**; admin error contracts **8/8** within **26/26**, TypeScript/build/lint/diff green. Keep release blocked until the exact pinned staging artifact proves reference continuity and safe copy in browser/apps, installed fault journeys pass, external Render/browser/device log access-retention-redaction is verified, and support staff can search a reference without exposing private account identifiers.
- [ ] **BUG-05 / P1** Unregistered number redirect: root cause confirmed—the sign-up form visually prefilled the E.164 phone but never initialized its internal `PhoneNumber`, leaving Create Account disabled. The working tree reconstructs and validates the redirected phone with longest-prefix country-code matching and enables Create Account; the redirected-signup plus client OTP recovery subset passes 8/8 with fatal analyzer checks clean. Closure still needs Android/iOS cold/warm/deep-link staging proof and telemetry.
- [ ] **BUG-06 / P0** OTP delay/false “sent”: two-developer real-carrier testing is approved but the deployed staging build cannot enforce the two-number scope. The local API now defaults paid sends off, adds an exact E.164 allowlist/live mode, atomic Redis delivery and issuance caps, bounded provider requests, strict Arkesel acceptance validation, a deployment-wide circuit with fenced half-open probes and out-of-order outcome handling, per-attempt callback capabilities, atomic code/attempt/consume state, and coverage for payout-method OTP rather than a raw-carrier bypass. The auth plus payout subset passes 14 suites/248 tests. Client/provider auth screens preserve explicitly active delayed codes, and provider payout OTP now parses canonical nested errors instead of reporting API rejections as connection loss; focused mobile gates pass. This is not closure: rerun the full integrated API after media settles; execute every Lua contract on real isolated Redis; privately configure/deploy the two numbers, caps, kill switch, and HTTPS callback; prove Ghana-carrier accepted/delivered/verified latency plus cost alarms; resolve provider-login's enumeration-safe no-code recovery; and repair or disable the legacy notification callback.
- [x] **BUG-07 / P0 local implementation closed; installed proof open:** client ride and service-job cancellation now clear state or navigate home only after the cancellation response or an authoritative read-back proves `cancelled`. A timeout-after-commit is recovered; an active or unreadable row remains visible with stable copy. Driver cancellation applies the same read-back rule. Driver and artisan lifecycle controls no longer invent a next status from a malformed acknowledgement; they require the exact acknowledgement or a read-back at/after the requested checkpoint. Artisan cash completion likewise requires an authoritative completed job. Focused cancellation/lifecycle authority evidence passes **22/22** and the exact mobile gate passes **394/394** with all analyzers clean. Keep release blocked until installed Android/iOS tests inject timeout-after-commit, rejected cancellation, failed read-back, delayed socket, app background/lock, and duplicate taps against the pinned staging artifact.
- [ ] **BUG-08 / P0 local stationary/background-driver freshness gap fixed; installed proof pending.** Live Android reproduction proved location, notification, overlay and foreground-service permissions were allowed and running, FCM registered successfully, and battery saver was off. On background handoff the app intentionally disconnected Socket.IO for push fallback, but its REST heartbeat replayed the cached fix with its original capture timestamp; the backend correctly returned `GPS_FIX_STALE`, then `DRIVER_ONLINE_SESSION_REQUIRED`, making the driver unmatchable before FCM or the native overlay could receive an offer. Online streams still request zero-distance periodic samples, and background handoff plus each fifteen-second durable sync now actively obtains a genuine device fix when the cache is missing or older than ten seconds. Unusable fixes fail closed, capture timestamps remain device-authoritative and are never invented, and Socket.IO no longer re-emits the same or an older captured sample. Focused background/location proof passes **9/9**, the complete provider suite passes **166/166**, full provider analysis reports no issues, and diff check passes. Keep P0 open until the rebuilt installed driver remains stationary Online for at least five minutes, remains visible/bookable from a different-number client while foregrounded, backgrounded, screen-locked and reconnected, and then proves authenticated FCM receipt plus native overlay or notification within the approved ten-second delivery budget without stale GPS, false offline, sequence rewind, excess battery/thermal impact or request loss. The broader false active-ride disconnect, telemetry, iOS suspension and multi-replica evidence remain open.
- [x] **BUG-09 / P0 local payment/payout false-success containment closed; installed proof open.** The ride and service-job “I've completed the payment” escape hatches that fabricated settled receipts without backend confirmation are removed and replaced by authoritative status checks. Job payment completion requires an exact `completed` acknowledgement or authoritative job read-back; ride gateway `success` remains `awaitingSettlement` until the payment-status API proves a terminal success; invented transaction references are removed; cash cannot use the digital status action; a provider payout success screen requires a non-empty payout ID and `pending`, `processing`, or `paid` status. Malformed/blocked/unknown responses remain visible failures. Focused money-authority proof passes **4/4** within the new **24/24** slice. Keep release blocked until delayed webhook, lost socket, malformed 200, timeout-after-commit, cash, OTP, retry, duplicate tap, and payout-disabled journeys pass on installed Android/iOS builds against the pinned staging payment sandbox/live-safe canary.
- [ ] **BUG-10 / P0 local provider-map remount fixed; installed proof pending.** The provider router watched authentication, legal-consent and active-work state while constructing `GoRouter`; every profile refresh or state emission replaced the router, destroyed and recreated the native Google Map, and restarted the eight-second foreground location lookup. This produced the visible blinking and repeated `[LOC] getCurrentPosition failed: TimeoutException` entries even when a usable last-known fix existed. The router is now constructed once, redirect state is read on demand, and a lifecycle-owned refresh bridge listens conditionally to exact authenticated dependencies without remounting the route tree or calling legal endpoints while logged out. A source regression passes **2/2**, the complete provider suite passes **155/155**, full provider analysis reports no issues, and diff check passes. Keep P0 open until the exact installed Android build remains stable through profile refresh, consent refresh, socket/FCM updates, Online/Offline and active-work transitions, with one map controller and no repeated startup GPS calls; separately verify a genuine GPS timeout keeps the last-known map fix and presents a non-blocking recovery path.
- [ ] **BUG-11 / P0 Online startup Riverpod crash fixed locally; installed proof pending.** The location socket bridge synchronously reserved a shared location sequence while Riverpod was still constructing the bridge, triggering `Providers are not allowed to modify other providers during their initialization` on the first cached fix after Go Online and destabilizing the app/map frame. The kick-once emit is now deferred until initialization completes, all event callbacks read live container state, and disposed bridge instances cannot reserve sequences or update the location cache after reconnect/rebuild. The exact regression emits once with the next monotonic sequence; the complete provider suite passes **163/163**, full provider analysis reports no issues, and diff check passes. The Google Maps “Capabilities unavailable without a Map ID” and early Flogger-drop messages in the same device log are not this crash. Keep P0 open until the exact installed Android build repeatedly enters Online, reconnects, backgrounds/resumes and enters active work without the Riverpod assertion, sequence gaps/rewinds, visible map instability or lost location delivery.
- [ ] **BUG-12 / P0 mixed ride-category availability fixed locally; installed proof pending.** The backend correctly returns driver availability separately for each ride category, but the fare screen treated the first/cheapest category as a route-wide flag. An unavailable first tier therefore showed “Every nearby driver is offline or busy,” disabled confirmation, and hid an eligible driver assigned to another category. The client now blocks only when every category is unavailable, automatically selects the first available tier, and keeps confirmation disabled until that exact available category is selected. The category regression passes **2/2**, the complete client suite passes **80/80**, full client analysis reports no issues, and diff check passes. Keep P0 open until an installed client build proves an unavailable first tier plus an available later tier renders/selects/books only the eligible tier against the rebuilt provider app with a fresh server-authoritative location session.
- [ ] **BUG-13 / P0 platform-toggle split brain fixed locally; staging migration/retest pending.** The staging Ride Hailing toggle wrote JSON string `"1"`. `PlatformConfigService.getBoolean()` treated it as enabled, so fare calculation and matching found the eligible driver, but the database-final V2 booking trigger correctly accepted only canonical JSON boolean `true` and rejected the ride insert as `RIDE_BOOKING_DISABLED`, surfaced as HTTP 503. Admin now submits `true`/`false`; the API canonicalizes both modern and legacy `true/false/1/0` inputs before persistence and audit; migration `20260720161000_canonicalize_platform_config_booleans` converts every API-managed operational toggle without changing unrelated config. Focused backend proof passes **51/51**, backend and Admin type checks pass, changed Admin lint has zero errors, Prisma schema validation passes, and a rollback-only PostgreSQL migration smoke test converts `"1"`/`"0"`, preserves canonical values, and leaves an unrelated key unchanged. Keep P0 open until the migration and backend/Admin commits deploy to staging, the public config returns boolean `true` rather than string `"1"`, a backgrounded different-number driver receives and acknowledges the created ride, and disabling/re-enabling Ride Hailing proves both API and database gates close/open together with an audit record.
- [ ] **BUG-14 / P0 ride commission snapshot fixed locally; staging migration/retest pending.** The provider summary contains no hard-coded 20%; it displays `commissionRatePercent` from the backend payment snapshot, and staging is currently configured at 20%. The audit nevertheless found a BR-49 settlement defect: the booking response carried the selected admin rate, but `rides` did not persist it and completion/payment re-read mutable `platform_config`. Migration `20260720170000_snapshot_ride_commission_rate` adds the explicit booking snapshot, backfills only an exact durable V2 booking-response value, refuses an unreconciled active ride, constrains the value, and makes the database reject future V2 inserts without it. Ride completion and payment now use only that snapshot; a missing legacy snapshot fails closed for manual review. Focused booking/payment/migration proof passes **191/191**, database/API type checks and Prisma validation pass. Apply/rehearse the migration, then prove on staging: set 12.5%, book a new ride, change admin to another rate before completion, confirm that ride settles and displays 12.5%, and confirm only a subsequently booked ride uses the new rate.
- [ ] Create support playbooks for all open live bugs before any rollout.

## 7. Cross-system feature audit

### Authentication, onboarding, OTP, and sessions

- [ ] Audit phone normalization, enumeration resistance, OTP entropy/TTL/attempts/resend cooldown, rate limits, replay, race conditions, SIM-swap/account recovery, and device takeover.
- [ ] Verify client/driver/artisan registration, login, role selection, logout, deletion, recovery, and old-app compatibility against BR-03/BR-13.
- [ ] Benchmark Arkesel, mNotify, Hubtel, Africa's Talking, WhatsApp OTP, and voice fallback on MTN/Telecel/AT Ghana using p50/p95/p99 delivery and verified-delivery data; select providers from evidence, not marketing claims.
- [ ] **OTP fallback shortlist, not a winner:** current official documentation confirms Arkesel per-message IDs plus callback/report statuses, [mNotify per-message delivery reports](https://readthedocs.mnotify.com/), and [Hubtel SMS delivery/spend tracking](https://developers.hubtel.com/). These capabilities do not establish handset delivery speed. Obtain sandbox/live credentials and sender approval, then run the same six-digit transactional template from the same Frankfurt worker to the two authorized test numbers on each Ghana network; record API acceptance, provider delivered, handset receipt, verification, cost, timeout, and duplicate-code rate before choosing primary/fallback. No Hubtel/mNotify/Africa's Talking traffic or purchase is authorized yet.
- [ ] Verify idempotent OTP send, bounded timeout, circuit breaker, failover without sending multiple valid codes, carrier-aware routing, delivery receipts, quotas, sender IDs, and cost alarms.
- [ ] Confirm the UI never says “OTP sent” solely because an upstream HTTP request was accepted.
- [ ] Close BUG-05 and test every signup redirect/CTA/deep link.

### Account and data isolation

- [ ] Build a field-by-field isolation matrix for profile, documents, tokens, sessions, FCM/APNs, bookings, earnings, payouts, loyalty, referrals, ratings, notifications, deletion, and online state.
- [ ] Require authoritative account type and account/profile ID in API authorization and events; never resolve an authenticated mutation by phone alone.
- [ ] Add IDOR/cross-role tests proving one role cannot read or mutate sibling-role data.
- [ ] Verify admin/security visibility follows the final approved linkage rule.
- [ ] Verify deletion/recovery/suspension of one role follows the final decision without leaking to siblings.

### Verification, documents, and provider go-live

- [ ] Model independent document versions with type, owner/vehicle, current flag, status, reviewer, review time, rejection note, upload confirmation, and expiry.
- [ ] Never supersede a valid document merely because an upload URL was requested; switch only after confirmed upload according to the approved rule.
- [ ] Derive driver/artisan/vehicle eligibility server-side from approved required sets and expose stable blocking reason codes.
- [ ] Enforce Ghana Card plus Business Registration XOR Trade Certificate for artisans.
- [x] Locally implement/test upload-confirmation, seven-day, 72-hour, 24-hour,
  two-hour and invalidity notifications, inclusive-GMT expiry, replacement
  grace and deadline auto-disable. Current focused backend/mobile evidence is
  recorded above; deployed delivery proof remains open.
- [x] BR-66 replacement/rejection/resubmission controls and copy are aligned in
  backend and provider app: valid approved evidence stays closed, server time
  reopens expired evidence, rejected evidence remains resubmittable, and an
  approved profile photo stays admin-only. Deployed staging proof remains open.
- [ ] Verify provider-level approval cannot bulk-approve or mask an unreviewed document. The backend and admin safety branches contain complementary guards, but both must be reviewed/landed and exercised together on staging.
- [ ] Verify rejected/suspended dashboard access and verbatim notes without accidental go-live permission.
- [ ] Test admin concurrent review, stale screen, double-submit, document replacement, audit log, and rollback.

### Online state, location, and vehicles

- [ ] Request background location in context after install/onboarding; explain why, support denial/limited/approximate/one-time permission, and deep-link to settings where the OS permits.
- [x] The mobile working tree no longer triggers location permission from startup or automatic home-screen initialization; the prompt is reserved for an explicit Go Online/map-recenter action, and iOS purpose strings now disclose online request plus active-work background use (2026-07-17).
- [x] The mobile working tree now presents a prominent precise/background-location disclosure immediately before the Go Online permission request, supports “Not now,” and offers a guided app/Location Settings recovery action. Three widget tests pass. Android 11+ does not offer “Allow all the time” in the normal permission dialog, so the OS still requires the user to make that choice on the app settings page; the app cannot enable it silently (2026-07-17).
- [ ] Build and physically verify the local native iOS While In Use -> Always bridge on supported devices and denial/retry states. The Dart/provider suite covers invocation policy, but an exact signed iOS rebuild and device-level authorization evidence after the latest changes are not yet recorded.
- [ ] Validate Google Play/App Store background-location declarations, privacy copy, foreground-service indicators, iOS background modes, and battery impact.
- [ ] Make online state server-authoritative and bound to account, selected vehicle, session, fresh timestamp, accuracy, and explicit offline intent.
- [ ] Do not treat an intentional app-background socket close as an active-ride outage. Require stale REST heartbeat/location as well as socket/session loss before escalation, and test phone lock for longer than two minutes.
- [ ] Handle `availability:forced_offline` and fetch authoritative availability on resume, socket reconnect, token refresh, and process relaunch; never leave Online displayed when matching is Offline.
- [ ] Require a fresh, sufficiently accurate GPS fix before Go Online. Never publish an unbounded cached fix as dispatch authority.
- [ ] Extend the strict captured-time conditional current-location write with explicit sample IDs/sequences, REST-batch/artisan Online epochs, server receipt time, device-clock telemetry, and retry idempotency; prove duplicate and cross-session batches cannot alter matching or trails across replicas.
- [ ] Add a visible stream/heartbeat-health state and watchdog; reconcile permission/service/network loss during active work according to the approved business rule.
- [ ] Verify notification permission and device-token delivery readiness according to the approved go-online policy; current background request delivery falls back to push after the socket closes.
- [ ] Prevent delayed/batched GPS updates from reactivating an offline provider.
- [ ] Test locked/backgrounded for >2 minutes, socket loss with healthy REST versus total loss, killed/force-stopped app, reboot, Doze/OEM battery saver, denied/revoked/approximate permission, missing push token, poor GPS, mock location policy, clock skew, tunnel, network handover, duplicate/out-of-order batches, and reconnect storms.
- [ ] Replace per-replica full-provider zombie polling with leased/fenced, expiry-indexed bounded processing and prove load, catch-up, duplicate execution, and failure recovery.
- [ ] Require exactly one eligible selected vehicle to go online; snapshot that vehicle on accepted rides.
- [ ] Prevent vehicle switching while online/active unless the final rule permits a controlled offline transition.
- [ ] Test multiple vehicles, expired inactive vehicle docs, concurrent devices, and stale selected IDs.

### Ride dispatch and lifecycle

- [ ] Enforce one active offer target and an independent persisted 45-second deadline per driver.
- [ ] Atomically close decline/expiry for A before B becomes eligible; prevent duplicate acceptance and stale acceptance.
- [ ] Prove ordering/idempotency across HTTP, Socket.IO, FCM/APNs, Android overlay, iOS Live Activities/CallKit-style surfaces, and cold-start recovery.
- [ ] Reconcile the provider's active ride from the server after reconnect/process death; never trust only local state.
- [ ] Enforce one active client ride server-side and test concurrent create requests.
- [ ] Verify accept -> `en_route` is one authoritative transaction and all consumers receive the canonical transition.
- [ ] Verify no-driver/radius expansion, category matching, self-match prevention, cancellation, timeouts, scheduled rides, multi-stop, fare recalculation, payment, completion, dispute, and rating flows.
- [ ] Implement/test the exceptional second-driver-request rule only after its distance/exhaustion decisions are approved.
- [ ] Test multi-replica matching, cron leadership/leases, Redis outage/eviction, DB retry, socket loss, push delay, and exactly-once business effects.
- [ ] Complete [provider request overlay TODO](provider-request-overlay-todo.md) physical-device matrix.

### Artisan marketplace

- [ ] Enforce solo=1 and non-solo=3 capacity transactionally using the approved counted states.
- [ ] Enforce one active client artisan job and allow one ride plus one job.
- [ ] Prove a client cannot bid on or otherwise act as provider for their own request under the final account model.
- [ ] Audit request creation, categories, search, offers, bids, admin-review bids, quote changes, scheduling, chat/calls, arrival, work start/end, dual confirmation/payment, cancellation, dispute, rating, and history.
- [ ] Test concurrent bids/acceptance, stale prices, provider capacity races, scheduled overlap, process death, duplicate webhooks, and notification loss.

### Geofence and lifecycle overrides

- [ ] Enforce arrive/start/end proximity on the server using fresh, accurate location.
- [ ] Implement the approved warning/override flow with audit metadata and abuse monitoring.
- [ ] Test GPS drift, boundary points, location denial, stale/offline location, spoofing policy, multi-stop, and emergency exceptions.
- [ ] Prove staging accepts Accra and production rejects it while both accept approved in-boundary test points.

### Cancellations, disputes, ratings, and safety

- [ ] Implement the final waiting clock, party consequence, GH₵5 debt ledger, next-ride collection, carry-forward, refunds, cash behavior, receipts, and admin support tooling.
- [ ] Prevent cancellation penalties from relying only on client clocks or UI state.
- [ ] The approved 24-hour dispute admission and partial-refund economics are implemented locally: rides anchor to `ride.completedAt`; both artisan dispute admission and its two-hour payout hold anchor to `clientConfirmedCompleteAt`; original and current economics are distinct; cumulative refunds recompute from retained gross using the immutable rate snapshot; and post-payout recovery is limited to actual provider overpayment. BR-62 is also implemented locally: the client enters a MoMo number/network, explicitly chooses SMS or WhatsApp, OTP-verifies the destination, and only a masked immutable snapshot is exposed afterward. Cash repayments use a deterministic transfer reference and verify before every retry; missing/expired/mismatched verification and uncertain transfer outcomes remain fail-closed. Original-payment submission timeouts enter a non-terminal, no-resubmit reconciliation state so an authoritative late Paystack success/failure can settle the same row. Only a processed refund may update final economics or create the provider clawback. Fresh proof is **171/171 API suites, 3,499/3,499 tests, 108 migrations plus seed**, focused BR-62 backend **154/154**, API-client refund contract **3/3**, mobile all-package **326/326**, and four consecutive **50/50** isolated E2E passes. Two earlier isolated runs produced one transient driver restore-online 403 and one artisan bid-selection 404; keep this operational gate open until those are reproduced or ruled out on the pinned artifact, the same PII-free preflight passes on authorized staging/production-shaped data, and live-safe Paystack/OTP/refund/recovery canaries reconcile exactly.
- [x] **BR-62 final routing-integrity follow-up:** Paystack refund object IDs are database-unique after a duplicate-refusing migration. A webhook with an exact refund ID routes only to that row; a transaction-reference-only webhook is accepted only when exactly one refund has ever used that original charge, and sequential partial-refund ambiguity is dead-lettered for manual review rather than guessed. This supersedes the earlier local counts above: focused BR-62 is **156/156**, full API is **171/3,504**, all **109 migrations** plus seed replay, and the lifecycle gate has five consecutive **50/50** passes. Live provider canaries remain open.
- [ ] Verify rating window/reveal/suspension behavior separately from the dispute rule.
- [ ] The local three-second SOS hold, optional booking/GPS recording, exact-role
  ownership, authenticated-role access, admin null-context display, truthful
  dialer/contact status and recording containment pass focused tests. Still
  prove cancellation, haptics, duplicate/lost-response behavior, responder
  notification, accessibility and foreground/background/terminated operation
  on installed staging Android/iOS builds.
- [ ] Complete [CallKit/PushKit TODO](callkit-pushkit-todo.md) two-device/network/device-state matrix.

### Payments, tips, commission, payouts, and loyalty

- [ ] Use integer pesewas and immutable/idempotent ledger entries for every charge, split, debt, tip, earning, refund, chargeback, payout, and redemption.
- [ ] Remove every production hardcoded commission rate and financial-copy assumption; load validated admin config and snapshot it on the booking/payment record.
- [ ] Define fail-closed behavior when financial configuration is unavailable.
- [ ] Verify quotes, receipts, provider earnings, settlement, reconciliation, admin reports, exports, and both apps use the same commission snapshot.
- [ ] Replace the seeded driver/artisan legal promise of a fixed 20% only through a reviewed, versioned terms update; do not silently publish mutable commission language or fabricate a rate for legacy payments.
- [ ] Prove tips are zero commission and never included in commission bases; test card/MoMo success, failure, retry, refund, chargeback, and payout behavior after decisions close.
- [ ] Reserve exact earnings items before payout; make on-demand/scheduled payout idempotent and reconcile provider/gateway results before marking paid.
- [ ] Use GMT for schedule/cutoff and test DST-independent behavior, retries, partial failure, duplicate job, insufficient balance, weekends/holidays, and manual admin action.
- [ ] Implement GH₵0.50 redemption with atomic reservation and double-spend prevention after scope/cap/funding decisions close.
- [ ] Restore or settle reserved points deterministically on cancellation/payment failure.
- [ ] Complete [earnings mobile handoff](earnings-mobile-handoff.md) tests.

### Notifications, realtime, calls, and background delivery

- [ ] Create one canonical event catalogue with schema versions, recipients, mandatory/optional classification, dedupe keys, ordering rules, TTLs, and fallback channels.
- [ ] Verify Socket.IO room authorization and no cross-user/cross-role/cross-environment leakage.
- [ ] Test FCM/APNs invalid tokens, delayed/out-of-order/duplicate pushes, collapse keys, notification permissions, quiet modes, logout, reinstall, multiple devices, and terminated apps.
- [ ] Make mandatory operational/security notifications durable and observable; marketing opt-out must not suppress them.
- [ ] Audit call signalling, TURN credentials/expiry, missed calls, background/locked device, permissions, network handoff, abuse, and call metadata privacy.

### Admin application

- [ ] Inventory actual routes/pages/actions and compare to document expiry, payments, support, ride-category, verification, vehicle, dispute, payout, commission, loyalty, cancellation, user, and live-operations specs.
- [ ] Add browser E2E and role/permission tests; current acceptance specs are not proof.
- [ ] Enforce admin 2FA, secure session lifecycle, CSRF/XSS/CSP, scoped RBAC/IDOR protection, step-up auth for sensitive actions, and audit logs.
- [ ] Verify destructive actions use the approved hold/confirmation interaction where applicable and are idempotent/recoverable.
- [ ] Verify dynamic configuration validates ranges, previews impact, records old/new values, and propagates safely without stale caches.
- [ ] Test independent document review, verbatim rejection notes, suspension/reinstatement, payout/reconciliation, dispute evidence, exports, pagination, concurrent changes, accessibility, and responsive layouts.

### API contracts, errors, and backward compatibility

- [ ] Designate generated OpenAPI/current backend contracts as canonical; reconcile divergent mobile/backend prose documents.
- [ ] Add consumer contract tests for both mobile apps and admin against the exact backend release candidate.
- [ ] Standardize stable machine error codes, safe messages, HTTP status, retryability, field errors, correlation ID, and localized UI mapping.
- [ ] Test installed older mobile versions against new API/socket/push schemas and define minimum supported versions/forced-update policy.
- [ ] Verify idempotency keys and state monotonicity for every retryable mutation and webhook.

## 8. Security, privacy, and legal audit

- [ ] Refresh the threat model for current Paystack/Arkesel/Render/Neon/Upstash/Firebase/storage/TURN architecture.
- [ ] Run current SAST, dependency/SCA, SBOM, secret/history, container, IaC, license, and mobile binary scans; triage every result.
- [ ] Audit OWASP ASVS/API Top 10 and MASVS controls: auth, IDOR, injection, SSRF, webhook signatures, socket authorization, rate limits, abuse, secure storage, screenshots/backups, rooted/jailbroken policy, and certificate/TLS posture.
- [ ] Independently test admin authorization and all public/share/webhook endpoints.
- [ ] Validate encryption/key lengths, rotation/versioning, backup encryption, least-privilege secrets, environment separation, log redaction, and secret rotation drill.
- [ ] Register/confirm standing with Ghana Data Protection Commission before release and obtain current legal signoff.
- [ ] Replace the publicly served version `1.0.0` draft/stale client, driver,
  and artisan Terms with a new approved, effective, audience-specific version;
  verify the live responses and execute the approved versioned re-consent
  cutover before rollout.
- [x] Implement exact role-scoped Terms/Privacy consent: independent unchecked
  controls, exact document identity/version/audience, OTP-time refresh,
  transactional immutable evidence, no legacy fabrication, authenticated
  re-consent, active-work continuity, new-work/matching fences, immutable
  publication and final-root audit dissociation. Local evidence: 198/198 API
  suites, 3,964/3,964 tests, all 193 migrations and 75/75 lifecycle E2E.
- [ ] Complete processor/subprocessor agreements and data-transfer assessment for every third party.
- [ ] Implement/verify privacy policy access before consent, consent version/timestamp, access/rectification/export/deletion rights, and child/age policy.
- [ ] Implement and rehearse 90-day retention disposition, legal/financial exceptions, deletion/anonymization, backups, object storage lifecycle, audit logs, and account recovery.
- [ ] Write and rehearse incident response, breach assessment/notification, contacts, evidence preservation, user communication, and post-incident review.
- [ ] Obtain an independent penetration test for the release candidate or record an approved, time-bounded risk exception.

## 9. Infrastructure, deployment, and staging isolation

- [ ] Adapt and review backend `docs/runbooks/staging-environment-isolation.md`, `docs/runbooks/deploy-and-migrations.md`, and the workflows from PR #96 before following them; as written, they are not an operational zero-spend deployment path.
- [ ] Choose and document the zero-spend executor. For dedicated self-hosted GitHub runners, scope GitHub Environments to the proper branches and require production reviewers. For an audited local executor, provide equivalent per-environment secret isolation, approval records, locking, exact-SHA evidence, and log retention.
- [ ] Protect/limit Render environment access, rotate hooks, audit deploy events, and prevent manual bypass of guarded commands.
- [ ] Pin exact commit/artifact; verify build provenance, immutable dependencies/lockfiles, container digest/SBOM, and rollback artifact retention.
- [ ] Add and verify a restrictive root `.dockerignore`; exclude `.git`, every real `.env*`, keys/certificates/service-account material, dependency stores, caches, outputs, coverage, logs, tests/docs, and unrelated applications while preserving required build files.
- [ ] Replace the whole-workspace runtime copy with a production dependency/runtime allowlist, keep application files root-owned, align and pin the supported Node image, and start/health-check the built container in the gate.
- [ ] Verify pooled runtime versus direct migration URL, connection limits/timeouts, migration status, snapshot, restore, and schema compatibility.
- [ ] Replace the misleading `migrate status` “no drift” assertion with a real migration-to-schema diff and make non-empty drift release-blocking.
- [ ] Verify `RUN_MIGRATIONS_ON_BOOT=false`, `SEED_DATABASE=false`, `PILOT_REGION_CHECK_DISABLED=false`, and exact approved boundary fingerprint on every deployed service.
- [ ] Verify distinct fingerprints for DB, Redis, Firebase/APNs, Paystack, OTP, storage, Maps, email, and TURN; separately prove no Smile credential is configured for v1.
- [ ] Write a random staging Redis canary and prove it is absent through a read-only production query.
- [ ] Generate a staging fare/surge update and prove production counters/timestamps do not change.
- [ ] Emit a synthetic staging Socket.IO room event and prove a production test client receives nothing.
- [ ] Prove staging actions create no production DB, payment, identity, notification, or storage records.
- [ ] Reauthenticate testers and recreate old staging rides/jobs after the empty Redis cutover.
- [ ] Verify health/readiness endpoints fail with 503 when DB/Redis is degraded and Render health checks use the correct path.
- [ ] Test graceful SIGTERM/drain, zero-downtime behavior, overlapping deploy policy, rollback, and schema forward/backward compatibility.

## 10. 100k-DAU scalability, durability, and resilience

- [x] **DEFERRED / P0 before aggregate batch payout enablement:** every batch
  schedule, retry, and direct run fails closed unless `FF_BATCH_PAYOUTS=true`;
  absent or malformed values preserve unsettled earnings and perform no gateway
  call. The production Blueprint pins the flag false. The legacy algorithm still
  requires immutable earning membership, multi-replica settlement fencing,
  PostgreSQL/Paystack concurrency and crash proof before a future enablement.
- [ ] **P0 / pending-transfer exclusive claim implemented locally; production proof pending:** queued rows use an owner-token CAS and processing rows are reclaimable only after an expiring lease. Reclaimed attempts verify the deterministic transfer reference before any new POST; every success/failure/retry write is fenced by the current token. Replica-claim, lost-lease, verification, and stale-worker tests pass. Keep the gate open until the migration, concurrent real-PostgreSQL workers, Paystack stub, crash reclaim, and saturation behavior pass on the pinned staging candidate.
- [ ] **P0 / payment and payout sweeper races fixed locally; production proof pending:** stale payment and payout workers now verify provider state first, then use guarded status/cutoff compare-and-set transitions; dependent booking/outbox mutations occur only after the claim succeeds. Both success-webhook race orders and late-success recovery are covered locally. Prove the same boundaries with real PostgreSQL concurrency and Paystack-stub delayed webhooks before release.
- [ ] **P0 / durable Paystack webhook inbox implemented locally; production proof pending:** signature verification uses the exact raw body, a SHA-256 event identity and minimal routing projection are persisted before HTTP success, duplicates are idempotent, and an owner-token leased worker retries or dead-letters with bounded metadata. The accepted `transfer.reversed` event is included. Prove controller crash boundaries, multi-replica claims, backlog alerts, retention, and real Paystack retry behavior on pinned staging.
- [ ] **P0 / payout destination OTP bypass closed locally; production proof pending:** ordinary driver/artisan profile routes reject all payout method/account writes regardless of lock state. The OTP bind is the only writer and persists `payoutLocked=true`, an exact method/account snapshot, and recipient code; every transfer boundary rejects an unlocked, missing, or mismatched snapshot. Focused profile, OTP, transfer, and payout tests pass. Prove real Arkesel/Paystack binding, role isolation, destination-change revocation, and every enabled money-out path on staging.
- [x] **DEFERRED / P0 aggregate payout suspension contained locally:** instant
  per-payment release retains `payoutPreference=batch` earnings instead of
  bypassing the suspension. Batch/retry and provider/admin aggregate payout
  entry points are exact-opt-in and both production Blueprint values are false.
  Deployment-value verification remains part of staging acceptance; the unsafe
  aggregate algorithms are not approved for re-enablement.
- [ ] **P0 / tip and reversal accounting fixed locally; production proof pending:** tips use a deterministic persisted reference plus owner-token lease/CAS, never complete on a synchronous enqueue acknowledgement, and settle only from authoritative verification/webhook state. `transfer.reversed` is accepted, exact per-payment payout reversals reopen only the affected escrow, tip reversals reconcile idempotently, and ambiguous aggregate reversals dead-letter for manual review. Prove real gateway ordering, duplicates, delayed reversals, crash recovery, and accounting reconciliation on pinned staging.
- [ ] **P1 / duplicate marketplace safety jobs fixed locally; staging preflight pending:** one canonical marketplace service now owns the 15-minute scheduler and both response routes; the second safety scheduler/implementation is removed. Migration `20260718268000_welfare_check_single_owner` refuses ambiguous historical duplicates before adding a partial unique one-open-check-per-job invariant. Artisan prompts and admin alerts use durable per-check dedupe keys, admin alert stamping follows durable fanout, welfare checks bypass the chatter quota, and `welfareCheckId` is retained in data-only push payloads. Evidence: focused 71/71, API typecheck/build, changed-file lint, full 171 suites/3,499 tests, and fresh 108-migration plus four consecutive 50/50 E2E rehearsals pass. Run the PII-free duplicate preflight and real multi-replica/crash/push/admin-inbox staging proof before closure.
- [ ] **P1 / shared HTTP and WebSocket admission implemented locally; staging/load proof pending:** default/auth HTTP limits use one atomic Redis sliding window and the approved cross-namespace role-account WebSocket cap uses expiring atomic Redis leases shared across replicas. Every supported mobile/admin client already requests WebSocket directly, and all location/chat/calls gateways now reject Engine.IO polling, so horizontal scale no longer depends on sticky polling sessions. Authority failure is bounded and fail closed; real Redis E2E proves two instances share HTTP hits/blocks/expiry plus WebSocket caps, immediate disconnect release, and crashed-replica lease reclamation. Repeat with two pinned staging replicas under realistic reconnect/cap churn and Redis latency, loss, restart, and eviction pressure; attach capacity, alerting, and zero-lost-request evidence before closing this P1.
- [ ] **P1 / topology not codified or proven:** both repository Blueprints still describe one Frankfurt free-plan API, no worker, no replica/autoscaling settings, and an externally supplied Redis URL. Dashboard state may differ and must be reconciled rather than inferred from YAML. Record the exact paid topology, split API from workers, and prove same-region Redis with no-eviction/capacity/HA/restore behavior.
- [ ] **P0 / deploy only corrected database-endpoint behavior:** the current mobile-repository backend copy globally replaces pooled `DATABASE_URL` with `DIRECT_DATABASE_URL` and enables boot migrations, causing long-running replicas to bypass the pooler and allowing overlapping replicas to race migrations. The pending backend candidate scopes the direct URL to migration commands and disables boot migrations. Preserve that candidate behavior, use one separately authorized migration job, and prove pooled runtime connections before release.
- [ ] Approve a capacity model translating 100k DAU into peak concurrent clients/providers, online GPS writers, sockets, ride/job offers, HTTP RPS, OTP bursts, payment webhooks, admin users, data growth, and regional distribution.
- [ ] Size API instances/autoscaling, DB compute/storage/IOPS/connections/pooler, Redis memory/ops/connections/bandwidth, and third-party quotas with at least 2x agreed peak headroom.
- [ ] Ensure every multi-replica cron/job has leadership, lease/fencing, idempotency, and catch-up behavior.
- [ ] Replace the current minute-by-minute all-online-provider heartbeat scan and N+1 Redis/DB work with a bounded expiry-indexed design; load-test the projected peak online-provider population, expiry storms, worker crash/reclaim, and reconnect storms.
- [ ] Use durable outbox/inbox patterns for notifications, webhooks, payments, payouts, and state transitions; prove replay and poison-message handling.
- [ ] Run smoke, baseline, load, stress, spike, soak, reconnect-storm, and failure-injection tests using realistic read/write/socket/GPS/payment/OTP mixes.
- [ ] Record p50/p95/p99/max latency, error rate, saturation, queue/lag, dropped sockets, matching time, offer loss, DB locks, Redis evictions, memory, CPU, and cost.
- [ ] Agree pass thresholds and abort limits before running tests; attach raw k6 results and exact commit/environment to `docs/load-test-report.md` or a release-specific report.
- [ ] Test Redis outage/latency/eviction, DB failover/connection exhaustion, provider outage, DNS/TLS failure, region loss, process crash, clock skew, and retry storms.
- [ ] Define SLOs/error budgets for auth/OTP, ride creation, offer delivery/acceptance, location freshness, payment, payout, and admin operations.
- [ ] Define and rehearse backup/restore, RPO/RTO, regional disaster recovery, credential recovery, and lost/corrupt queue/state reconciliation.
- [ ] Run at least one restore rehearsal and one game-day/chaos exercise with timestamps and evidence.
- [ ] Validate cost model and budgets/alerts for normal peak, 2x peak, vendor failover, log volume, maps/routes, OTP, and egress.

## 11. Observability and operations

- [ ] **P1 / health and shutdown implemented locally; pinned staging proof pending:** the candidate exposes dependency-free `/v1/health/live` and concurrent DB/Redis `/v1/health/ready`, returns 503 while draining, retains `/v1/health` as a compatibility readiness alias, enables Nest shutdown hooks, points Render at readiness, and explicitly quits both Socket.IO Redis clients with a bounded force-disconnect fallback. The documented and validated `HEALTHCHECK_DB_TIMEOUT_MS` and `HEALTHCHECK_REDIS_TIMEOUT_MS` now control their respective probes with 3-second and 2-second defaults and 100–10,000 ms bounds; every result and duration feeds the protected metrics registry. Exact-current full API **175/3,540**, fresh 112-migration E2E **56/56** and five repeats **280/280**, schema-drift verification, online-index proof, build/typechecks/lint/diff, and the earlier real built-process HTTP+SIGTERM smoke pass. Keep P1 open until the exact pinned staging replica proves load-balancer removal, in-flight request/socket drain, worker lease handoff, dependency timeouts under fault, and no connection leak during repeated deploys.
- [ ] **P1 / protected runtime metrics implemented locally; monitoring and DR proof pending:** pinned `prom-client` exports HTTP request count/latency/in-flight, DB/Redis readiness results and duration, shared HTTP/WS admission outcomes, local socket leases, default Node CPU/memory/event-loop/resource saturation, scrape outcomes, and a sanitized version/commit marker. `/v1/metrics` is absent by default, startup validation requires a random bearer token of at least 32 characters when enabled, the route remains scrapeable during maintenance but independently authenticated, and route/label whitelists reject raw UUID/long-ID cardinality. Focused metrics/health/interceptor/admission evidence passes **62/62**; real HTTP E2E proves disabled 404, unauthorized 401, correct Prometheus content type, authorized metrics, and absence of fixture phone/account identifiers. Exact-current API **175/3,540** and five 112-migration-backed runs **280/280** pass. Keep P1 open: no external scraper, multi-replica aggregation, retained dashboards, thresholds, paging ownership, synthetic journeys, mobile/admin telemetry, DB-pool/Redis-server metrics, queue/cron/outbox/payment/business-invariant instrumentation, or timed Neon/object/Redis restore drill is yet proven.
- [ ] Add end-to-end correlation IDs across mobile, API, socket, notification, payment, and admin events without logging secrets/PII.
- [ ] Monitor mobile crash-free users/sessions, ANR, startup, network failures, background kills, battery/location impact, and symbolication for both platforms.
- [ ] Monitor API/admin errors, latency, saturation, DB/pooler, Redis memory/evictions/connections, socket counts, cron leases, outbox lag, and third-party health.
- [ ] Build business funnels/alerts for OTP accepted/delivered/verified, signup, provider verification/go-live, ride request/offer/present/accept/expire/reassign/cancel/complete, artisan jobs, payment/webhook/refund, and payout/reconciliation.
- [ ] Alert on ride loss, stale provider state, verified-provider go-live failure, duplicate charges/payouts, isolation violations, geofence guard failure, and mandatory notification failure.
- [ ] Define alert thresholds, owners, paging/escalation, runbooks, maintenance windows, synthetic journeys, and status-page/customer messaging.
- [ ] Add release markers and compare canary/cohort metrics to baseline.
- [ ] Verify log retention/redaction/access and that high-cardinality telemetry cannot exhaust the platform.

## 12. QA, stores, rollout, and support

- [ ] Pin exact mobile/backend/admin release commits, version/build numbers, API minimum versions, feature flags, and release notes.
- [ ] Correct and approve the public client/provider store descriptions before submitting `1.4.1`. Current copy must not promise instant/same-day payout for every booking, a 24-hour verification SLA, both artisan trade credentials, one/two-tap SOS, full Twi support, loyalty redemption, or any deferred feature. Describe the approved three-second hold, manual verification, Ghana Card plus either Business Registration or Trade Certificate for artisans, and role/vehicle-specific driver evidence accurately. Do not publish a support mailbox until its named monitoring owner is confirmed.
- [ ] Run unit, widget/component, contract, integration, E2E, migration, socket, webhook, security, and performance suites with fresh artifacts.
- [ ] Execute real-device matrices for supported Android/iOS versions, low-memory devices, OEM battery savers, permissions, background/terminated/locked state, poor/offline/flapping networks, and clock skew.
- [ ] Complete accessibility, localization/copy, Ghana phone/currency/timezone, dark/light mode, text scaling, screen reader, and reduced-motion checks.
- [ ] Validate Android signing/AAB/symbols/Play declarations and iOS signing/archive/dSYM/privacy manifests/background modes/App Review notes using [release setup](release-setup.md).
- [ ] Test internal Play/TestFlight builds against isolated staging before store review.
- [ ] Define UAT scripts and obtain product, mobile, backend, admin/ops, QA, security/privacy, finance/payments, support, and release-owner signoffs.
- [ ] Create canary/phased rollout cohorts, kill switches, monitoring windows, numeric abort thresholds, rollback commands, schema constraints, and customer/support communications.
- [ ] Confirm on-call coverage and support macros for OTP, verification/go-live, ride loss, payment, payout, cancellation, and account recovery.
- [ ] After production start, perform T+15m, T+1h, T+24h, and T+72h checks and reconcile rides, payments, OTP, notifications, and payouts.

## 13. Final production go/no-go checklist

- [ ] Exact staging release candidate has passed all required exact-SHA local/self-hosted CI equivalents and tests, with durable evidence.
- [ ] Every confirmed business rule has implementation plus contract/E2E evidence.
- [ ] Every open business decision is resolved or explicitly removed from release scope.
- [ ] All reported live bugs have verified fixes, telemetry, support workaround, and rollback.
- [ ] No open/unwaived P0 or P1 finding remains.
- [ ] Any accepted P2 has owner, approver, mitigation, due date, and expiry.
- [ ] Staging and production isolation fingerprints/canaries pass.
- [ ] Production snapshot/restore and migration rehearsal pass.
- [ ] 100k-DAU capacity, soak, reconnect, and failure tests pass agreed thresholds with 2x headroom.
- [ ] Security/privacy/legal/processor/incident-response gates are signed off.
- [ ] Production monitoring, paging, on-call, status communication, and support are ready.
- [ ] Store builds are signed, symbolicated, reviewed, and staged for phased release.
- [ ] Rollback has been rehearsed and abort thresholds are visible to the release commander.
- [ ] Named release commander records the final GO with timestamp and exact SHAs.

## 14. Evidence and reference index

### Current policy/runbooks

- [Mobile release workflow](release-workflow.md)
- [Mobile release setup](release-setup.md)
- [Provider request overlay TODO](provider-request-overlay-todo.md)
- [CallKit/PushKit TODO](callkit-pushkit-todo.md)
- [Earnings mobile handoff](earnings-mobile-handoff.md)
- Backend PR #96: `docs/runbooks/staging-environment-isolation.md` (reference pending zero-spend adaptation)
- Backend PR #96: `docs/runbooks/deploy-and-migrations.md` (reference pending zero-spend adaptation)
- Current backend tree: `tests/load/README.md` (fail-closed load-harness operator contract)
- Current backend tree: `docs/load-test-report.md` (load, fault, reconciliation, and sign-off evidence record)
- Backend PR #96: `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`, and `infrastructure/docker/entrypoint.sh` (not an approved operational path until adapted and reviewed)
- [GitHub Actions billing](https://docs.github.com/en/billing/concepts/product-billing/github-actions): standard self-hosted runner usage does not consume GitHub-hosted runner minutes.
- [GitHub secure-use guidance](https://docs.github.com/en/actions/reference/security/secure-use): use isolated, disposable self-hosted infrastructure; do not place a persistent release runner on a credentialed developer workstation.

### Reference only; must be freshly revalidated

- Backend `docs/security-audit-report.md` (2026-04-10 historical stack/baseline)
- Backend `docs/dpa-compliance.md` (living legal checklist with unresolved/stale items)
- Backend `docs/load-test-report.md` (blank result template, not evidence)
- Backend/admin specs under `docs/admin-frontend-spec-*`, `docs/admin-module.md`, and `docs/admin-auth-flow.md`
- PRD/EDD, project-status, sprint, handoff, and prose API documents; reconcile their owners and conflicts with generated contracts/current code.

## 15. Decision and progress log

| Date (GMT) | Type | Decision/progress | Evidence/next action |
| --- | --- | --- | --- |
| 2026-07-16 | Infrastructure | Staging Neon recorded as project `myshop`, branch `staging`, AWS Frankfurt; pooled runtime URL reported. | Verify endpoint fingerprints, PII state, and direct/runtime secret placement. |
| 2026-07-16 | Infrastructure | Staging Render remains suspended; Redis isolation values were saved-only. | Do not resume alone; prove safe exact-commit first deploy or replace service. |
| 2026-07-16 | CI | Paid hosted Actions are excluded. On clean Node 20 Linux at PR head `3f27dd9…` (tree-identical to merge `1f1ada8…`), lint/typecheck, fresh-DB migration smoke, unit/coverage, and Docker build passed; E2E failed. | Repair E2E, then rerun all five gates on the resulting exact `staging` SHA; use a dedicated disposable self-hosted runner for recurring releases. |
| 2026-07-16 | Repository control | GitHub API reported branch protection unavailable for the private repo on the current plan. | Upgrade or record/enforce manual merge control. |
| 2026-07-16 | Infrastructure | Owner confirmed staging Render Auto-Deploy is OFF and `DIRECT_DATABASE_URL` is absent. | Verify remaining environment values/fingerprints and configure the protected direct migration secret outside Render. |
| 2026-07-16 | Merge state | PR #96 was merged by `Ayiks` at 21:16:48 GMT with no recorded review; merge commit `1f1ada8…` has the audited tree. Its hosted push CI failed at startup, and no deploy run was listed. | Auto-deploy remains OFF. Treat this as containment: do not deploy/unsuspend; land a reviewed E2E follow-up and re-gate the new staging head. |
| 2026-07-16 | E2E | Fresh PostGIS/Redis run failed both suites and 50/50 tests at stale `Client.referralCode` fixtures; teardown leaked a worker and fixed fixture identities prevent clean reruns. | Move fixture referral codes to `User`, add E2E typechecking/isolation, and rerun on pristine services. This test-only correction does not resolve BR-03. |
| 2026-07-16 | Database | All 69 migrations and trigger guard passed, but an actual Prisma migration-to-schema diff returned exit 2 after `migrate status` said up to date. | Reconcile the reported drift and make a genuine diff check part of the release gate. |
| 2026-07-16 | Container | Pristine merge commit Docker build passed with 6.86 MB context and manifest `sha256:e156ce16…`; isolated test-mode startup returned `/v1/health` 200 with DB/Redis `ok`. No `.dockerignore` exists: after local caches, the same build sent 814.98 MB and produced a different image. Runtime/CI Node versions also differ. | Restrict context/runtime contents, align and pin Node, rebuild, inspect SBOM/image, and add representative staging-mode startup plus health smoke to the gate. |
| 2026-07-16 | Deployment | Current `deploy.yml` requires paid hosted infrastructure and a successful GitHub CI record, so it cannot consume this local evidence. | Review a zero-spend dedicated self-hosted or audited local migration/deploy executor before staging start. |
| 2026-07-16 | Audit | Master cross-system release tracker created; current verdict remains NO-GO. | Work immediate queue, then update each row with fresh evidence. |
| 2026-07-18 | 100k-DAU infrastructure and durability audit | All 31 API cron decorators, Blueprint topology, DB/Redis/socket limits, financial workers/webhooks, health/shutdown, query hot paths, telemetry, backup/DR, and existing load scripts were inspected. Release-blocking defects include duplicate multi-replica batch payouts, a non-exclusive pending-transfer claim, success-overwriting stale sweepers, non-durable Paystack webhook acknowledgement, and unsafe direct-DB/boot-migration behavior in the older deployment copy. Process-local throttles, duplicate welfare jobs, polling affinity, shared authoritative/cache Redis, missing worker topology, graceful drain, metrics, restore, and representative load evidence remain P1. No external system or data was changed. | Do not horizontally scale or release yet. First land atomic money-movement claims/inbox/CAS transitions and the corrected DB entrypoint; separate workers from API; then implement shared limits, same-region no-eviction Redis, readiness/drain, telemetry/DR, and measured multi-replica 100k-DAU tests. |
| 2026-07-17 | Staging deployment | `myshop-api-test` started with boot migrations disabled, protected Ghana fingerprint `ece9c…`, staging Redis/Socket.IO prefixes, DB connection, and `NODE_ENV=staging`. Fetched GitHub `origin/staging` remains `1f1ada8…`. | Retain Render event SHA/image/build ID and rollback reference as durable provenance. |
| 2026-07-17 | Staging health | Independent `/v1/health` returned HTTP 200 with DB and Redis `ok`; OTP-channel discovery, ride-category, and surge canaries also returned HTTP 200. Render's prior GitHub deploy incident has no unresolved record. | Begin controlled isolation and functional canaries; do not treat startup health as production-release approval. |
| 2026-07-17 | Staging integrations | Startup shows production Arkesel/WhatsApp channels, disabled SES email, and disabled emergency-recording storage. The deployed OTP path has no allowlist/global cap/kill switch and can acknowledge before provider acceptance. | Block live OTP testing until the two-number containment patch is deployed; configure safety storage and decide email release scope before broad staging access. |
| 2026-07-17 | OTP test decision | Owner approved live Arkesel and WhatsApp usage for the current two-developer staging phase. Audit then proved the deployed API cannot technically enforce that scope. | Deploy the E.164 allowlist, Redis caps, timeout, honest error handling, and telemetry before sending; privately configure the two numbers and reassess before broader access. |
| 2026-07-17 | OTP containment implementation | Local backend delivery policy defaults off and adds explicit allowlist/live modes, atomic Redis global plus phone/channel reservations, fail-closed Redis behavior, bounded carrier requests, sanitized provider failures, and awaited carrier acceptance for initial register/role-login sends. Six focused auth transport/policy suites pass 34/34, AuthService passes 104/104, and the integrated API passes 107 suites/2431 tests; no live number, carrier, credential, or environment was accessed. Enumeration-safe provider login no longer claims delivery, while ambiguous delivery failure deliberately leaves the code active because it may still arrive. | Resolve the ambiguous-timeout/mobile-navigation policy, implement authenticated per-attempt callback correlation and delivery/cost telemetry, approve caps, privately configure staging, prove kill-switch/cost controls, review, and deploy controllably before resuming carrier tests. |
| 2026-07-17 | Provider audit | Confirmed the destructive provider-requeue migration, premature replacement invalidation, missing expiry/grace worker, inconsistent online gates, bulk document approval, absent vehicle model, and sibling-role exposure. | Audit staging impact read-only, contain the production migration, land reviewed eligibility fixes, then implement only the remaining explicitly approved rules. |
| 2026-07-17 | Ride-loss audit | Confirmed B+ offer instant-expiry, pre-limit candidate starvation, unsafe one-driver/45-second config overrides, skipped exact maximum radius, Redis-only authority, unlocked all-replica expiry scans, competing pending offers, and missing delivery outcomes. The local narrow patch now passes 52/52 focused and 98 suites/2290 API tests plus typecheck/build/targeted lint/format/diff check; exhausted 10 km search cancels with the exact approved client event/push copy. | Resolve the recorded delivery/capacity decisions, implement PostgreSQL dispatch/offer/outbox authority and indexes, then execute concurrency, Redis-failure, load, and physical-device tests. |
| 2026-07-17 | Document contract | Owner confirmed that a printed expiry date is inclusive in GMT: 17 July remains valid for the whole day and becomes invalid at 00:00 GMT on 18 July. Roadworthiness and insurance are distinct records attached to the relevant vehicle. Strict API and mobile boundary tests pass, but the current provider-scoped storage model cannot yet satisfy per-vehicle ownership. | Retain the strict date-only contract; implement the expand-only vehicle model only after the remaining lifecycle decisions and legacy-data preflight are approved. |
| 2026-07-17 | Mobile fixes | Artisan XOR/status fallback, independent document rendering, strict inclusive-GMT expiry, required insurance display, deterministic selection, removal of fabricated vehicle state, S3 confirmation, auth persistence ordering, FCM role selection, redirected-signup CTA, background-location disclosure/Settings recovery, coordinate-log redaction, one-way authoritative Offline reconciliation, contextual iOS Always-permission escalation, and the BR-30 Go Online threshold passed the then-current six-package gate. Android AABs and iOS archives/IPAs built from that earlier tree, but those IPAs became stale after the required app-owned privacy-manifest corrections and must not be uploaded. | Complete server matching/lifecycle BR-30 authority, BR-31 invalid-token force-offline/matching freshness, BR-32 restoration, the versioned availability/location contract, and physical-device staging evidence, then resolve backend contracts before merge. |
| 2026-07-17 | Active-role API containment | Local `/users/me` now selects only the access-token role, excludes sibling/sensitive internals, role-guards mutations, and returns `<ROLE>_PROFILE_NOT_FOUND` instead of a blank 200 for absent/soft-deleted role rows. Focused user tests pass 83/83; after location/security/OTP/availability/privacy additions the full API passes 107 suites/2431 tests, typecheck/build, config build, relevant-file formatting, lint with 0 errors/48 existing warnings, and diff check. | Keep production blocked until shared-principal deletion, referral, emergency-contact, IDOR, compatibility, migration, and staging decisions/evidence close BR-03. |
| 2026-07-17 | Background availability audit | Android/iOS declarations and in-context disclosure exist, but phone lock/background closes the active-ride socket; cached/delayed batches lack monotonic server authority; active-work loss is split-brain; and the per-replica all-provider liveness sweep is unbounded/N+1. Local containment makes false-disconnect grace depend on post-close liveness and reconciles authoritative backend Offline into an idle mobile UI; backend tests now read the actual role/provider heartbeat key and reject malformed heartbeat/location data. API 2431/2431, provider 67/67, API client 80/80, client 26/26, API build/typecheck/lint, all-package analysis, and diff checks pass. BR-30 is implemented only for mobile Go Online; BR-31 has contextual mobile recovery plus an Offline-to-Online active-token backend gate; BR-32..BR-34 remain incomplete. | Add server matching/lifecycle BR-30 authority, invalid-token force-offline/matching freshness for BR-31, and BR-32..BR-34; close the offline/assignment race with a shared invariant, replace Redis/unleased polling with versioned durable authority, and run permission, locked-device, failure, multi-replica, and 100k-scale evidence before staging signoff. |
| 2026-07-17 | Location/privacy containment | Exact coordinates and selected high-risk phone/provider-response details were removed or sanitized locally. HTTP request logging now records only a bounded path plus query/body key names; `+233`, `233`, and local Ghana phone formats fail closed; audited Paystack, Smile/KYC, media, admin, refund, notification, and safety paths use stable public errors plus safe error classes. Utilities pass 34/34 and the API passes 107 suites/2431 tests plus lint/typecheck/build/format/diff gates. This remains partial: full-suite output identified raw push, places/routes, pending-transfer, welfare, dispute/reconciliation, tip, VoIP, and WhatsApp provider errors, and `/media/confirm` still lacks authoritative upload-intent/object binding. | Continue the repo-wide log/HTTP-response redaction and media-provenance fixes, restrict/rotate the Maps credential, review/land, then verify redacted staging logs. |
| 2026-07-17 | Encryption and reviewer bypass | Local config/runtime validation now rejects a non-hex pseudo-key even when it is 64 characters; the full API passes 2431/2431 and config build/typecheck/build/format/lint/diff checks pass. Audit also confirmed the reviewer account can combine fixed OTP, device-lock bypass, and region bypass when enabled; BR-34 now forbids it as a standing normal-production capability. | Preflight encryption key/canary/backup/rotation and GCM configuration; implement and prove the isolated/time-boxed reviewer controls before any production candidate. |
| 2026-07-17 | Encryption deployment follow-up | Render's official Blueprint contract confirms `generateValue: true` creates a base64-encoded 256-bit value, incompatible with the API's required 64-character hexadecimal key. The local Blueprint now uses manual retained `sync: false`; the stale unused CBC configuration label is removed and the example names AES-256-GCM. Integrated API/config gates pass. No Render environment or key was read, printed, regenerated, or changed. | Privately preflight and escrow staging and production independently and prove an existing-ciphertext plus round-trip canary before this strict validation can deploy. Add version/key ID, authenticated context, boot canary, and resumable rotation before any future key change. |
| 2026-07-17 | Arkesel callback containment | Official V2 documentation uses `sms_id` and `status` callback query parameters and does not document a provider signature. The audited auth OTP send path does not attach a callback, while the notification callback expected a different body contract and failed open without a configured secret. The local patch now returns 503 when its secret is absent, compares the query secret in constant time, masks `+233`/`233`/local recipients, maps terminal statuses conservatively, and prevents query values from entering HTTP logs; focused callback/SMS tests pass and the integrated API passes 2431/2431. | Do not treat a static URL secret as trusted delivery proof. Implement high-entropy per-attempt correlation/HMAC, expiry/replay rejection, idempotent status transitions, auth-OTP callback attachment, and delivery/cost telemetry against a staged real callback. |
| 2026-07-17 | Business decision batch | Owner approved expiry enforcement for licence/roadworthiness/insurance with inactive-vehicle isolation, cancellation plus “All drivers are busy. Please try again.” after exact 10 km exhaustion, 30s/50m matching and 15s/30m lifecycle location thresholds, notification reachability as a Go Online prerequisite, relaunch restoration only after full eligibility plus fresh-location revalidation, degraded-but-finish-active-work handling after location loss, a normally disabled/restricted reviewer-bypass policy, and manual v1 verification with Smile automation deferred (BR-28..BR-35). BR-29's exact copy and BR-35 source containment are implemented locally; BR-30 and BR-31 are partial; BR-32..BR-34 are not implemented end to end. | Resolve server-dispatch/handset-delivery acknowledgement semantics and timeout; then complete all approved rules end to end with stable codes and staging proof. |
| 2026-07-17 | SmileKYC v1 scope containment | Local backend removes the Smile initiation/public callback routes while preserving dormant future implementation, removes the Render secret prompt, and marks the source API contract and provider legal terms as manual review. Client/provider copy no longer claims Smile verification and provider compliance now reflects aggregate manual verification. Focused controller 4/4, API 107 suites/2431 tests and build, provider 67/67, client 26/26, and both app analyzers pass. | Review/land; confirm deployed old routes are 404 and all Smile credentials are absent; obtain legal review and publish a new terms version before release. |
| 2026-07-17 | Admin verification safety | The admin could approve despite rejected/unresolved docs, treated upload-confirmed rows as approved, skipped to a provider decision when no documents were returned, mislabeled approval as sending the provider online, compared/displayed expiry through the browser timezone, omitted insurance, and tracked the wrong expiry types. Reconstructed branch `codex/admin-verification-v2` blocks unsafe approval, preserves server errors, uses GMT, exposes insurance with required expiry, tracks the approved three expiring types, and prevents new artisan XOR conflicts. ESLint has zero errors, TypeScript passes, diff check passes, and the full 45-route Next.js production build passes; the repository has no tests. | Add component/E2E/timezone-boundary tests, repair legacy conflicts, implement vehicle-bound evidence, review with the authoritative backend guard, land both, and run concurrent/stale-document staging scenarios. |
| 2026-07-17 | Dynamic commission safety | Local branch `codex/commission-v2` removes the default/hardcoded runtime rate, validates admin values, fails settlement and ride creation closed when required configuration is unavailable/invalid, and removes commission math from the synchronous fare helper. Utils 28/28 and focused API 125/125 tests, API build, typechecks, lint (0 errors), and diff check pass. | Resolve rate scope, commission base, and snapshot lifecycle; then review/land and prove quote, settlement, earnings, reports, promos, cash debt, tips, refunds, and historical reconciliation use one authoritative snapshot. |
| 2026-07-17 | BR-36 receipt decision and adversarial ride review | Owner approved the configurable 10-second authenticated delivery-receipt window. The local v2 candidate now fixes Redis-as-failure-authority, cross-role socket actions, slow-initialization recovery, duplicate canceled-card replay, rolled-back late expiry, false notified counts, all-replica orphan recovery, handset-clock rejection, prior-driver query growth, and oversized duplicated push details. Final local evidence is 99 API suites/2327 tests, API/database build and typecheck, provider 75/75, API client 85/85, analyzers, and Prisma format/validate/generate. Independent review still proved two P0 policy/architecture gaps: durable client booking idempotency after a lost create response, and an unsafe mixed v1/v2 backend/provider rollout. | Keep production NO-GO. Decide existing-ride recovery versus cancel-on-lost-response; choose mandatory-update maintenance cutover versus a built compatibility phase; then implement, integration-test on real PostgreSQL/multi-replica/Redis failure, rehearse migrations/cutover, and prove physical devices plus load before staging activation. |
| 2026-07-18 | BR-37..BR-39 ride durability decisions | Owner approved same-ride recovery through a durable client booking key, two-minute cancellation only for never-dispatch-ready initialization, mandatory client+provider updates plus maintenance drain/stop-old/start-new V2 cutover, and a five-minute dispatch-ready matching cutoff that preserves a timely recipient's full 45-second decision window. | Local backend/mobile candidates now implement these contracts; affected client 39/39, API client 88/88, API 99 suites/2341, build/typecheck/analyze gates pass. Keep P0 open for minimum-version branch integration and rollout proof, exact-patch integration, isolated PostgreSQL/multi-replica/device/load proof, and runbook rehearsal. |
| 2026-07-18 | BR-37 client recovery implementation | Client persists only booking UUID, SHA-256 fingerprint, and timestamp; resolves before retry/restart/changed request; fails closed on uncertain lookup; and keeps ambiguous keys. Backend binds client+UUID uniquely to the canonical request, returns live state, rejects changed reuse, and cancels after two minutes only if dispatch never became ready and no offer exists. Client recovery ceiling corrected from an unsafe 255 seconds to BR-39-safe 375 seconds; mandatory client update is approved. | Fault-inject every post-commit boundary, integrate/prove the minimum-version candidate, run real PostgreSQL concurrency and physical kill/relaunch/account-boundary tests, then review/land. |
| 2026-07-18 | BR-38 cutover containment | [Runbook](ride-v2-controlled-cutover.md) added. It uses a ride-only booking gate because global maintenance would block active trips, requires two zero-legacy snapshots and stopped old workers, starts V2 closed, fences stale Redis config, and prevents old-artifact rollback once protocol-1 work exists. Booking migration DDL is resumable around concurrent indexes and forces the DB gate closed. | Rehearse on an isolated production-size clone and staging; pin artifacts/owners/PITR/telemetry; do not execute on production without a separate authorization. |
| 2026-07-18 | BR-37 response-snapshot and promotion audit | The ride candidate now writes the complete priced booking response before any fallible dispatch work, so same-key recovery cannot lose ETA/fare/stops if the final convenience snapshot fails; focused ride tests pass 63/63 and API typecheck/diff checks pass. Independent review then proved promotion caps/scopes/per-client limits are non-atomic or unenforced and failed initialization leaks usage. | Keep P0 open; obtain the consume/restore policy, implement an atomic ride-bound reservation ledger, and prove cap/cleanup/dispatch races on PostgreSQL. |
| 2026-07-18 | BR-40 promotion lifecycle decision | Owner approved atomic reservation at ride booking; exact-once consumption when the ride reaches `completed`, whether or not immediate payment collection succeeds; release only when the ride never completes because of initialization/no-driver/system/driver/client cancellation; indefinite retention through retryable collection failures with no payment-failure release; and separate cancellation-abuse controls. | Implement the ledger and every transition; obtain the separate canonical scope decision and repair legacy values; prove last-slot/per-client/idempotency/cleanup/payment races before release. |
| 2026-07-18 | Media upload-intent implementation and adversarial review | The local backend/mobile candidate replaces client-authoritative confirmation with a durable intent, provider-derived finalization, metadata plus JPEG/PNG/PDF signature checks, and transactional confirmation. Initial gates pass: media 24/24, API 110 suites/2462, API client 93/93, client 39/39, builds/typechecks/Prisma/analyzers/diff. Adversarial review then found role loss after confirmation, rollout incompatibility, non-idempotent response-loss recovery, arbitrary-URL consumers, storage-cost/privacy/purge gaps, encryption-sensitive ETag assumptions, and blocking migration DDL. Owner approved mandatory minimum versions for affected `/media/*` callers and no legacy fallback. | Correct and re-review role/retry handling; integrate/prove the version candidate; decide privacy/retention policy; enforce every consumer; prove an online-safe migration and real S3/Cloudinary behavior before release. |
| 2026-07-18 | Media/support role-isolation follow-up | The local backend now persists the exact client/driver/artisan role on upload intents, confirmed uploads, and new support tickets. Every user-facing ticket list/read/reply/status/read-receipt path requires exact `userId + ownerRole`; legacy role-unknown tickets remain admin-visible but return 404 to every user role and produce no user-targeted push. Confirmation is idempotent only for the same owner role. An independent rerun passed 5 focused suites/28 tests; API/database typechecks, Prisma validation/generation/schema diff, and diff check also passed in the implementation stream. | Keep the gate open until the migrations apply/retry on isolated PostgreSQL, real S3/Cloudinary objects pass, every arbitrary-URL consumer is converted or disabled, the minimum-version cutover is integrated, and privacy/retention decisions are approved. |
| 2026-07-18 | Reviewer-bypass containment and adversarial review | Local configuration requires explicit enablement plus exact reviewer identity/OTP and future expiry, rechecks expiry while running, refuses production at configuration and startup layers, excludes credentials from Render configuration, and avoids reviewer identifier logs. Adversarial review proved that existing sessions survive disable/expiry, OTP/role-selection state could cross expiry, no maximum window or restricted endpoint matrix exists, rate/audit/alert/kill-switch controls are incomplete, and the seed violates approved document rules. The local follow-up now carries the exact expiry through fixed OTP and selection state and rechecks it at load plus session mint; focused auth/reviewer tests pass 125/125 and API typecheck/diff checks pass. | Rerun the integrated API; prove old production use or revoke/rotate every reviewer state; set maximum window; build and test the restricted synthetic identity, durable controls, repaired seed, and operational revocation before BR-34 closes. |
| 2026-07-18 | Mandatory mobile-version candidate | The combined local backend candidate now includes dormant-by-default minimum builds for client/provider Android/iOS, stable fail-closed 426 responses, HTTPS-only store links, and guards for ride creation, media, provider availability, device registration, foreground-driver GPS, background-driver GPS, and artisan GPS. Shared mobile metadata plus a non-dismissible update state is implemented in both apps. The integrated backend guard/config/route tests pass 22/22, and API/config typechecks plus diff check pass; earlier isolated mobile evidence was client 40/40, provider 76/76, API client 98/98, and shared UI 23/23 with clean analysis. | Keep P0 open: complete exact-tree ride integration and full gates; pin higher build numbers and exact store URLs; reset old receipt-capability rows only after old workers stop; bind re-registration to the current authenticated online session; build signed artifacts; prove installed old/new versions; and rehearse BR-38. |
| 2026-07-18 | Mandatory mobile-version hardening follow-up | Route coverage now includes foreground-driver, background-driver, and artisan GPS Go Online paths. Backend configuration rejects missing or non-HTTPS store URLs whenever a minimum is active, preventing a forced-update dead end. Focused guard/config/route tests pass 22/22; the baseline API passes 98 suites/2301 tests; config/API typecheck and build pass. Independent ride review confirmed that a stale active `offerReceiptVersion=1` token can otherwise remain DB-dispatchable and burn ten-second windows. | Bind offer capability to the current authenticated online session, reset stale rows while old workers are stopped, prove zero candidates until the pinned app re-registers and goes Online, then run old/new installed-build and delayed-delivery staging tests. |
| 2026-07-18 | Public mobile-store metadata verification | Exact bundle/package identities were reconciled with the public stores: client `com.gilmoretech.myshopclient` maps to Apple app `6773658114` and the Play listing; provider `com.gilmoretech.myshopprovider` maps to Apple app `6773660049` and the Play listing. Apple's Ghana lookup reports live client `1.3.9` and provider `1.4.0`; the official Play pages now report `1.3.11` for both apps. Both repository pubspecs and the release resolver declare the approved next marketing version `1.4.1` with an operator-supplied private build number above the `+20` floor. | Verify the highest private Android/iOS build numbers in Play Console and App Store Connect, correct the inaccurate listing descriptions, then set minimum builds only after signed artifacts are uploaded and installed old/new-build cutover tests pass. |
| 2026-07-18 | Exact-tree mobile regression gate | `melos run test` passes every current test in all six test-bearing packages; the client finishes 47/47 and provider 100/100. `melos run analyze` executes `dart analyze --fatal-infos` across all seven packages and reports no issues. This run includes the artisan optional-profile-photo correction, exact-role document logic, selected-vehicle server-preflight delegation, durable booking recovery, V2 receipt parsing, background-location disclosure, and mandatory-update metadata/interceptor paths. | Keep release gates open for signed Android/iOS builds, private console version/build reconciliation, installed old/new build proof, real locked/background permission journeys, and staging API/socket/notification integration. |
| 2026-07-18 | Current-source Android release compile | `tool/build.sh client android` and `tool/build.sh provider android` both succeed from the current mobile tree. Client AAB: 119.8 MB, SHA-256 `fcc463279d0513951708414616f0a084e4fb1b082e0494c2aff4d03333851f9e`, signer SHA-256 `79:71:B3:D1:B0:62:D1:17:D2:E4:8B:E7:71:4D:48:5C:9C:87:30:31:BC:81:6A:05:60:5D:38:E4:89:29:4D:D0`. Provider AAB: 118.2 MB, SHA-256 `480608e0ff7912f157f226202e9d0dc26f719eca0deae991199e1d48e452b637`, signer SHA-256 `B8:6D:8B:0F:4A:ED:17:E0:BD:AE:D0:AC:17:87:73:1F:29:48:8B:16:CC:3D:BA:78:9A:5E:CF:FE:65:22:D8:C8`. `jarsigner` verifies signatures but strict trust exits 4 because both Android upload certificates are self-signed and untimestamped; that is not proof of Play identity. | Compare each signer fingerprint with its Play Console upload certificate, choose unused version codes, rebuild after the approved version change, upload only to an internal track, and run Play-installed staging journeys. These artifacts are compile evidence, not releasable store artifacts. |
| 2026-07-18 | Android Maps signer/config reconciliation | The provider AAB signer SHA-1 matches its app-specific production Maps certificate setting. The client app-specific setting is absent, but the configured generic fallback exactly matches the client AAB signer; no production API key value was printed. | Current builds receive the correct certificate header. Add the client-specific non-secret fingerprint during the next controlled local configuration update so a future generic fallback rotation cannot silently break client Maps calls; separately verify Google Cloud package/certificate/API restriction state. |
| 2026-07-18 | Current-source iOS release compile | Initial archives revealed that both app-owned `PrivacyInfo.xcprivacy` files existed in source but were absent from exported IPAs; Xcode resource membership is fixed in both projects. A later audit found the client declaration also omitted Government ID and sensitive information despite Ghana Card collection. The corrected client archive now embeds and validates those types plus precise location with tracking disabled. Both current-source archives compile, but both App Store exports are blocked because the machine has no authorized Apple Distribution account/certificate. The older client IPA hash `2221aac25b7f2e7b2d912478dd8899f8aa593e6f9c6c66d79ba63156763ab805` and prior provider IPA are stale and forbidden for upload. | Restore an authorized Xcode/App Store signing identity, choose versions/build codes above the private store maxima, rebuild both IPAs, inspect embedded manifests/entitlements, validate with Transporter, and prove production APNs/background behavior. |
| 2026-07-18 | Fare, cancellation, category, and version decisions | Owner approved BR-47 through BR-50 and BR-54: vehicle categories use distinct Coordinator→Regional Manager review; frozen promotion type/value/cap applies to final metered pre-promotion fare; dynamic commission uses final pre-promotion fare; cancellation terms are booking snapshots with free cancellation plus review when missing/invalid; and trusted time is billed by exact fractional minutes with no invented duration. REL-03 selects marketing version `1.4.1` for both apps. | Implement and re-run the fare regressions. Read the highest private iOS/Android build numbers before choosing strictly higher build codes; then rebuild and prove signed installed artifacts. |
| 2026-07-18 | Role-account isolation decisions | Owner approved BR-51 and BR-52: one private phone-auth identity may anchor three role accounts; sessions may coexist; every API/resource remains JWT-role scoped; deleting one role leaves siblings; loyalty, referrals, and emergency contacts are separate per role. | Finish role threading/IDOR fixes, design a non-lossy migration for existing shared rows, add role-local deletion/purge, and prove client↔driver↔artisan cross-access denial before release. Do not assign legacy shared rewards/contacts by assumption. |
| 2026-07-18 | Exact-tree admin regression gate | The current admin candidate passes 6/6 vehicle-lifecycle and 12/12 exact-role/security contracts, `tsc --noEmit`, `git diff --check`, and the full Next.js 15.5.19 production build of all 46 routes. ESLint exits successfully with 0 errors and 98 warnings; warning debt is recorded and was not misreported as clean. | Keep P0 open for browser/backend IDOR E2E, real Coordinator→Regional Manager journeys, signed private-document access, legacy-data remediation, audit evidence, and deployment canaries. |
| 2026-07-18 | Admin batch-payout containment | Audit found the admin still advertised a next 18:00 GMT batch, retries, and a `Force Run` action even though the backend deliberately rejects forced aggregate payout and release flags disable scheduled/retry runs. The candidate now presents batch history as read-only, removes the force action and misleading schedule, and clearly states that scheduled, retry, and forced aggregate payouts are suspended while earnings remain preserved. Vehicle tests 6/6, TypeScript, diff check, and a fresh 46-route production build pass after the change. | Keep backend flags `FF_BATCH_PAYOUTS=false` and `FF_PROVIDER_AGGREGATE_PAYOUTS=false`; do not reintroduce an action until immutable earning membership, durable claims, reconciliation, policy approval, and end-to-end financial tests exist. |
| 2026-07-18 | OTP concurrency, carrier-failure, and client-state containment | Audit proved separate issuance/resend operations could send different concurrently valid codes or duplicate paid resends, valid OTP consumption used non-atomic `GET` then `DEL`, failed attempts were not bounded, provider outages had no deployment-wide breaker, payout-method OTP bypassed the containment service, Arkesel could return HTTP success without an accepted recipient/message ID, and 503 response details were discarded by mobile. Local follow-ups now atomically reserve issuance/resend and payout issuance, reset auxiliary state with each stored code, bound wrong attempts, compare-and-delete single-use codes, fail closed on Redis authority loss, handle concurrent/out-of-order provider outcomes with one fenced half-open probe, strictly validate Arkesel acceptance, correlate callbacks with an expiring per-attempt capability, and keep explicitly active delayed codes usable in auth and payout screens. Auth plus payout passes 14 suites/248 tests; API client 103/103, focused client/provider/shared auth OTP 7/7, 3/3, and 1/1, and provider payout OTP 4/4 pass; API/config typecheck and diff check pass. | Keep P0 open: complete independent review and full integrated gates after media settles; prove Lua and callback race behavior on real Redis plus Arkesel; configure the private two-number allowlist/caps/HTTPS callback; prove circuit/kill-switch/cost alerts and installed-device behavior; link accepted/delivered/verified telemetry; resolve provider-login's uniform no-code failure recovery; and disable or repair the separate legacy notification callback before staging sends resume. |
| 2026-07-18 | OTP vendor capability review | Current provider documentation confirms message-level delivery visibility for Arkesel, mNotify, and Hubtel, but no independently comparable Ghana-network p95/p99 handset-delivery evidence was found. Hubtel and mNotify are the immediate SMS benchmark candidates alongside the already implemented WhatsApp path; Africa's Talking remains conditional on verified Ghana transactional routing and commercial access. No vendor traffic, credentials, account, or purchase was used. | Do not call any vendor “fastest” yet. Obtain explicit authorization/credentials, sender approval, and capped spend; run the same instrumented two-number MTN/Telecel/AT matrix, then select primary/fallback using measured acceptance-to-delivery and verification data. |
| 2026-07-18 | Reported signup redirect regression | Client signup now reconstructs and validates the redirected E.164 value, including longest-prefix country-code matching, so Create Account is no longer left disabled after an unregistered-number redirect. The redirected-signup plus client OTP recovery subset passes 8/8 and fatal analyzer checks are clean. | Keep BUG-05 open until installed Android/iOS cold/warm/deep-link staging journeys, telemetry, and rollback evidence pass. |
| 2026-07-18 | Go Online error-message containment | The provider availability controller now maps known machine codes to stable, actionable copy and distinguishes network/server failures while suppressing arbitrary backend text. In particular, a server-side verification mismatch tells an apparently approved provider to refresh Documents & Verification and contact support if every item remains approved. Focused availability/message tests pass 6/6 and target analysis is clean. | Keep BUG-04 and BUG-02 open until the backend returns exact eligibility reason codes, all app/admin error surfaces are inventoried, installed staging journeys pass, and support-safe correlation/telemetry exists. |
| 2026-07-18 | Document lifecycle mobile alert | Provider mobile now recognizes all four backend expiry/replacement lifecycle event types, supplies actionable fallback copy, and derives the only corrective tap destination from a known event allowlist (`/account/documents`) rather than trusting an arbitrary remote route. Focused mapping/routing tests pass 2/2 and provider analysis is clean. | Keep BR-41 open until the backend guarantees a durable rate-limit-exempt in-app record, expiry during every active-trip/upload/admin path preserves completion, the post-trip Offline fence runs, and locked/background physical-device alerts are proven. |
| 2026-07-18 | Active-trip expiry and vehicle lifecycle decisions | Owner approved BR-41: an accepted trip survives driver/selected-vehicle evidence expiry, while new offers stop immediately, a persistent actionable alert identifies the remediation, and the driver is forced Offline after completion. Owner approved BR-42: drivers create and may edit vehicles only before approval; an approved vehicle is driver-immutable and admin-editable; drivers request removal but only an authorized admin retires it. Owner approved BR-43: every vehicle needs coordinator approval and Regional Manager final approval before selection. BR-44 makes an admin edit a pending immutable version that repeats the two-person review while the old approved version remains authoritative; safety blocking is a separate explicit audited action. BR-45 forbids the same normalized plate across driver accounts, BR-46 assigns categories per vehicle, and BR-47 requires distinct Coordinator→RM category review. | Finish and independently test every expiry/upload/admin path, post-trip Offline fence, matching exclusion, mobile alert, immutable vehicle state transitions, two-person approval/version/category switching, requested admin retirement, plate races, category isolation, and accepted-ride vehicle/category snapshots. Decide exact safety-block authority and legacy remediation before the vehicle cutover. |
| 2026-07-18 | Per-vehicle mobile/admin lifecycle integration | Provider mobile now uses strict vehicle CRUD/version contracts, makes approved vehicles driver-immutable, selects roadworthiness and insurance independently by vehicle ID, fails closed on unknown lifecycle responses, and allowlists corrective notification routes. Admin now has a vehicle queue/detail flow for Coordinator→RM decisions, pending revisions, driver-requested retirement, and separately versioned per-vehicle categories; reachable legacy driver-category controls were removed. Evidence: API vehicle/document 7/7, provider focused 25/25 plus auth 5/5, full provider/API-client analysis clean, provider Android release appbundle built, admin vehicle 6/6, TypeScript/build pass, lint 0 errors, and both diff checks clean. | Keep P0 open until the combined backend vehicle migrations/routes pass full exact-tree gates, BR-47 reviewer authority is enforced, legacy records are remediated, accepted rides snapshot vehicle/category, and installed staging journeys prove two vehicles never leak eligibility or documents. |
| 2026-07-18 | Mobile role-name isolation | The shared API-client model no longer falls back from an absent client/driver/artisan legal name to the top-level phone-auth identity. It uses only that role's legal/display/business name or a neutral `Client`/`Driver`/`Artisan` label. Two focused regressions inject `Shared Human Name` and prove it is never surfaced; the complete API-client package passes 114/114 and package analysis reports no issues. | Re-run the full mobile gate after the backend account contract stabilizes, ensure `/users/me` returns the role-account ID rather than private auth ID, and prove chat/call/socket payloads use public role IDs on installed clients. |
| 2026-07-18 | Active-trip fallback decision | Owner approved BR-53: exhaust eligible idle drivers first; then an active driver may receive one queued next ride only within ten minutes of current completion and when the new pickup is within two kilometres of the current final drop-off. Current work finishes first, adjusted ETA is shown, and normal receipt/decision timing applies. | Implement the database-authoritative capacity/distance/time predicates, deterministic idle-exhaustion proof, one-queued-ride invariant, sequential offer flow, ETA disclosure, and race/load/device regressions before enabling this exception. Until then matching must continue to fail closed on active drivers. |
| 2026-07-18 | Promotion subsidy and role-safe payment settlement | The local payment candidate now keeps provider earnings at final pre-promotion fare minus snapshotted commission, records platform-funded promo/loyalty subsidy separately, bypasses Paystack for a fully subsidized zero client charge, and reconciles cash subsidy against commission without changing provider economics. Database checks enforce both sides of the ledger; retries, cancellation, explicit zero-value, cash, tip, insufficient-balance, earnings, and role-specific external identity paths are covered. Full payment evidence: 21/21 suites and 587/587 tests; focused payment/job/migration 127/127; API/database typechecks, Prisma generate/validate, and diff check pass. | Keep the gate open until BR-54's exact-duration implementation passes, the subsidy migration applies/retries on isolated PostgreSQL, the combined full API passes, and staging Paystack-stub/live-safe reconciliation plus crash races pass. |
| 2026-07-18 | Mobile role-owned rewards/contact containment | Client parsing now distinguishes an omitted, unassignable loyalty balance from an explicit role-owned zero; the loyalty screen shows a secure-review state for quarantined legacy ownership, referral failures no longer become a fabricated zero history, and emergency-contact ownership failures warn the user not to rely on in-app alerts until support review. Focused API-client role/DTO tests pass 5/5, client profile regressions pass 4/4, and both package analyzers report no issues. | Keep BR-52/P0 open until the final backend role-account schema and endpoint contract land, single-role legacy rows are deterministically migrated, ambiguous multi-role rows follow an owner-approved quarantine/manual-resolution policy, safety behavior is approved, and installed cross-role IDOR journeys pass. |
| 2026-07-18 | BR-53 backend fallback candidate and independent review | The combined local backend contains a fallback candidate that exhausts idle rings first; requires an `in_progress` predecessor, fresh backend ETA no greater than ten minutes, pickup within two kilometres of final drop-off, exact matching eligibility, one successor per driver/predecessor, and the normal receipt/decision timing. Independent review found unsafe migration ordering/flag seeding plus mobile predecessor loss/navigation interruption, missing client queue state, non-durable activation, no activation-time eligibility recheck, stranded cancellation paths, ordinary queued fees/penalties, coordinate/predecessor leakage, unfenced ETA leases, and unbounded route-call cost. The migration follow-up now preflights before mutation, is retry-aware around concurrent indexes, forcibly fences the flag false twice, and verifies OFF/index postflight; contracts pass 11/11. | Keep it out of the emergency release until isolated apply/failure/operator-resolve/retry proof. Keep `ride_active_trip_fallback_enabled=false` and do not enable BR-53. Close every remaining P0/P1 and owner policy, add explicit queued mobile states, then prove process/Redis/network/device/load failures before a later controlled activation. |
| 2026-07-18 | Exact-role session recovery/admin candidate and failed adversarial review | Mobile now sends an exact role; new rows persist it; admin responses no longer intentionally aggregate sibling profiles; and revoke checks role plus private phone. Initial migration/auth/backend-admin/admin-web gates pass. Independent review nevertheless proved that an old request can revoke a newer session because no target SID is stored, scoped admins have nationwide access, public callers can exhaust a victim's quota, dismiss can overwrite revoke, external revocation is not atomic, legacy rows leak inferred session metadata, mobile overstates that support was notified, private Ghana Card keys are not signed here, and old apps are incompatible. | Keep the recovery path NO-GO. Implement SID-bound compare-and-revoke, concurrency/outbox, legacy redaction, signed document access, challenge-bound filing, truthful copy, and compatibility tests. Obtain explicit admin-scope, shared-phone visibility, and notification-channel decisions before enabling or staging it. |
| 2026-07-18 | Provider cleanup/assignment race containment | Driver zombie cleanup now shares the acceptance row lock and rechecks active rides. Artisan cleanup shares the artisan lock with bid confirmation; selection uses stable artisan→job→bid order and revalidates active role, verification/RM approval, online reachability, capacity, job ownership/status, bid ownership/status, and directed assignment. Focused provider/location/marketplace race evidence passes 4 suites/145 tests. | Production vehicle/API cutover remains NO-GO until the PII-free production preflight is run and every legacy driver lacking explicit vehicle, selection, approval/category, or vehicle-bound roadworthiness/insurance is remediated. Stop booking, matching, and old offer workers during the migration fence; never infer legacy approval. |
| 2026-07-18 | Combined backend regression, migration, and schema-drift refresh | From the current combined local tree, all **171 API suites / 3,499 tests** pass. API/E2E and database typecheck, API/database build, Prisma generation/validation, changed-file lint with zero errors, and all repository `git diff --check` gates pass. All **108 migrations** apply in order on fresh disposable PostgreSQL and seed completes. Every modelable migration/schema difference is reconciled; the exact drift guard passes **6/6** and allows only the intentional PostGIS GIST index. The E2E harness proves test cron jobs are unmounted and has four consecutive **50/50** passes, but one earlier restore-online 403 and one earlier bid-selection 404 were transient and remain staging watch items. This includes vehicle/role migrations, distinct two-hour payout and 24-hour dispute clocks, verified BR-62 repayment-before-clawback settlement, cumulative retained-amount economics, single-owner welfare safety jobs, deployment-leased provider liveness, ride V2/BR-53-off, OTP/media, exact-role sockets/admin/session recovery-off, provider cleanup races, authoritative offer-time eligibility, and client exit fencing. | This is local evidence only. Run the PII-free legacy preflight and production-shaped clone rehearsal; reproduce or rule out both transient E2E failures; run real Redis/object-store/carrier/payment fault tests; execute the drift guard against the isolated release database; deploy one pinned staging SHA; and repeat cross-role/device/load/rollback gates before production. Migration 263000 must precede the API because boot migrations are disabled. |
| 2026-07-18 | Payout/webhook containment re-audit | Source inspection confirms default-off batch and aggregate payout fences, exclusive pending-transfer leases, deterministic verify-before-retry, CAS-safe payment/payout sweepers, durable raw-body-verified webhook receipt before acknowledgement, OTP-only destination binding with exact recipient snapshot, and deterministic tip/reversal reconciliation. Fresh focused evidence passes **12 suites / 471 tests**; the full 171-suite API gate and 108-migration rehearsal also pass. BR-62 now extends this with verified client refund destinations and repayment-before-clawback ordering. | This closes local implementation gaps only. Keep batch/aggregate flags false, then prove real PostgreSQL concurrency plus Paystack/Arkesel webhook, retry, crash, reversal, backlog, cash-refund, and reconciliation behavior on one pinned staging SHA. |
| 2026-07-18 | BR-17/BR-62 dispute clock, cash-refund destination, and partial-refund economics | The local candidate admits ride disputes for exactly 24 hours from `ride.completedAt`; both artisan dispute admission and the two-hour artisan payout hold start when the client confirms completion at `clientConfirmedCompleteAt`, and both stored clocks are immutable after capture. Original payment economics remain immutable, cumulative partial refunds recompute commission and provider entitlement from retained gross using the booking's snapshotted rate, pre-payout settlement reduces only unpaid entitlement, and post-payout clawback recovers only actual provider overpayment. BR-62 now has an end-to-end local implementation: client-entered MoMo number/network, explicit SMS/WhatsApp OTP, immutable encrypted destination plus masked read model, deterministic verify-before-send cash transfer, leased retries, durable failure/reconciliation states, and provider clawback only inside the authoritative refund-success transaction. An original-payment HTTP timeout never resubmits and remains eligible for a late terminal webhook. The mobile dispute screens now call the real ride/job APIs and can resume the exact existing dispute verification flow; admin uses the matching camelCase contract, blocks cash approval before verification, and never labels a pending refund as complete. Fresh evidence: focused backend **154/154**, API-client **3/3**, full backend **171/3,499**, mobile **326/326**, admin build plus **18/18** contracts, **108 migrations**, and four consecutive **50/50** E2E passes. | Run the PII-free preflight on authorized staging/production-shaped data; reconcile real Paystack transfer/refund success, failure, timeout, duplicate webhook, reversal, and clawback canaries; prove Arkesel/WhatsApp OTP delivery/resend; reproduce or rule out the two earlier transient E2E failures; and verify notification/rollback/monitoring on one pinned staging SHA. |
| 2026-07-18 | Final BR-62 webhook-routing authority | A later audit proved that sequential partial refunds can share one original Paystack transaction reference. The candidate now makes Paystack refund object IDs database-unique after refusing legacy duplicates, and transaction-reference-only webhook routing refuses two-or-more historical candidates instead of selecting the newest. Unknown submission outcomes remain non-terminal and never resubmit or create a clawback; exact late webhook outcomes remain authoritative. Focused BR-62 passes **156/156**, the full API passes **171/3,504**, all **109 migrations** plus seed replay, and the lifecycle gate has five consecutive **50/50** passes. | Run the duplicate-ID preflight on authorized staging data and prove exact-ID, missing-ID, duplicate/replayed, delayed, failed, reversed, and timeout Paystack events on the pinned artifact before release. |
| 2026-07-18 | E2E HTTP lifecycle stability correction | The seemingly unrelated restore-online 403, bid-selection 404, and escalation `socket hang up` occurred while Supertest repeatedly auto-opened and auto-closed a shared unbound Nest HTTP server. Both lifecycle suites now bind one ephemeral loopback listener during bootstrap and close it only at suite teardown; application routes and assertions were not weakened. E2E typecheck passes, artisan-only repeats pass **140/140**, ride-only repeats pass **110/110**, and five full two-suite runs pass **250/250** on fresh 109-migration/seed environments. | Preserve this harness behavior in the reviewed release change and repeat the same gate on the exact pinned staging commit. This is local transport-harness evidence, not proof of real socket/push/carrier behavior or production readiness. |
| 2026-07-18 | Provider-liveness 100k-row query and lease correction | A disposable PostgreSQL 16/PostGIS database with 50,000 synthetic drivers plus 50,000 synthetic artisans showed the ID-first stale scan touching roughly **18k–22k shared buffers** and taking **5–15 ms** for a 2,000-row page. The worker now keysets on `(COALESCE(last_location_at, '-infinity'), id)` over the matching partial online-provider index. The same fixture used about **2k buffers** and completed in **under 1 ms**. It also owner-renews the deployment lease at every bounded stage and fails closed before provider mutation or cursor commit after ownership loss. A real worker scenario was added to E2E; all 109 migrations replay, API **171/3,507**, E2E **51/51**, five full repeats **255/255**, typechecks, builds, Prisma validation, lint, and diff checks pass. | This is a database microbenchmark, not 100k-DAU certification. Repeat `EXPLAIN (ANALYZE, BUFFERS)` on the production-shaped clone and prove multi-replica renewal, Redis loss, a chunk stalled beyond the lease, worker crash, DB pool/event-loop budgets, full endpoint traffic, and soak behavior before closing the capacity P0. |
| 2026-07-18 | Welfare-check single-owner safety fix | Removed the second registered scheduler and routed both response APIs to the canonical marketplace service. A preflight-first partial unique index permits only one unresolved check per job across replicas; per-check notification dedupe prevents repeated artisan/admin records; admin alerts are durably fanned out before stamping; and data-only pushes now contain the welfare-check ID. Focused 71/71, API typecheck/build, changed-file lint, full 171 suites/3,499 tests, and fresh 108-migration plus four consecutive 50/50 E2E proofs pass. | Local evidence only. Preflight legacy duplicates without PII, then prove concurrent replicas, scheduler crash/catch-up, failed push recovery, admin scope, and operator resolution on pinned staging. |
| 2026-07-18 | Current-tree container/secret boundary | `Dockerfile.api` builds successfully from a 21.15 MB filtered context into a 213,890,502-byte image (`sha256:8a3d4d6…`). Direct runtime inspection proves `myshop`/UID 1001, no checked Git/env/test/docs or credential-pattern files outside dependencies, and correct refusal of Render boot migrations and an unspecified geofence guard. | Pin the reviewed source SHA and Node base digest; remove runtime source/dev dependencies; produce SBOM/vulnerability/license evidence; start against isolated `rediss://` plus PostgreSQL and pass geofence/health/SIGTERM/rollback checks before staging deployment. |
| 2026-07-18 | Exact-tree mobile regression refresh | `melos run test` passes all tests in all six test-bearing packages on the current mobile tree. `melos run analyze` passes all seven packages with fatal infos enabled and no issues; `git diff --check` passes. This includes exact-role recovery payloads, chat role-account identity, role-owned rewards containment, OTP delay handling, durable booking recovery, vehicle/document authority, mandatory update, media containment, and request receipt behavior. | Signed Android/iOS artifacts, private store build-number reconciliation, old/new installed-build proof, queued BR-53 UX, and physical-device background/locked/reconnect/permission/OTP/media journeys remain open. |
| 2026-07-18 | BR-54 exact-duration implementation | Ride completion now prices trusted duration as exact seconds converted to fractional minutes, applies no invented duration/time charge when authority is absent, and relies on the configured minimum fare for the floor. Persisted duration is rounded only to the schema's two decimal places, so 20 seconds records as 0.33 minute without pricing it as a whole minute. Focused fare/status evidence passes 81/81 and the final 164-suite API plus 50-test E2E gates pass. | Prove real metered completion, admin completion, missing-duration, retry, and receipt output against isolated staging before closure. |
| 2026-07-18 | BR-59 authoritative Offline and fresh vehicle selection | Backend Offline transitions clear `activeVehicleId`; an Offline driver cannot Go Online without an explicit eligible vehicle; the provider UI opens a fresh unselected vehicle picker on every Go Online; switching is rejected while Online. The mobile Offline action now uses the no-GPS authoritative endpoint, waits for success, and cannot silently skip the server transition when cached coordinates are absent. API-client availability/media tests pass 12/12, focused provider Offline-state tests pass 2/2, backend availability tests pass 16/16, and the final full backend/mobile gates pass. | Add installed-device active-offer/active-work/network-loss tests, run the PII-free legacy preflight, manually remediate every affected driver, and prove two-vehicle isolation on staging. |
| 2026-07-18 | BR-61 media consumer containment | Exact confirmed-upload provenance is enforced locally for client profile photos, client Ghana Card images, job photos, dispute evidence, support attachments, and privileged provider-document uploads. Ghana Card authority is purpose-separated; general chat/portfolio writers are closed; support/dispute writers default OFF; stored bytes, magic, MIME, size, version, privacy, SHA-256, exact owner role, and signed admin reads are checked. The final focused media/support/dispute/storage stream passes 69/69, the admin-provider stream passes 362/362, API typecheck/build pass, and the full 161-suite backend plus full mobile gates pass. | Keep support/dispute and privileged provider upload flags OFF until isolated real S3/Cloudinary policy/private-GET/delete proof, deployed-flag verification, legacy public-object inventory/remediation, crash-orphan cleanup, retention/quota, and malware/PDF scanning decisions and evidence pass. |
| 2026-07-18 | Client booking/logout and socket authority | A durable exact-client SID plus monotonic generation now serializes ride/job creation against logout, self/admin delete, and emergency force logout. Login, refresh, and the HTTP guard fail closed; generation zero is one-time rollout only. Chat, call, and location gateways require exact role-account ID plus current SID at sensitive event boundaries and revoke all namespaces without touching sibling roles. PostgreSQL 18 proved migration apply/retry and both race orders; focused socket/account tests and the full 161-suite backend pass. | Apply 263000 before the API, then prove migration ordering, Redis-loss cleanup, installed socket reconnect/takeover, multi-replica revocation, and deployment canaries. Decide durable provider generations versus a one-time forced provider OTP reauthentication for legacy refresh tokens. |
| 2026-07-18 | Artisan dispatch freshness and capacity | Artisan GPS capture time/accuracy are persisted monotonically; matching requires <=30-second, <=50-metre fixes, active role notification reachability, independent approved Ghana Card plus exactly one approved trade credential, RM online stage, category, and capacity. Solo is hard-capped at one; non-solo cannot exceed three; manual directed assignment shares the same lock/capacity authority. The migrations have 5/5 contracts and PostgreSQL 18 apply/retry, exact-constraint/index, invalid-write, and unsafe-legacy-refusal proof. | Preflight real legacy rows without PII and confirm whether non-solo may be configured 1–3 or only 2–3, which pending/scheduled states reserve slots, and scheduled reservation timing. Complete server-owned location session/sequence and lifecycle transition authority before staging. |
| 2026-07-18 | Driver/artisan KNN dispatch no-starvation and scale correction | The authoritative driver and artisan queries now order through the geography GiST KNN operator `<->` after every eligibility predicate instead of sorting all eligible rows with `ST_Distance`. Artisan active-job capacity moved into the SQL predicate before `LIMIT`, removing two per-candidate N+1 reads and preventing nearer full shops from hiding eligible providers. On disposable PostgreSQL 16/PostGIS, the 100,000-driver fixture improved from about **36,000 buffers / 89.7 ms** to **472 buffers / 6.5 ms**. With **100,000 artisans, 200,000 approved documents, 100,000 category/device rows, and 100,000 historical jobs**, the old artisan path used about **2.06 million buffer accesses / 1,001 ms** and selected ten full providers; the corrected path used **313 buffers / 8.1 ms**, skipped those ten, and returned eligible ranks 11–13. A permanent real-database E2E regression recreates the same nearest-full pattern. Fresh exact-current evidence: API **174/3,523**, focused SQL/matching/bid **113/113**, artisan E2E **29/29**, all 109 migrations plus seed, and five full ride/artisan runs **275/275**; typechecks, builds, Prisma/schema guards, lint with zero errors, and diff checks pass. Geography `<->` reports spherical metres; radius eligibility remains exact `ST_DWithin`, and no business threshold changed. | Repeat both exact plans against an authorized production-shaped clone and the pinned staging artifact with realistic rides/offers/jobs/categories, prepared statements, pool concurrency, multi-replica dispatch, socket/push delay, saturation, and soak. The local query proof closes the observed SQL starvation/per-call hot path, not the 100k-DAU capacity or delivery-loss P0. |
| 2026-07-19 | Bounded health/readiness and graceful shutdown correction | Added a dependency-free liveness endpoint, concurrent DB/Redis readiness probes capped at 100–10,000 ms with separate documented 3-second/2-second defaults, explicit draining readiness failure, Nest signal hooks, bounded Socket.IO Redis pub/sub shutdown, and Render readiness routing. A real built-API process on disposable PostgreSQL/Redis returned HTTP 200 from `/v1/health/live` and `/v1/health/ready`, then exited after SIGTERM with explicit Redis and PostgreSQL disconnects. Exact-current full API **175/3,533** and five fresh 109-migration E2E runs **280/280** pass. | Repeat on the pinned staging service while holding an in-flight HTTP request and sockets across two replicas; verify Render stops routing before termination, another replica owns worker leases, and repeated deploys leave no DB/Redis/socket connections. Topology/monitoring/DR P1s remain open. |
| 2026-07-19 | Shared Redis HTTP rate-limit authority | Replaced per-process Nest HTTP counters with an atomic Redis-time sliding window. Default **300/minute** and auth **20/minute** limits are unchanged; hits, block state, and reset are shared across replicas, cluster keys share one hash tag, and Redis errors or malformed authority responses return bounded `RATE_LIMIT_AUTHORITY_UNAVAILABLE` 503 responses instead of silently admitting traffic. Focused storage evidence passes **5/5**; a real Redis E2E alternates the same identity across two storage instances and proves the combined third hit blocks both until expiry. Exact-current API **175/3,533**, build, API/E2E typechecks, lint with zero errors, all 109 migrations, one **56/56** E2E run, and five repeats **280/280** pass. | Repeat with two real staging replicas under correct E.164/IP/user identities and inject Redis delay, loss, restart, and eviction pressure. Alert separately on authority failure. This does not close the topology or production Redis P1s. |
| 2026-07-19 | Shared WebSocket admission and polling-affinity removal | Replaced three per-process gateway counters with one role-account-scoped Redis lease authority shared across location, chat, calls, and every API replica. Admission atomically prunes expired members and claims the last slot; one bounded batched heartbeat renews local leases; disconnect removes immediately; a process crash recovers within the configured 90-second bound; two consecutive renewal-authority failures disconnect before expiry. Redis keys hash the role-account ID and share one cluster slot. Every supported mobile/admin socket client already requests `websocket`, and all three server gateways now reject Engine.IO polling. Focused limiter/transport/gateway evidence passes **99/99**. Real Redis E2E proves cross-replica/cross-namespace cap, over-cap rejection, release, and crashed-replica reclamation. Exact-current API **175/3,533**, API/E2E/config typechecks, API/config build, lint with zero errors, 109 migrations, and five runs **280/280** pass. | Repeat on two pinned staging replicas with real Socket.IO clients and exact role sessions; inject Redis delay/loss/restart/eviction, replica death, reconnect storms, max-cap churn, and deploy drain. Measure lease renewal ops, Redis memory/latency, dropped/rejected sockets, request delivery, and recovery. Production still requires same-region no-eviction Redis and capacity/alert evidence. |
| 2026-07-19 | Protected PII-safe runtime telemetry | Added a pinned Prometheus client and a default-off `/v1/metrics` endpoint that requires a startup-validated random bearer token of at least 32 characters. It reports bounded-label HTTP RED/in-flight, DB/Redis readiness, HTTP/WS admission, local socket lease, default Node saturation, scrape, and sanitized build metrics; framework route templates replace raw URLs and UUID/long-ID labels fail to `/unmatched`. Metrics remain available during maintenance only through their independent bearer check. Focused metrics/health/interceptor/admission evidence passes **62/62**. Real HTTP E2E proves disabled 404, unauthorized 401, correct exposition content type, authorized scrape, and no fixture phone/account identifiers; full API **175/3,540**, all 112 migrations, **56/56** E2E, and five repeats **280/280** pass. | Configure a private external scraper on the pinned staging artifact; prove multi-replica aggregation, retention, dashboards, numeric alerts and owners. Add DB-pool/Redis-server, queue/cron/outbox, payment and business-invariant instrumentation, mobile/admin telemetry, synthetic journeys, and timed DR/restore evidence before closing monitoring P1. |
| 2026-07-19 | Complete pending populated-table online-DDL correction | Every one of the **65** pending indexes on pre-existing populated tables is now a manifest-controlled standalone one-statement `CREATE [UNIQUE] INDEX CONCURRENTLY`; the four required legacy removals are standalone `DROP INDEX CONCURRENTLY IF EXISTS`. Ordinary builds remain only on tables created inside the pending rollout. Source migrations no longer perform invalid-index cleanup, ordinary drops, explicit `LOCK TABLE`, or `ACCESS EXCLUSIVE`; dependent postflights run after their online builds and verify exact name/readiness/validity/table binding. Pinned Prisma 7.6 applied all **183 migrations** twice from empty PostgreSQL. The permanent proof observed the expected online lock for every build and drop, admitted an ordinary writer within 750 ms, verified final catalogs, and proved invalid build remnants fail closed until invalid-only recovery. Exact-current evidence passes **176/176 API suites, 3,697/3,697 tests**, schema-drift guard **6/6**, deploy guard **12/12**, actual drift verification, API/E2E/database/config typechecks, five-package build, Prisma validation, lint with zero errors, diff check, **56/56** E2E, and five complete runs **280/280**. | Run all PII-free preflights and measure data-volume/backfill/constraint-validation runtime on an authorized production-shaped clone; rehearse invalid-remnant resolve/retry and the BR-38 stopped-old-worker sequence on the exact reviewed staging artifact. No production execution or downtime approval is implied. |
| 2026-07-19 | BR-38 database-final booking gate and zero-mutation refusal proof | The booking migration now installs a `BEFORE INSERT` PostgreSQL gate after forcing `rides_enabled=false`; it accepts only explicit JSON boolean/string true, fails every other or missing state with SQLSTATE `23514` and `RIDE_BOOKING_DISABLED`, and the API converts that race outcome to the existing stable `503 RIDE_BOOKING_PAUSED` response. A permanent local-only PostgreSQL proof creates a real online driver/session/token/approved vehicle, accepted ride, and delivering offer; closes the gate; proves a direct new ride insert is refused; executes the exact online-session and provider-vehicle source migrations; proves both refuse active work; and verifies the complete sentinel state is unchanged after each rollback. The proof is now part of the default E2E gate. Fresh evidence: **76/76** focused ride/contract tests, **176/176 API suites and 3,697/3,697 tests**, two empty-database **183-migration** runs, **56/56** E2E, and five repeats **280/280**; API/database typechecks, lint with zero errors, formatting, and diff checks pass. | Keep rides closed. Repeat the PII-free preflights, direct gate/API behavior, timing, old-worker shutdown, Redis cache fence, capability reset, exact migration chain, and rollback on an authorized production-shaped clone and the pinned staging artifact; then prove installed old/new devices before any reopening. |
| 2026-07-19 | Release-preflight read-only and privacy containment | All seven production operator preflights now enable immediate psql failure, disable paging, and execute inside PostgreSQL `REPEATABLE READ READ ONLY` transactions with two-minute statement and five-second lock timeouts. Raw role-account, driver, ride, plate, and payment identifiers/fingerprints were removed; results are aggregate-only. A permanent local-only proof runs the exact scripts against `myshop_test`, rejects mutations and identifier-shaped output, and verifies that no exact sensitive value stored across role, provider, vehicle, payment, device, or media fields is printed. Static coverage passes **7/7**; the default E2E gate and five repeats pass, and exact-current API evidence is **176/176 suites and 3,697/3,697 tests**. | Execute the exact reviewed preflights on the authorized production-shaped clone and pinned staging artifact, retain only aggregate output in restricted release evidence, reconcile every non-zero count through an approved operator procedure, and never paste row-level PII into tickets or this checklist. |
| 2026-07-19 | Provider Online boundary and GPS parity | Closed three locally reproducible eligibility gaps: artisan direct Online and cached driver/artisan Online now enforce the same current service boundary as driver direct Online; artisan direct Online also rejects invalid, future, older-than-30-second, or worse-than-50-metre fixes before mutation; and idle driver Offline no longer fails merely because the provider is outside the boundary. Provider UI maps the two GPS failures to specific actionable copy. Real PostgreSQL/PostGIS E2E proves outside-boundary artisan refusal returns `OUTSIDE_PILOT_REGION`, preserves location and availability, and still permits idle Offline. Exact-current evidence: focused backend **72/72**, full API **176/3,705**, provider **110/110**, full mobile **327/327**, one fresh **58/58** E2E lifecycle, and five complete repeats **290/290**; API/E2E typechecks, backend lint, mobile analysis, and both diff checks pass. | Approve/install the exact production boundary and border policy; replace first-active-region resolution with coordinate-based ownership; then repeat inside/outside/border and cached-boundary-change journeys on the exact pinned staging artifact and installed provider builds. |
| 2026-07-19 | Monotonic provider location and current-session authority | PostgreSQL/PostGIS driver and artisan helpers now lock the provider row and apply only a strictly newer captured time. Direct, socket, and background driver paths and direct artisan paths stop before Redis GEO, liveness, degradation recovery, trail, eligibility, or broadcast effects when a fix loses the ordering race. Driver socket updates require the exact current Online epoch; direct existing-driver writes prove a durable session before mutation and avoid a duplicate heartbeat transaction; existing-artisan heartbeats can validate but never activate Online state. Offline ignores GPS. A real database test proves an older artisan update returns `locationAccepted:false` with PostGIS location, timestamp, availability, and Redis heartbeat unchanged. Provider copy safely explains `GPS_FIX_OUT_OF_ORDER`. Exact-current evidence: focused location/session **144/144**, full API **176/176 suites and 3,716/3,716 tests**, provider **110/110**, API/E2E/database typechecks and builds, API lint with zero errors, one **59/59** E2E run, five repeats **295/295**, and both diff checks pass. | Add explicit server-owned Online epochs/sample idempotency to REST batches and artisan ingestion; enforce the approved lifecycle-transition GPS gate; then prove cross-replica ordering, device clock skew, background retry, logout/re-online, lock/termination, Redis/network failure, and load on the pinned staging artifact and installed builds. |
| 2026-07-19 | BR-30 server-owned driver/artisan location-session completion | Added one server-owned epoch and monotonic sequence authority for both provider roles across Go Online, reconciliation, direct, socket, and background-batch location paths. A PostgreSQL row lock atomically accepts only the exact current epoch, a higher sequence, and a newer capture time; duplicate/old packets have zero Redis, liveness, degradation, trail, eligibility, or broadcast effects. Offline clears all authority. The new migration refuses before DDL if any provider is Online, and the real cutover proof verifies PostgreSQL `P0001` plus zero mutation. Exact-tree evidence: backend **177/177 suites and 3,728/3,728 tests**; all **184 migrations** applied twice from empty PostgreSQL; schema drift, seed, online-index, read-only-preflight, and cutover-refusal gates pass; lifecycle E2E **61/61** and five repeats **305/305**; mobile **337/337** (provider **117**, API client **131**, client **51**, shared UI **25**, shared models **7**, overlay **6**); typechecks, builds, analysis, lint, and both diff checks pass. | Before migration, drain both roles fully Offline and prove zero aggregate Online counts. Keep production blocked until the exact pinned staging artifact and installed builds prove logout/re-online, duplicate/old epoch, clock skew, background/locked/terminated retry, invalid-token, multi-replica, Redis/network failure, lifecycle-transition GPS, and load behavior. |
| 2026-07-19 | BR-30 lifecycle-transition GPS authority | Ride arrival/start/completion and artisan arrival/start/mark-complete now require a device fix no older than **15 seconds** and with accuracy no worse than **30 metres**. The API rejects missing, stale, inaccurate, or invalid fixes with stable codes before mutation; a single proof cannot skip multiple checkpoints; each accepted checkpoint appends its location to the trail; and same-status idempotent replay remains safe. Mobile reuses only a qualifying cached fix, otherwise requests high-accuracy location with a bounded timeout and sends the proof. Exact-current evidence: backend **178/178 suites and 3,744/3,744 tests**, mobile **346/346** (provider **122**, API client **135**, client **51**, shared UI **25**, shared models **7**, overlay **6**), API/E2E typechecks, API build, all-package analysis, lint with zero errors, and both diff checks pass. All **184 migrations**, drift, seed, read-only preflight, cutover-refusal, and online-index gates pass; E2E passes **63/63** and five repeats **315/315**. | Approve exact ride/job geofence radii and override audit/evidence behavior without changing the approved GPS freshness/accuracy limits. Prove the transitions on installed foreground/background/locked devices, two replicas, clock skew, Redis/network failure, and the pinned staging artifact. |
| 2026-07-19 | BR-31 invalid notification-token provider fencing | Permanent FCM invalid-token results now deactivate only the exact failed token, role, user, and expected FCM value. When it is the final eligible token for the current role/session, the backend forces only that provider role Offline, clears session/location authority plus Redis presence/heartbeat, and emits exact-role `availability:forced_offline`; client/sibling role tokens remain isolated and any active ride/job remains finishable. Device registration and invalidation share a user-row lock, so a stale Firebase failure cannot deactivate a newly registered replacement token. Real PostgreSQL/Redis E2E invalidates the last driver token during an in-progress ride and the last artisan token during an active job, proves both work items finish, and proves provider presence is cleared. The same exact-current evidence is **178/3,744**, mobile **346/346**, E2E **63/63** and five repeats **315/315**, with builds/typechecks/analysis/lint/diff gates passing. | Define provider-token maximum age/rotation policy and full artisan receipt assurance; then prove replacement-token, multi-device, background/locked/terminated, transient Firebase failure, multi-replica, Redis-loss, and user-copy behavior on the exact staging artifact and installed builds. |
| 2026-07-19 | BR-32 exact-role relaunch recovery | The provider app now persists a prior Online intent only under the exact driver/artisan role-account ID after successful explicit Go Online. Process-death recovery waits for Firebase readiness, requires the retained selected driver vehicle, obtains a fresh BR-30 fix, and calls the ordinary availability endpoint so server-side RM/document/vehicle/category/notification/location eligibility is revalidated before Online is restored. Recovery never silently prompts for notification/location permission or Android overlay Settings. Role/account mismatch fails closed; active work remains delegated to ride/job recovery. Failed revalidation consumes the intent, attempts authoritative Offline, and presents stable actionable copy derived from machine reason codes rather than raw backend prose. Current exact mobile evidence is **361/361** (provider **135**, API client **137**, client **51**, shared UI **25**, shared models **7**, overlay **6**), all-package analysis with fatal infos, and `git diff --check`. | Prove process death/relaunch, Firebase delay, retained/missing vehicle, stale/inaccurate/denied location, expired/unapproved evidence, RM rejection, role coexistence, active work, network/Offline-confirmation failure, and no-automatic-prompt behavior on installed Android/iOS builds against the exact pinned staging artifact. |
| 2026-07-19 | Exact-session recovery filing containment | Replaced the public phone+role+device filing claim with a random 256-bit, five-minute, one-time capability issued only from the exact active-session conflict while recovery is explicitly enabled. Redis stores only the capability hash and atomically consumes it before phone quota or database access; the payload binds phone hash, user, exact role account, target SID, and requesting device. Invalid, expired, replayed, mismatched, changed-session, and authority-failure paths fail closed. Both apps retain the capability only in memory and hide Contact support when none was issued. Full evidence at this checkpoint: backend **178/3,747**, mobile **361/361**, real **64/64** E2E and five repeats **320/320**, API/E2E typechecks, all-package analysis, lint with zero errors, and both diff checks. | The later durable resolving-saga checkpoint closes the local crash-consistency implementation gap. Keep `FF_SESSION_RECOVERY=false` until the exact integrated staging artifact passes multi-replica fault, installed-app, operator, and audit proof. |
| 2026-07-19 | Durable exact-role sessions and crash-consistent recovery saga | Added durable active auth SID/generation authority for client, driver, and artisan; role login/refresh/guard/exit/recovery now fail closed against PostgreSQL even when Redis cleanup is delayed. Recovery approval commits the exact-role fence plus a stable leased `resolving` operation before external cleanup; bounded retry, `SKIP LOCKED` reconciliation, safe error state, replacement-SID rejection, final durable-fence proof, one final audit, idempotent resolved replay, and purge exclusion close the local crash window. The admin exposes `resolving`, operation, attempt, retry, and safe error state. Real E2E crashes after the fence, proves the old token receives `SESSION_TAKEN_OVER` while its Redis SID still exists, resumes cleanup, and proves one audit after replay. Exact-current evidence: backend **179/179 suites and 3,760/3,760 tests**; migration compatibility **380/380**; API/E2E typechecks, build, lint **0 errors / 56 warnings**, and backend/admin diff checks pass; admin TypeScript plus **18/18** role/vehicle contracts pass. All **187 migrations**, drift, seed, read-only preflight, cutover-refusal, and **66-build/4-drop** online-index safety gates passed on five complete fresh stacks; E2E passes **65/65** per stack and **325/325** total. One separate shared-database repeat-harness attempt hit a local macOS Node 24/pnpm `SIGSEGV` on its second invocation; no application assertion failed, and five independent fresh-stack runs passed. | Keep `FF_SESSION_RECOVERY=false`. Re-run on the pinned Node 22 Linux release artifact and prove two-replica crash after each phase, Redis delay/loss/restart, lease takeover, operator visibility, exact-role installed-app behavior, audit/alert delivery, and rollback before activation. Production remains **NO-GO**. |
| 2026-07-19 | OTP Redis scale, privacy, and cluster containment | Replaced host-clock tumbling paid-send counters with atomic Redis-time sliding windows, hashed every phone-keyed auth/legacy OTP key, made auth/delivery/provider-circuit/payout/refund Lua key sets Redis Cluster slot-safe, and made malformed Redis responses fail closed. The exact-current real Redis E2E proves per-phone and global quota scope, physical-key phone privacy, distributed circuit open/half-open/close, resend cooldown, three-attempt invalidation, and one-winner concurrent correct verification. Full evidence passes **179/179 suites and 3,762/3,762 tests**, API/E2E typechecks, build, lint **0 errors / 56 warnings**, diff check, all **187 migrations**, drift, seed, preflight, cutover, and online-index safety on five independent fresh PostgreSQL/Redis stacks; E2E passes **68/68** each and **340/340** total. Paid delivery remains default-off. | Keep the live-channel P0 open until staging and production cap values are owner-approved, the two tester numbers are privately allowlisted, and one pinned staging artifact proves Arkesel/WhatsApp acceptance and authenticated callbacks, handset-delivery/verification telemetry, cost alerts, circuit/kill switch, installed-device fallback, credential rotation, and immediate disable. Do not place phone numbers or secrets in this checklist. |
| 2026-07-19 | BUG-07 cancellation and lifecycle false-success containment | Client ride and service-job cancellation now clears local state only after a successful write or authoritative `cancelled` read-back; timeout-after-commit is recovered, while rejected/unknown outcomes preserve the active work. Driver cancellation follows the same rule. Driver and artisan lifecycle controls require an exact acknowledgement or a read-back at/after the requested checkpoint instead of inventing a local status from malformed data. Artisan cash completion also requires authoritative `completed` state. Stable copy hides raw backend errors. Focused cancellation/lifecycle evidence passes **22/22**; after the subsequent error/payment authority slice, the exact mobile tree passes **394/394** tests (client 68, provider 145, API client 143, shared UI 25, shared models 7, overlay 6), all seven analyzers, and diff check. | Prove timeout-after-commit, authoritative rejection, failed read-back, delayed/lost socket, duplicate tap, app lock/background/termination, and recovery on installed Android/iOS builds against one pinned staging SHA before closing the operational P0. |
| 2026-07-19 | BUG-04/BUG-09 stable errors, support correlation, log containment, and payment authority | Mobile added shared safe copy, removed raw API/gateway prose and audited payload logs, removed ride/job self-confirmed payment shortcuts, requires authoritative payment/job read-back, rejects malformed payout success, and never fabricates references or coordinates. Backend now generates one opaque support UUID per HTTP request, returns it in header/envelope, correlates route-template/value-free logs, ignores inbound request IDs, hides unexpected exceptions, and retains only safe exception kinds across provider/infrastructure/service logs; the new privacy contract prevents regression. Admin discards backend prose, uses stable code/status copy, forwards only validated support references, uses codes for recovery/dispatch decisions, and removes raw carrier/body/URL/runtime output from SMS, PDF, analytics, verification, and proxy paths. Evidence: mobile **394/394** and all analyzers; backend **180/180 suites, 3,767/3,767 tests**, build/typecheck/lint/diff; admin **26/26 contracts**, TypeScript/build, lint **0 errors / 98 warnings**, diff. | Local code containment is closed, but production remains blocked until the exact pinned staging artifact proves reference continuity and safe copy across browser/apps, installed payment/payout and error journeys, live-safe carrier/gateway canaries, external log-sink access/retention/redaction, and signed Android/iOS artifact evidence. |

| 2026-07-19 | Post-correlation fresh-stack regression | A new disposable PostgreSQL/Redis stack applied all **187 migrations**, passed schema drift, seed, read-only preflight, cutover-refusal, and online-index safety, then passed the exact-current **68/68** API lifecycle E2E suite after the support-reference, error-envelope, and log-containment changes. | Repeat this proof on the pinned Node 22 Linux staging artifact and complete installed-device, carrier, payment, multi-replica, load/soak, rollback, and observability evidence. |
| 2026-07-19 | Release-bound load harness and seventh exact-current stack | Replaced the stale false-pass load suite with a fail-closed non-production runner, URL+commit SHA-256 target binding, unique server-issued exact-role fixture validation, live target authentication of every fixture, current driver/artisan Online epoch/sequence GPS, one-booking-per-client idempotency/recovery, exact driver GPS+Socket.IO receipt/decline sessions, current artisan and Paystack inbox flows, clone-only destructive scenarios, and explicit USSD-stub refusal. Unsafe JWT signing and direct database seeders were removed. Artisan job/bid presets now maintain one monotonic GPS stream per exact Online artisan instead of allowing eligibility to stale during the run. The evidence report and staging runbook require an approved traffic model, private results, metrics/alerts, abort ownership, multi-replica faults, reconciliation, rollback, and 2x headroom. Contracts pass **8/8**; pinned k6 1.3.0 compile-inspects all seven supported entrypoints with networking disabled; API passes **182/182 suites and 3,777/3,777 tests**, all typechecks/builds, lint with **0 errors / 56 warnings**; and a seventh fresh stack passes all 187 migrations plus **68/68** E2E (**476/476** across seven). | Capacity remains unproved. Build/review a target-local fixture provisioner, approve the 100k-DAU traffic model and abort thresholds, deploy one pinned production-shaped clone with at least two replicas and external metrics, then execute load/soak/fault/rollback/reconciliation evidence. Decide whether USSD is v1 or fully disabled/deferred with corrected release/legal claims. |
| 2026-07-19 | Shared private auth-identity fallback containment | Removed shared `users.full_name`/`users.email` fallback from payment/refund, ride snapshot/dispute, marketplace, notification/email, admin job/ride/map/activity/emergency/report/transaction, and clawback paths. Names and email now come only from the exact role profile; missing names use a neutral role label. Active admin detail responses no longer nest or serialize the private auth entity, artisan search/clawbacks omit its ID, and admin consumes the direct exact-role response shapes. Regressions inject a conflicting `SHARED AUTH NAME`; a source contract prevents shared joins from returning. Evidence: focused backend **9/9 suites, 586/586 tests**; full backend **182/182 suites, 3,777/3,777 tests**; API typecheck; admin TypeScript; **27/27** admin contracts; production admin build. | This local P0 is closed. Keep production NO-GO until the separate account-ownership migrations/IDOR/delete-purge gates, exact reviewed artifact, deployed staging, installed-app, and operational evidence pass. |
| 2026-07-19 | Exact-role exit dispute-liability correction | Client and provider role deletion now prefers immutable `payment.disputeEligibleUntil` and uses only the approved legacy 24-hour fallbacks: `ride.completedAt + 24 hours` or `artisanJob.clientConfirmedCompleteAt + 24 hours`. It no longer shortens deletion protection to a mutable two-hour config value or starts artisan protection from `job.completedAt`. Focused exit proof passes **24/24**; the full backend passes **182/182 suites and 3,777/3,777 tests** plus API typecheck. Exact-role exit tests also prove only the selected role is soft-deleted and the private auth root plus sibling roles remain untouched. | Local code is closed. Keep the release gate open for production-shaped legacy-data/preflight, cross-role IDOR, pinned staging deletion/dispute timing, and implementation/proof of the subsequently approved BR-63/BR-64 contracts. |
| 2026-07-19 | Database-final immutable ride-pricing contract | Verified that active V2 application paths freeze booking-time pricing/cancellation/completion policy, then closed the remaining direct-writer gap: the database gate now rejects protocol-0, incomplete booking identity, missing/corrupt policy snapshots, and invalid surge even after bookings reopen. Added a schema-aware, aggregate-only legacy-policy preflight to the permanent read-only/privacy gate and release runbook. A fresh disposable PostgreSQL/Redis stack applied all **188 migrations**, passed drift, seed, **8/8** preflights, reopened invalid-writer refusal, pre-cutover legacy-state zero-mutation cutover refusal, online-index safety, and lifecycle E2E **68/68**. Focused pricing/migration proof passes **96/96**; full API passes **183/183 suites and 3,783/3,783 tests**, API typecheck, and Prisma validation. | Local P0 code is closed. Run the exact preflight and cutover rehearsal on the reviewed production-shaped clone and pinned staging artifact; reconcile legacy rides only from authoritative booking-time evidence, never current mutable rates. Production remains **NO-GO**. |
| 2026-07-19 | Pinned Node 22 production-only runtime artifact | Aligned source/build/deploy/runtime on Node **22.23.0**, pinned the exact base digest, replaced the whole-builder copy with a production-only runtime allowlist, removed runtime `npx`, and added source contracts plus a reusable image verifier. The final local image (`sha256:8fb88d38e8f65778e309ddaf5689c021f6abc3b8dd08ab70293381cde719f64e`, 181,187,690 bytes) passes non-root/root-ownership, filesystem/dependency containment, bundled Prisma/`tsx`, 188-migration seed, TLS Redis, geofence/prefix, readiness, and SIGTERM checks. Its SPDX SBOM has 828 packages and the full pinned-Linux regression passes **184/184 suites and 3,787/3,787 tests**. | Build the identical artifact from one reviewed clean SHA, retain signed provenance and a formally reviewed SBOM/license/vulnerability result, deploy that digest to staging with a pinned version/commit marker, then complete multi-replica, rollback, observability, external-dependency, and installed-device gates. Production remains **NO-GO**. |
| 2026-07-19 | Exact-session notification registration/logout fence | Closed the guard-to-write race for FCM, VoIP, ActivityKit push-to-start, and ActivityKit update-token registration. Every writer now locks and revalidates the exact durable client/driver/artisan role SID at its final transaction boundary against the same user+role rows used by account exit. Logout-first produces stable `ROLE_SESSION_CHANGED` with no token reactivation; registration-first is subsequently removed or deactivated by logout; sibling roles are not inspected or changed. Focused notification evidence passes **51/51**; API and E2E typechecks pass; the full pinned Node 22 regression passes **184/184 suites and 3,794/3,794 tests**; and a fresh 188-migration PostgreSQL/Redis stack passes **70/70** E2E scenarios including both real lock-order races. | Local code is closed. Before production, prove delayed mobile registration, logout, role coexistence, locked/background/terminated devices, multi-replica behavior, Redis/network failure, and invalid-token replacement on one reviewed pinned staging artifact. |
| 2026-07-19 | Three-repository staging-package inventory | Refetched all three `origin/staging` branches. Backend and admin worktrees are based exactly on their current staging SHAs. Mobile HEAD is one merge commit behind by graph, but `git diff HEAD origin/staging` is empty, so its committed content already matches staging; a new release branch must nevertheless start at `f88f43c…` to avoid misleading PR history. No unresolved merge paths, changed binary files, changed files larger than 500 KB, or high-confidence secret-pattern additions were found. Added a repository ignore for nested Gradle caches. Confirmed the backend deletions replace the duplicate safety welfare service with the marketplace authority and remove unsafe JWT/direct-DB load seeders; the admin create-user dialog has no remaining references. One untracked, unreferenced NestJS file under mobile `apps/api/` is outside the mobile architecture and is explicitly excluded pending provenance review. | Freeze all three trees; never use blanket `git add .`; package exact reviewed paths onto branches created from the recorded staging SHAs; remove or intentionally relocate the excluded mobile file; rerun every exact-tree gate after commit; and record clean commit SHAs before any deploy. |
| 2026-07-19 | Deferred USSD runtime and release-claim containment | Found the production Blueprint explicitly enabling an always-public static USSD menu whose service still lists every real operation as TODO. The candidate now defaults and deploys `FF_USSD_CHANNEL=false`, rejects enabling the stub in production configuration, accepts only exact `true` at the controller boundary, returns stable 404 without invoking it, removes `SkipThrottle`, excludes the controller and tag from Swagger, and removes phone/session fragments from its log. The first real E2E exposed that ConfigService returned raw `"false"` and a truthiness check reopened the route; exact-value parsing closes that bypass. The Blueprint also pins every other deferred P0 surface false, with a **17/17** source contract preventing stale dashboard values from silently reopening them. Evidence: focused USSD **3/3**, full pinned Node 22 **186/186 suites and 3,814/3,814 tests**, API/E2E typechecks, config and production API builds, diff check, and a fresh PostgreSQL/Redis stack applying all **188 migrations**, drift, seed, and **71/71** E2E. | Keep the flag false everywhere. Owner/legal still must decide defer-versus-future scope and correct seeded terms, Acceptable Use, changelog, project-status, store, and support claims before production. Enabling USSD requires a complete separately reviewed implementation; the stub is not a feature. |
| 2026-07-19 | Deleted-role recovery and 90-day retention truth containment | Audited self-delete, admin delete, suspension reinstatement, dormant recovery, purge, mobile copy, and technical documentation. Exact-role deletion fences work/sessions/devices, now stores `recoveryDeadline=deletedAt+2,160 hours`, retains the private auth root and sibling roles, and reports recovery unavailable until the reviewed workflow exists. The obsolete public controller route and the dormant environment-toggleable phone-only service method are removed; the Blueprint/environment no longer expose their toggle; lifecycle E2E proves `/v1/auth/recover` is 404; and suspension reinstatement cannot revive deleted rows. Registration now returns stable `ROLE_ACCOUNT_RETAINED` before OTP persistence/delivery, and both apps map it to safe recovery copy plus a support action. The purge cron remains deliberately fail-closed. Current delta evidence: backend **4/4 suites and 243/243 tests**, API typecheck, API-client **13/13**, client signup **2/2**, provider auth **3/3**, all affected analyzers, and both diff checks pass. The most recent pre-delta pinned Node 22 baseline passed **186/186 suites and 3,815/3,815 tests**. BR-63 now fixes the exact clock and recovery principles; BR-64 approves high-level deletion, retention/hold, storage, backup, and final-root disposition. | Keep recovery unavailable until the exact client/provider reviewer chain is confirmed and the full OTP request/review workflow passes PostgreSQL, cross-role, admin, mobile, and staging evidence. Keep purge disabled until record-class retention periods, hold release authority, object/notification deletion proof, backup expiry SLAs, and corrected legal terms pass review. Then rerun the broad pinned gates. Production remains **NO-GO**. |
| 2026-07-19 | BR-63 database deadline and BR-64 purge policy approval | Added migration `20260719020000_enforce_role_recovery_deadline`: it deterministically repairs only role lifecycle metadata and database-enforces `recoveryDeadline = deletedAt + 2,160 hours` for deleted client/driver/artisan rows while active rows have no deadline. Added a ninth bounded, database-read-only, aggregate-only preflight that reports invalid deadlines and overdue inventory without enabling purge. BR-64 now authorizes a minimal non-personal tombstone, selective erasure, six-year minimum transaction/accounting retention, 24-hour object deletion, seven-day verification, 30-day backup expiry, and purge reapplication after restore. Exact-current evidence: Node 22 backend **187/187 suites, 3,821/3,821 tests**; focused BR-63 set **7/7 and 270/270**; typechecks/build/Prisma/lint/diff pass; a fresh stack applies all **189 migrations**, passes drift/seed/nine preflights/cutover/index safety, and passes **72/72** E2E. Mobile passes **402/402**, all seven analyzers, and diff check. | Keep user recovery unavailable until its exact reviewer chain and complete OTP/admin/mobile workflow are confirmed and proved. Keep automatic purge disabled until hold-release governance, exact non-tax post-closure periods, implementation, legal terms/review, storage/backups, and production-shaped staging evidence pass. Production remains **NO-GO**. |
| 2026-07-19 | BR-63 exact deleted-role recovery implementation and BR-64 purge containment | Implemented a feature-gated OTP workflow bound to exact user, role account, role, region/category, deletion/deadline, device hash, and stable request key. Client approval is global Operations/Super Admin only. Provider intake is restricted to a named Admin in the provider region, then ordinary exact-category Coordinator review and RM final approval; intake resets documents, sessions, location, and vehicle authority and keeps the provider Offline. No recovery reject/dismiss action exists. Database constraints/triggers reject false bindings and invalid transitions; the tenth aggregate preflight reports inconsistent state without PII. Both apps expose a working recovery dialog from retained-role registration, preserve a request key across lost-response retry, use safe copy, and explicitly preserve siblings. Admin keeps deleted-role recovery distinct from device-session recovery. The deletion response now derives `recoveryEnabled` from the same exact fail-closed feature gate instead of stale hard-coded copy. Exact evidence: pinned Node 22.23.0 backend **190/190 suites and 3,861/3,861 tests**; API/E2E/database typechecks, production build, Prisma and lint; fresh **190-migration** stack plus **74/74 E2E**; mobile **413/413** plus seven analyzers; admin **29/29**, TypeScript and 47-route build. `FF_ROLE_ACCOUNT_RECOVERY` and `NEXT_PUBLIC_FF_ROLE_ACCOUNT_RECOVERY` default false. | Package reviewed exact commits and execute the controlled staging OTP/client/provider/IDOR/expiry/lost-response/notification chain before exposure. The approved deadline governs user filing and named regional Admin intake only; timely intake may complete Coordinator/RM review later while the provider stays Offline until RM approval. Keep automatic purge disabled: hold release requires the responsible Legal/Safety owner plus a different Operations/Super Admin, and Legal still must set exact post-closure non-tax retention periods. Production remains **NO-GO**. |
| 2026-07-19 | Support-channel, visible-version, and recovery-deadline truth | Removed placeholder/malformed client/provider support phone and WhatsApp destinations; the shared contact sheet now fails closed unless WhatsApp is exact E.164 digits. Both help-article ticket actions navigate to the correct form and preserve `TicketCategory.other`. Client support identity is consistent across recovery, support UI and ride/job PDF receipts; provider support identity is centralized separately. The approved `1.4.1` marketing version is passed to visible UI by the same release resolver as `--build-name`, replacing stale `1.0.0` labels. The owner also approved that filing plus regional Admin intake must beat the 2,160-hour deadline, after which Coordinator/RM review may finish with the provider Offline and without auto-expiry. Exact-current client **73/73**, provider **150/150**, shared UI **30/30**, all affected fatal-info analyzers and the release-version contract pass. | Confirm both mailboxes have named monitored owners; prove email/ticket actions and rendered PDF receipts on installed builds; verify the highest private build numbers and signed artifacts. Do not enable WhatsApp/call support until a real destination and operating owner are approved and device-tested. |
| 2026-07-19 | Exact mandatory-update store authority and listing-truth audit | Verified all four official public listings against the exact Android package and iOS bundle identities. Backend configuration now accepts only those canonical URLs and refuses a one-platform-only minimum-build activation that could strand legacy devices without platform headers. The approved `1.4.1` exceeds public Android client/provider `1.3.11`, iOS client `1.3.9`, and iOS provider `1.4.0`; private console build numbers remain unknown. Current public descriptions contain release-risk claims about payout timing, verification SLA/credentials, emergency gesture, localization/rewards, and support that are not all true under the approved v1 containment. Exact evidence: guard **17/17**, config/API typechecks, pinned Node 22.23.0 backend **190/190 suites and 3,863/3,863 tests**, five-package build, and all repository diff checks. | Correct and approve both store descriptions, read the four private build-number maxima, restore iOS distribution signing, build/install the four artifacts, then activate both platform minimums for each app together only during the rehearsed BR-38 cutover. |
| 2026-07-19 | Runtime-authority cleanup and live legal verification | Removed three unused environment-schema/example values that contradicted approved runtime authority: commission no longer advertises a hard-coded 20% environment fallback, the decision window no longer advertises 15/30 seconds instead of the immutable 45 seconds after receipt, and a two-hour dispute environment value can no longer be confused with the immutable 24-hour dispute boundary or separate two-hour payout hold. Config build, API typecheck and diff checks pass; the already-tested runtime continues to use database-owned commission snapshots, the 45-second protocol constant and the 24-hour boundary. Public production reads proved the three audience Terms are live as version `1.0.0`, have no effective date, are marked `DRAFT`, and contain materially stale commission, gateway, dispute, payout and USSD claims; Acceptable Use is also a live no-date draft with USSD claims, and Community Guidelines lacks an effective date. The candidate now makes source publication explicit/default-off, requires effective non-placeholder content, rejects same-version mutation, and adds an eleven-script aggregate preflight set. Focused legal/preflight contracts pass **14/14** and the new SQL correctly reports fourteen check outcomes on migrated local PostgreSQL. | Use the factual [legal correction brief](legal-v1.4.1-release-correction-brief.md), prepare new approved versions, add effective dates, implement versioned consent/re-consent, publish and verify all required audiences, and require zero legal-preflight violations. Do not defer this while stale documents remain public. |
| 2026-07-19 | Exact local staging-candidate manifest | Pinned byte-identical mobile staging-tree proof plus exact backend/admin staging bases, deterministic source fingerprints, candidate counts, the stray mobile `apps/api/` exclusion, secret/artifact screening, fail-closed staging feature set, local test evidence and ordered packaging/acceptance procedure in `docs/staging-release-candidate-manifest.md`. All three fingerprints reproduced after documentation updates and all three repository diff checks pass. | This is a local snapshot, not a deployable artifact. Create reviewed branches from the pinned bases without blanket staging, record clean commit SHAs, rerun the gates on those commits, then deploy and execute the staging/device/fault runbook. Production remains **NO-GO**. |
| 2026-07-19 | Truthful fail-closed SOS and emergency-number correction | Auditing the emergency path found that the API always requested recording, marked `police_called=true` after merely asking mobile to open a dialer, treated configured contacts as notified, and sent exact GPS plus booking/role identifiers through an FCM topic. A client ride-screen SOS also bypassed the approved three-second hold, two quick-dial buttons were inert, stale 193/192 numbers were shown, and UI copy claimed recording was active. The candidate now routes every client SOS entry through the hold screen; opens only the external dialer; uses Police 191 and Ambulance/Fire 112, verified against current official Ghana Police, National Ambulance Service and Ghana National Fire Service pages; records no confirmed call; counts only exact SMS-provider acceptance without calling it handset delivery; sends a generic topic wake-up with no GPS/booking/role ID; and defaults recording OFF with production enablement rejected. Focused backend safety/push/SMS/config evidence passes **6/6 suites and 165/165 tests**; the pinned full backend gate passes **193/193 and 3,892/3,892**; the fresh **190-migration** lifecycle gate passes **75/75** including a real PostGIS SOS insert and recording refusal; mobile passes **416/416** plus all analyzers. | Local code is closed. Keep recording OFF. Before production, prove the three-second hold, dialer handoff, 191/112 targets, admin authenticated queue/ack, real Arkesel accepted-versus-delivered telemetry, and failure copy on installed Android/iOS builds and the exact pinned staging artifact. Do not claim that MyShop called police, delivered a contact SMS, or recorded audio/video. |
| 2026-07-19 | Deferred-email and recovery-authority containment | The email service previously enabled whenever SES credentials existed unless the flag was exact `false`, so retained dashboard credentials could silently reopen an unreviewed channel. Its weekly template also promised daily 18:00 payouts even though batch payouts are suspended. The environment schema and example separately retained an unused 24-hour soft-delete recovery setting that contradicted the database-final 90×24-hour role-recovery rule. Email is now exact-opt-in, pinned false, production-forbidden for this release and covered by the Blueprint contract; the payout promise is removed; the obsolete recovery setting is removed and regression-protected; deferred recording routes are excluded from Swagger. Exact evidence passes **5 focused suites / 92 tests**, API/config builds and typecheck, pinned Node 22.23.0 **193/193 suites / 3,892/3,892 tests**, and the fresh 190-migration **75/75** lifecycle gate. | Keep email OFF. A later release must approve truthful templates, sender/reply-to identities, SES callback/security configuration, suppression/retention policy and operating owners before production activation. The 2,160-hour database deadline remains the only role-account recovery clock. |
| 2026-07-19 | Account-deletion and recovery-copy containment | The final mobile truth audit found both apps implying ordinary recovery availability while the release flags keep self-service deleted-role recovery OFF. The provider deletion screen also exposed fake fixed work/rating counts, an unapproved Police Clearance/fee claim, a false permanent-history claim, and an invented seven-year Ghana retention claim. Both apps now describe exact-role deactivation, sibling isolation, 90-day retention, support-assisted phone verification and the approved client/provider reviewer chains without claiming automatic recovery. Provider consequences now use only the approved access, pending-review, retained-record, legal-hold, unsettled-payment and clawback rules. The shared API client no longer calls soft deletion permanent, and the March PRD/EDD are visibly marked non-authoritative with their stale 24-hour recovery text corrected. Focused client/provider/API-client tests, all three affected analyzers, and the complete **416/416** six-package mobile gate pass. | Keep self-service deleted-role recovery OFF for the emergency release unless the separately pinned staging/admin/OTP workflow is deliberately enabled and proved. Keep permanent purge OFF; support must not promise restoration timing or completion while the automated workflow is disabled. |
| 2026-07-19 | Verification, payout and dispute-claim containment | The deadline truth sweep removed unapproved automated police/background-check, instant-payout, funds-arrived/released, fixed three-hour completion, 48-hour dispute, 24/7 support and single-role-per-phone claims from client/provider release UI. Manual independent document decisions plus Coordinator→RM authority remain the only v1 verification claim. The dormant police callback is now exact-opt-in, excluded from Swagger and stable 404 unless `POLICE_CHECK_ENABLED=true`; malformed values stay closed. Payment screens identify server-owned settlement, the approved two-hour payout hold and 24-hour dispute clock without inventing provider transfer success. Exact-current evidence: pinned Node 22.23.0 backend **193/193 suites and 3,905/3,905 tests**, API typecheck, five-package build and diff check; a disposable stack applies all **190 migrations**, passes drift/seed/all eleven preflights/cutover/index checks and **75/75** lifecycle E2E; mobile passes **418/418** (client 77, provider 151, API client 147, shared UI 30, shared models 7, overlay 6), affected analyzers and diff check. | Keep automated police/Smile verification and aggregate payout automation OFF. Package reviewed commits, then prove manual verification, earnings/payout status and dispute timing on installed staging builds. Production remains **NO-GO**. |
| 2026-07-19 | Fail-closed post-hold payout cutover and outbound WhatsApp opt-in | The deployment audit found `FF_INSTANT_PAYOUTS` absent from the production Blueprint and configuration schema. Missing/false/malformed activation made a released payment fall back to the legacy direct-transfer path, bypassing the durable per-payment outbox; `.env.example` also claimed fixed 20%/80%, seconds-level delivery and batch fallback. The candidate now retains every payment when the durable outbox is not explicitly enabled, retains pre-cutover payments for exact manual reconciliation, makes no gateway call in either case, and requires a valid cutover timestamp or exact legacy opt-in. The Blueprint requires a private dashboard decision rather than silently enabling it. General WhatsApp notifications are exact-opt-in and production-pinned false, so retained Meta credentials cannot activate an outbound channel; this does not alter the separately allowlisted OTP path. Misleading fixed-split/timing/fallback comments are removed. Evidence: focused **3 suites / 122 tests**, config/API typechecks, five-package build, lint 0 errors/56 warnings, exact-current pinned Node 22.23.0 **193/193 suites / 3,905/3,905 tests**, and a fresh all-190-migration/all-11-preflight/**75/75** lifecycle gate. | Before ordinary provider settlement is exposed, choose the exact deployment cutover timestamp, privately set `FF_INSTANT_PAYOUTS=true` plus `INSTANT_TRANSFERS_ENABLED_AT`, prove hold→outbox→Paystack verification/reversal/reconciliation on pinned staging, and inventory every older retained payment for exact manual handling. Keep general WhatsApp notifications false; the two-developer OTP exception still requires its allowlist, caps, callbacks and cost telemetry. Production remains **NO-GO**. |
| 2026-07-19 | BR-66/67 provider document replacement authority | Provider upload and confirmation now re-check the current approved logical document using server/database time. A still-valid approved document remains closed; rejected or expired evidence reopens, with expiry occurring only after the full printed GMT date. Approved non-expiring evidence and approved provider profile photos stay provider-locked, with the latter separately admin-only. Status responses expose authoritative expiry/replacement state so handset clock skew cannot reopen a document. The provider app removes the pre-expiry upload action and shows truthful notice copy. Focused verification passes **69/69** and focused mobile document/API-client proof passes **34/34**. | Local business-rule implementation is closed. Keep privileged admin writers fail-closed until private-store/IDOR proof; then prove valid, rejected, expiry-boundary, clock-skew and active-trip continuity on the exact deployed staging artifact. |
| 2026-07-19 | BR-68/69/70 release containment and exact-current gate | Added durable document reminders at upload confirmation, seven days, 72 hours, 24 hours, two hours and invalidity; SMS is limited to the final two stages and non-OTP SMS is exact-opt-in. New scheduled artisan work is default-off while accepted commitments remain manageable. SOS accepts every authenticated non-deleted role with optional GPS/booking and exact-role active-booking validation when supplied; Admin renders absent booking context safely. The final exact-current gate passes pinned Node 22.23.0 **195/195 suites and 3,949/3,949 tests**, a fresh **192-migration** stack with all preflights/cutover/index checks and **75/75** E2E, mobile **428/428** plus seven analyzers, Admin **29/29** plus TypeScript/47-route build, and production runtime image verification for `sha256:3a807696ce4238328b2a874a1a72470a018c128aee86f5bf2d95ba18f9a68457`. | Local automated proof is closed. Keep scheduled jobs, general SMS/WhatsApp, recording and other deferred surfaces OFF. Reviewed commits, exact staging deployment, installed devices, live-safe carrier/payment canaries, legal correction, Redis durability, rollback and monitored rollout remain release gates. |
| 2026-07-19 | Remaining P0/P1 reconciliation and emergency release cut | Re-read the unchecked findings against the exact current source instead of carrying obsolete baseline defects forward. Replacement upload atomicity, inclusive-GMT expiry, independent review, exact-role isolation, vehicle ownership/selection, location degradation, media authority and promo settlement are now marked locally closed where later source/tests supersede the historical finding. BR-53, aggregate payouts, promo/rewards and Smile remain future P0/P1 conditions but are explicitly non-blocking only while their database/environment authorities stay false. Added the short non-deferable release register and legal-version-consent gap. Focused Blueprint/BR-53/legal/authority contracts pass **34/34**; all stale-public legal source files remain `publish: false`. | Production remains NO-GO. The remaining release cut is reviewed exact commits, corrected approved legal versions and exact-version consent, Frankfurt no-eviction Redis, reviewed staging/preflight deployment, signed build numbers/artifacts, installed-device core journeys, live carrier/payment canaries, monitoring/rollback and bounded capacity evidence. |
| 2026-07-19 | Deferred notification log-privacy closure | General SMS, WhatsApp and email paths no longer log user/notification identifiers, full or masked destinations, template variables, provider message IDs, callback IDs or raw provider errors. Channel/event/template/status, safe error class and aggregate counts remain available for operations, and a production-log contract prevents reintroduction. Focused **5-suite / 141-test** proof, API typecheck, changed-file lint and diff check pass; the exact pinned backend gate is **195/195 suites / 3,949/3,949 tests**. The exact-current 192-migration/**75/75** E2E gate and production runtime verifier also pass. | Keep all three general channels OFF for this release. Before later enablement, approve retention/access/callback policy and prove provider telemetry without personal data in the deployed log sink. |
| 2026-07-19 | Exact legal-consent authority audit | Verified the full registration chain. Client signup has no explicit consent control and unconditionally submits `privacyPolicyAccepted=true`; provider signup requires a checkbox but discards the accepted document identities and later submits the same boolean. Redis carries only an initiation timestamp, each role profile stores only `privacyPolicyAcceptedAt`, no acceptance ledger exists, and the Admin legal editor can mutate a published version in place. Therefore neither the document shown nor the Terms/Privacy version accepted can be proved, and a version string is not immutable evidence. | P0 remains open. Product must approve the exact required-document set and cutover/re-consent behavior. Then add immutable role-scoped acceptance records, exact-current/effective validation, explicit client consent, authenticated re-consent for existing roles, published-version immutability, stale-consent admission gates, migration/preflight/E2E/mobile/Admin proof, and corrected approved documents. Do not backfill a version from timestamp-only legacy data. |
| 2026-07-19 | Exact legal-consent implementation and lifecycle closure | Owner approved separate exact Terms/Privacy acceptance per role, AUP incorporated in Terms, hard registration cutover, no timestamp backfill, re-consent for legacy roles, active-work continuity, new-work blocking, OTP refresh on version change and immutable publication. Client/provider registration now starts with two unchecked controls and submits exact identities; Redis binds the snapshots to OTP; completion revalidates and writes the ledger transactionally; status/re-consent and server-side booking/matching fences are active. Migration `20260719060000_immutable_role_legal_acceptance` database-enforces role ownership, audience precedence, current/effective identity and immutable evidence. The final-root purge may null only the deleted private identity pointer while preserving every audit field. Exact local and pinned Node 22 evidence passes **199/199 suites, 3,974/3,974 tests**, the production build, all **193 migrations**, drift/seed/preflight/index checks and **75/75 E2E**; the exact-current runtime image passes its repository verifier, mobile remains **428/428** with all analyzers, and Admin passes **29/29** plus its 47-route build. | Local mechanics and automated proof are closed. Production remains blocked on qualified review and publication of the four corrected `1.4.1` rows, zero strict-preflight violations, reviewed commits/fingerprints, exact staging/device proof, Frankfurt Redis and the other non-deferable release gates. |
| 2026-07-19 | Production Redis eviction fail-closed enforcement | The release reconciliation found that `/health/ready` treated any responding Redis as healthy, so the known eviction-enabled production store could discard OTP, admission, session and dispatch coordination keys while remaining ready. `RedisService` now requires `maxmemory-policy=noeviction` at production bootstrap and cached readiness rechecks, bounds and de-duplicates the probe, and sanitizes provider failure. Staging/test retain connectivity checks without silently claiming production durability. Exact local and pinned Node 22 evidence passes **199/199 suites and 3,974/3,974 tests** plus typecheck/build/lint/diff; the **181,259,010-byte** image `sha256:00de4c7e989db3540f41d6ceaad121cf6109923ab20cb4c694383d5fd336c1f3` applies all **193 migrations**, refuses `allkeys-lru`, accepts TLS `noeviction`, reaches readiness and drains cleanly. | Local fail-open behavior is closed. Production remains **NO-GO** until the Cape Town eviction-enabled store is replaced by Frankfurt no-eviction Redis with HA, capacity, backup, alert, failover and exact-prefix evidence; deploying this candidate before that replacement will intentionally fail readiness/startup. |
| 2026-07-19 | Release-critical scheduled-worker replica fencing | Audited all 28 `@Cron` workers and corrected the highest-risk unfenced effects. Active-ride disconnect escalation and stale Paystack verification now use token-owned Redis claims and fail closed without Redis authority. Scheduled-job no-show, bid expiry, job staleness, rating reveal and small clawback write-off use database compare-and-set or one-statement claim semantics. Directed-quote, insufficient-balance and reminder notifications use deterministic durable dedupe. Reconciliation report creation is unique and its discrepancy alert now has a PostgreSQL-leased durable retry outbox. Focused scheduler proof passes **268/268**, payment proof **80/80**, pinned Node 22.23.0 passes **199/199 suites and 3,990/3,990 tests**, typecheck/build/lint/diff pass, and a fresh all-194-migration stack passes every safety check plus **77/77 E2E**. The exact **181,266,020-byte** runtime image `sha256:6a9f3f45997b5c3357a20ac2e06b2dc19637815863398fe76331c5248e827380` passes production Redis eviction refusal, TLS `noeviction`, geofence, readiness and SIGTERM verification. | Local duplicate durable effects and the known reconciliation-alert crash window are contained. Keep production **NO-GO** until the reviewed exact commit is deployed against Frankfurt no-eviction Redis and staging proves two-replica process/Redis/network faults, scheduler scan/database-pool budget, alert visibility, reconciliation and rollback. Repeated candidate scans remain a capacity concern even where effects are claimed. |
| 2026-07-19 | Durable reconciliation discrepancy alert recovery | Migration `20260719070000_reconciliation_alert_outbox` makes every unbalanced report a durable alert outbox with PostgreSQL `FOR UPDATE SKIP LOCKED` claims, a two-minute recoverable lease, attempt/error telemetry and five-minute retries. Authorised-admin fan-out remains permission-scoped and per-admin deduped. Zero recipients or persistence failure releases only the token-owned claim; a crash after fan-out retries without creating another inbox row. Unit proof passes **19/19**. Real PostgreSQL E2E runs two workers concurrently, proves one claim, simulates the post-fan-out crash boundary, proves one inbox row, retains a report with no authorised admin, then delivers it after authority returns. The exact gate is **199 suites / 3,993 tests**, **194 migrations** and **77/77 E2E**. | Local crash consistency is closed. Staging must still prove operator visibility, paging for retained/unalerted reports and attempts, two-replica process termination at each boundary, database pool/scheduler budget, reconciliation runbook ownership and rollback before production. |
| 2026-07-19 | Durable active-ride disconnection escalation consolidation | Removed the remaining log-only admin escalation. After the approved 120-second socket-loss grace, the detector joins the durable BR-33 location-degradation episode, removes new-dispatch presence, persists permission-scoped admin plus exact-role provider/client inbox records, retains its Redis sentinel when no authorised admin exists, and retries safely after process failure. The shared episode timestamp makes the independent stale-GPS worker converge on the same dedupe keys; a database completion fence prevents a second escalation. Active work continues, the legacy client socket event remains for released apps, and unactivated BR-53 queued successors cannot be mistaken for the current ride. Focused location proof passes **3/3 suites and 114/114 tests**; local and pinned Node 22 full gates pass **199/199 suites and 3,993/3,993 tests**; typechecks, five-package build, lint (0 errors/56 existing warnings), diff check, fresh **194-migration / 77/77 E2E** and exact runtime verification pass. Final image: **181,266,517 bytes**, `sha256:8c7a5918678a5a52e69998de9429cfdd895bbf478f32a3580d62d5f59fc2cdd2`. | Local invisible/duplicate alert behavior is closed. Staging still must prove killed-replica/reconnect timing, operator visibility/paging, real background/locked-device recovery, two-replica Redis/network failure, scan/pool budget and rollback. Production remains **NO-GO**. |
| 2026-07-19 | Durable Paystack webhook dead-letter operator visibility | Replaced the remaining release-enabled payment `ADMIN_ALERT` log with migration `20260719080000_webhook_dead_letter_alert_outbox`. Each signature-verified dead-letter row now owns a PostgreSQL `FOR UPDATE SKIP LOCKED` alert lease, token-owned acknowledgement, attempt/error telemetry, immediate claim release on failure and deterministic per-admin event dedupe. Zero recipients remain pending. Global `view_payments` operators receive a privacy-safe inbox alert linked to a new read-only 48th Admin route; the API/UI omit raw bodies, account destinations, Paystack/payment/transfer references and provider error prose, and expose no retry/refund/transfer/payout/reconciliation action. Focused backend proof passes **4 suites / 81 tests**; Admin passes **32/32**, TypeScript, lint with 0 errors/101 existing warnings and its 48-route build. Exact pinned Node 22 passes **201/201 suites and 4,002/4,002 tests**; a fresh stack applies all **195 migrations**, all safety proofs and **79/79 E2E**, including two-replica ownership, post-fan-out crash recovery, dedupe, zero-recipient retention and later delivery. Final exact-commit runtime image: **181,271,488 bytes**, `sha256:16d9b3a088cbe37e5bbf27e2527037e1ec4b0d4f26a332b269cd9466257ded03`. | Local alert loss/privacy/action containment is closed. Staging still must prove the exact operator inbox/link/queue, alert paging and reconciliation ownership under killed-replica/database-pool pressure. No automatic financial reconciliation is approved. Production remains **NO-GO**. |
| 2026-07-19 | Staging-parent packaging and exact-commit verification | Created isolated local candidates directly from the pinned staging bases without changing the original dirty indexes: mobile source `b0d05d0df53a34102c5d38ad442ad0d951d3c7b2`, backend `0c40e30b3602c7da6862d2651f8c83c7f99739c2`, and Admin `f65df4fa8e7ad5400fd70e5c7b1056e9bcaa96e2`. The inventory contains 315 mobile, 629 backend and 55 Admin changed paths; reviewed deletions match the manifest, the stray mobile `apps/api/` tree is excluded, and whitespace/conflict/credential/generated/binary/large-file scans pass. Clean bootstrap exposed and fixed a stale tracked Melos override, after which the mobile checkout stays clean. Exact committed evidence passes mobile **428/428** plus all seven analyzers and the release-version contract; Admin **32/32**, TypeScript, 0-error lint and 48-route build; backend pinned Node 22.23.0 **201/201 suites / 4,002/4,002 tests**, forced five-package build, fresh **195 migrations / 79/79 E2E**, eight load-harness contracts, and runtime verification for the exact image above. | Local packaging and exact-commit automation are closed. No branch was pushed, no PR was opened, and no deployment changed. Obtain second-developer path/flag review, push and merge the exact branches, then run staging preflights, deployment, installed-device and live-safe canaries. Production remains **NO-GO**. |
| 2026-07-22 | Production system-audit deployment recovery | The audit candidate crashed on Neon’s pooled endpoint with PostgreSQL `08P01` because the API supplied custom `options=-c app.environment=production` at connection startup. Hotfix `abe88db…` removes the unsupported option, uses tracked `application_name=myshop-api:<environment>`, and adds migration `20260722010000_neon_pooled_audit_environment` so database-triggered audit rows retain a strict environment label. Focused audit tests pass **5/5**, API typecheck/build and the exact Node 22.23.0 production Docker build pass, and the SQL helper returned `production` against disposable PostgreSQL. PR #114 merged to staging as `9b3776b…`; PR #115 merged to main as `d918243…`. Hosted Actions created no job steps or logs because the paid runner path remains unavailable, so they are not execution evidence. A live read-only baseline after the merge returned HTTP 200 with DB/Redis `ok` and confirmed production still serves rollback commit `aca3b55…`. | Do not deploy the new image yet. Apply the single pending migration through the production direct/unpooled URL, verify migration status, then manually deploy main `d918243…`. Prove health/readiness, deployed commit, the `production_service_booted` audit event, environment=`production`, no `08P01`, Redis no-eviction, OTP live policy, and contained feature flags before reopening traffic. |
| 2026-07-22 | Fresh-database and disaster-recovery migration rehearsal | A disposable empty PostgreSQL database exposed two independent history-order prerequisites in the exact **204-migration** backend tree. First, restored historical migration `20260615000000_ride_max_broadcast_drivers_config` omits required `platform_config.updated_at`; after adding the already-intended default only in the disposable database and resolving that failed attempt, the chain advanced through every later migration until `20260722000000_comprehensive_system_audit`. That migration then correctly refused to install because the exact active controlled Super Administrator account does not exist before the ordinary post-migration seed step. No staging or production database was changed. | Existing production already contains the required config default and controlled admin, so this does not alter today’s additive hotfix procedure. However, clean environment creation and schema-only disaster recovery are **blocked**. Preserve deployed migration checksums; design and review an idempotent history bridge for the config default plus a secure pre-audit controlled-admin bootstrap/recovery procedure, then prove an empty-database migrate/seed/runtime-image rehearsal before calling DR ready. |
| 2026-07-22 | Pilot 100–500 simultaneous in-app call capacity gate | The exact backend candidate passes **40/40** focused call gateway/session, exact-role socket authentication, Redis lease and Cloudflare TURN tests plus **10/10** fail-closed call-harness contracts. The reviewed scenario requires 100/250/500 already-accepted calls, 200/500/1,000 unique target-issued role sessions, ten-minute signalling holds, exact target/commit binding and production-host refusal. At 500 calls the synthetic workload deliberately drives about 200 signalling events per second; each event rechecks the active session before and after its Redis-backed call lookup, so regional latency and Redis capacity are part of the measured gate. The reviewed k6 1.3.0 runner is now available and passes the separate one-VU ride smoke, but no private call fixture, same-region multi-replica target, retained call metrics or device/carrier evidence exists, and no call-capacity load has run. | **Do not claim 100, 250 or 500-call capacity yet.** Deploy at least two same-region API instances with same-geography PostgreSQL and TLS `noeviction` Redis, configure and observe Cloudflare TURN, provision one-time exact-role fixtures, then execute the 100→250→500 ladder with one-instance restarts and the Android↔Android, iOS↔iOS and Android↔iOS carrier matrix. The last fully passing level becomes the temporary pilot cap. |
| 2026-07-22 | Production maintenance-mode admin lockout | Production health now returns HTTP 200 with database/Redis `ok` and exact backend commit `d918243…`, but `POST /v1/auth/admin/login` returns `503 MAINTENANCE_MODE` before credential evaluation because the global guard bypasses `/v1/admin/*` while sign-in lives under `/v1/auth/admin/login`. Backend hotfix `97ff8b3…` adds an exact-path exemption only for admin sign-in, retains containment for every client/provider auth route and lookalike path, preserves authenticated Super Admin incident-response access, and updates the audit runbook. Focused **4/4** regression tests, API typecheck/build and diff checks pass. The commit is pushed to `feat/system-audit` but is not yet in backend `staging` or `main`. Admin audit-vault source is now merged through Admin `staging` (`b4bebb5…`) into Admin `main` (`30f2ed0…`), but the exact deployed Admin SHA has not been independently observed. | Keep maintenance and ride/job containment unchanged. Merge backend `97ff8b3…` through `staging` to `main`, manually deploy that reviewed main SHA, and prove the admin-login route no longer returns `MAINTENANCE_MODE` before entering credentials. Then verify the deployed Admin SHA, sign in as the exact controlled Super Administrator, complete audit timeline/export/integrity/403 smoke tests, and only then restore approved production service flags. |
| 2026-07-22 | Production services reopened and Admin artifact observed | A later independent read shows `maintenance_mode=false`, `rides_enabled=true`, and `artisan_jobs_enabled=true`; both the custom API domain and direct Render domain serve healthy commit `d918243…` with DB/Redis `ok`, and malformed admin credentials now reach DTO validation with HTTP 400 rather than the maintenance guard. OTP policy advertises live SMS primary with WhatsApp fallback. The Vercel production Admin route is live at `admin.myshop.gilmoretechnologiesgh.com`; public deployment `dpl_6rNMmNbA97TPyvU4uURTg5tPLXMz` serves an audit-page chunk containing `System Audit Vault`, mobile activity, integrity verification and audit exports, proving the new audit UI is deployed even though Vercel does not expose the source SHA in the public artifact. | The immediate maintenance lockout is cleared, but do not treat malformed-input reachability as a successful admin login or audit-vault acceptance. The controlled Super Administrator must now sign in and run the timeline, mobile activity, export, integrity and Product Owner 403 smoke tests. Retain backend hotfix `97ff8b3…` for the next maintenance window, reconcile why live service flags were reopened before that authenticated smoke evidence was recorded, and monitor live rides/jobs while completing the remaining production checks. |
| 2026-07-22 | Production legal publication integrity and unresolved DPC identifier | Public role-specific reads prove that Client, Driver and Artisan each receive exactly two effective/published records: their own Terms `1.4.1` plus shared Privacy `1.4.1`. After reproducing the publisher's trim semantics, SHA-256 comparison proves all four production bodies are byte-identical to the reviewed repository sources: Client Terms `eb3d6246…`, Driver Terms `b942f2b5…`, Artisan Terms `f5b3b4a7…`, and Privacy `d0f2bd0b…`. The published Privacy source nevertheless still contains the owner-requested placeholder `DPC registration number: PENDING — MUST BE ADDED BEFORE PRODUCTION`. The aggregate-only strict legal preflight explicitly counts that placeholder as `current_core_legal_documents_with_dpc_registration_placeholder`; production being live therefore does not satisfy the documented legal release gate. | Obtain and owner-confirm the actual DPC registration number. Do not mutate published `1.4.1` in place: the database and publisher make published/accepted content immutable. Create and review a new Privacy version containing the real identifier, update the exact-version preflight contract deliberately, publish/effect it, prove public body/source hash equality and zero strict-preflight violations, then exercise exact-role re-consent before treating legal readiness as closed. |
| 2026-07-22 | Signed mobile artifact provenance and build-number containment | Existing Client/Provider Android AAB/APK and iOS IPA artifacts all identify as `1.4.1 (23)`. Both AAB signatures validate against the separate expected Client (`79:71:B3:…:4D:D0`) and Provider (`B8:6D:8B:…:D8:C8`) certificates; Android/iOS AOT binaries contain the production API `https://api.myshop.gilmoretechnologiesgh.com/v1`. Recorded store hashes are Client AAB `3da1a489…`, Client IPA `6770f8fd…`, Provider AAB `78353877…`, and Provider IPA `1dbdd451…`. However, all were built on 21 July immediately after mobile main `f2556ea…`, before the 22 July privacy-safe mobile audit/telemetry source existed; none contains the current telemetry endpoint/marker. They are therefore older signed artifacts, not the final audited source build. Release tooling now treats build `23` as occupied, requires a full explicit `RELEASE_SOURCE_COMMIT`, exact clean `origin/main`, and embeds a bounded source SHA in mobile telemetry. It rejects the current feature branch as a release source. Release-version/source guard proof, full API-client **161/161** tests, focused analyzer with zero issues and diff checks pass. | Do not upload/promote the existing `+23` files as the final audit release. First merge the mobile audit branch through staging to main and recheck both store consoles; use at least build `24`, or higher than either console. Build all four artifacts from the same clean explicit `origin/main` SHA, verify the compiled production endpoint and `@<source-sha>` telemetry marker, record hashes/signing identities, install on physical devices, then upload those exact files to internal TestFlight/Play tracks before production submission. |
| 2026-07-22 | System Audit maintenance and export boundary hardening | Follow-up review found that maintenance mode blocked the HMAC-authenticated deployment recorder, so started/failed/rollback evidence could disappear during the exact drain window it describes. It also found that a missing legal-hold boolean removed a hold, mobile telemetry trusted the caller's app label instead of binding it to the authenticated role, and spreadsheet software could evaluate formula-capable stored text from CSV exports. Backend commit `e3e1c29…` fixes all four boundaries while exposing no neighbouring audit route. Focused maintenance/controller/service/interceptor proof passes **4 suites / 18 tests**; API typecheck/build, changed-file lint and diff checks pass. The commit is pushed in mergeable draft PR **#116** against backend staging; hosted checks remain red only because GitHub reports failed account payments/spending-limit exhaustion and launched no job steps. | Review and merge PR #116 through staging/main, manually deploy its exact main SHA, and prove Admin login plus signed deployment status recording during a controlled maintenance rehearsal. Then run the authenticated Super Admin timeline, telemetry, CSV/JSON, legal-hold and integrity smoke while confirming Product Owner receives 403. Do not treat GitHub's unstarted jobs as test evidence. |
| 2026-07-22 | Post-release baseline and resumed 100k-DAU programme | Reconciled all three clean repositories after outside changes and recorded the exact runtime/store state in `docs/post-release-100k-dau-roadmap.md`. Production API is healthy on backend `d918243…`; Admin is live; rides/jobs are open; OTP advertises SMS plus WhatsApp fallback. Official listings show Android client/provider `1.4.1`, while Apple still serves client `1.3.9` and provider `1.4.0`. Backend PR #116 and mobile PR #92 are merged to staging only. The current load evidence record remains explicitly NOT RUN, the repository Render Blueprint still says `plan: free` with no replica/autoscaling policy, and the runtime database pool defaults to max 10 per process. | Treat the released Android build as the baseline, not 100k evidence. Close the next-update legal/iOS/audit gates, approve a peak traffic model, confirm the actual Render/Neon/Redis topology and budget, then run the isolated multi-replica normal→2x→soak→fault→reconciliation→rollback ladder. Keep all separately deferred product flags closed. |
| 2026-07-23 | Guarded disposable-clone fixture and k6 smoke | Added a target-local exact-role fixture provisioner, database authorization guard, operator runbook and explicit synthetic-route isolation. Provisioning requires `disposable-clone`, test/staging runtime, exact release/target fingerprint, non-production geofence, a clean database, dedicated Redis prefix and one matching random nonce in the operator shell, PostgreSQL and Redis. Contracts pass **16/16** locally and under pinned Node 22. The first k6 1.3.0 smoke failed honestly at 5/9 checks because three intentionally invalid-key Google Routes requests exposed a missing dependency boundary; no live credential was used. Ride scenarios now fail closed unless the operator explicitly selects `synthetic-cache`, which covers all 18 fixed ordered routes for four hours and records its authority/expiry in the private fixture. A brand-new local clone then completed exact-role provisioning/preflight and passed smoke at **9/9 checks**, **0/7 HTTP failures**, p99 **133.23 ms**; one test ride was terminal and zero active rides/jobs remained. Logs showed cache hits, not a routing-provider failure. All delivery/deferred providers were disabled, and no staging/production service was touched. | Safe fixture generation, routing isolation and the local smoke gate are closed. This is one VU on one local replica—not pilot, call or 100k-DAU capacity evidence. Approve the traffic model/owners/topology, deploy an isolated same-region multi-replica clone, and execute normal→2x→soak→fault→reconciliation→alerts→rollback. Qualify live Google Routes separately with an approved cost/quota canary. |

## 16. Release approval record

| Function | Name | Decision | Date/time GMT | Evidence/conditions |
| --- | --- | --- | --- | --- |
| Product/business |  |  |  |  |
| Mobile |  |  |  |  |
| Backend |  |  |  |  |
| Admin/operations |  |  |  |  |
| QA |  |  |  |  |
| Security/privacy/legal |  |  |  |  |
| Finance/payments |  |  |  |  |
| Support/on-call |  |  |  |  |
| Release commander |  |  |  |  |
