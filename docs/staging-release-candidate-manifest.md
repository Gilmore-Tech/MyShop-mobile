# Staging release-candidate manifest

Snapshot time: **2026-07-19 18:52:55 GMT**
Status: **packaged as three local staging-parent candidates; not pushed,
deployed, merged, or production-approved**

This manifest freezes the current source candidate closely enough to detect any
subsequent workspace drift before packaging. Production remains **NO-GO** until
the checklist gates pass.

## 1. Pinned bases and local candidate fingerprints

| Repository | Packaging base | Base-tree proof | Candidate source set | Candidate SHA-256 |
| --- | --- | --- | --- | --- |
| Mobile | `origin/staging` at `f88f43c85e52ecf9f7d69b0f6e0a500665a0edcb` | Its tree `a969851cffded5878895242c58b8ade3bcebb4be` is byte-identical to local HEAD `d200d3dd5fbf0f6031c9930046c62cfc816e6cd5`; do not merge the old feature-branch history | 193 tracked changes + 121 untracked source files, excluding this self-referential manifest and `apps/api/` | `e4894a21ca27c194e962e2ffd43f33c04af7586cad1f8160f86512f496111396` |
| Backend | `origin/staging` and local HEAD at `1f1ada8b7ad3b9b8ab937fe8fab3dad611aae7bd` | Both use tree `e241fcfe198813274a897928b5671508d353ccb3` | 293 tracked changes + 336 untracked source files | `0c77153cf988549a37e6f55ab8978ffce8e8a7c9643f20d2270548f8df024413` |
| Admin | `origin/staging` and local HEAD at `78fabd15d8235e399dba45933bbe125b13c81077` | Both use tree `1eaa6e67be8936fa89b36242d2d6fac214eb0b7f` | 44 tracked changes + 11 untracked source files | `2d8d4ab8367e5c2fcae1ee43db6a56868bf41c4b6b8ec8a8385162de7f5e4b39` |

The tested mobile source candidate is
`b0d05d0df53a34102c5d38ad442ad0d951d3c7b2`; the packaged backend candidate is
`0c40e30b3602c7da6862d2651f8c83c7f99739c2`; the packaged Admin candidate is
`f65df4fa8e7ad5400fd70e5c7b1056e9bcaa96e2`. Each is parented directly to its
pinned staging base. A docs-only evidence commit may sit above the tested mobile
source commit; record both SHAs in the PR and private release inventory.

These are `myshop-candidate-v1` fingerprints generated by
`tool/candidate-fingerprint.mjs`: SHA-256 receives the full binary Git diff from
the pinned base, the literal NUL-delimited `MYSHOP_UNTRACKED_V1` marker, then
one JSON-line record containing `path`, Git mode and Git blob hash for each
included untracked file sorted by UTF-8 bytes. The mobile manifest excludes
itself to avoid a recursive hash. Reproduce the mobile value with:

```text
node tool/candidate-fingerprint.mjs f88f43c85e52ecf9f7d69b0f6e0a500665a0edcb --exclude=docs/staging-release-candidate-manifest.md --exclude=apps/api
```

Run the same reviewed helper from the backend/admin roots with their recorded
bases and no exclusions. Any source edit changes the fingerprint and requires a
new snapshot and gate run before packaging.

## 2. Mandatory packaging boundaries

- Create each packaging branch directly from its pinned `origin/staging` base.
  Apply the reviewed working-tree source delta; do not merge the mobile feature
  branch graph.
- Exclude mobile `apps/api/` in full. It contains one stray, untracked backend
  file (`src/common/services/provider-verification.ts`) with no mobile release
  provenance. The authoritative backend is the separate backend candidate.
- Exclude `.dart_tool/`, `build/`, `.next/`, coverage, Pods, derived data,
  dependency stores, logs, dumps, local databases, editor state and temporary
  test output. Lockfiles and reviewed native project/privacy files remain part
  of the source candidate.
- Include no `.env`, signing key, keystore, provisioning profile, private key,
  database URL, access token or service-account file. `.env.example` contains
  placeholders and fail-closed defaults only.
- A high-confidence added/untracked-content scan found no mobile or admin
  credential match. Backend matches were inspected and are synthetic
  localhost/runtime-audit database URLs in tests and verification scripts; no
  live credential is authorized in the candidate.
- The largest candidate files are reviewed source, lockfile or audit documents;
  no candidate file exceeds 500 KiB and no new binary artifact is admitted.
- Preserve the admin deletion of `components/users/create-user-dialog.tsx`; it
  is part of the exact candidate and must not be accidentally restored by
  blanket copy or conflict resolution.

## 3. Latest exact-current local evidence

| Surface | Required evidence already passed on this snapshot |
| --- | --- |
| Backend | Exact current source: pinned Node 22.23.0 Linux passes 201/201 API suites and 4,002/4,002 tests; local typecheck, lint with zero errors and the five-package production build pass. The release-critical scheduler audit adds atomic claims, compare-and-set transitions and durable notification dedupe to the active-ride disconnect, no-show, bid expiry, job staleness, directed-quote, rating reveal, clawback, reconciliation, insufficient-balance and stale-payment paths. Reconciliation discrepancies and signature-verified Paystack dead letters now use PostgreSQL-leased durable alert outboxes with zero-recipient retention and per-admin dedupe. The webhook alert/API omit raw bodies, destinations, payment/transfer references and provider error prose, and expose no automatic financial action. After the approved 120-second socket-loss grace, active-ride escalation joins the same durable BR-33 episode as stale-GPS detection, persists permission-scoped admin and exact-role provider/client alerts, retries safely, deduplicates both detectors, keeps active work alive and excludes unactivated queued rides. The exact-commit **181,271,488-byte** runtime image at `sha256:16d9b3a088cbe37e5bbf27e2527037e1ec4b0d4f26a332b269cd9466257ded03` passes the repository verifier for Node 22.23.0, UID/GID 1001, runtime allowlisting/read-only containment, all 195 migrations, seed, production eviction refusal, TLS Redis with `noeviction`, geofence/readiness and SIGTERM handling. |
| Database/E2E | Exact-current fresh PostgreSQL/Redis: all **195 migrations**, approved-PostGIS-only drift and seed, bounded/read-only release preflights, ride-cutover refusal, online-index safety, and **79/79 lifecycle E2E**. The real PostgreSQL scenarios prove one of two replicas owns each reconciliation/webhook alert claim, post-fan-out crash recovery stays deduped, and a report/event remains pending until an authorised payment admin exists. Synthetic roles carry exact immutable Terms/Privacy evidence; final private-auth-root deletion preserves the audit tuple while nulling only the deleted identity pointer. Functional scheduled-job E2E deliberately uses `FF_SCHEDULED_JOBS=true`; unit/Blueprint contracts prove the release default is OFF. Strict legal release readiness correctly fails until corrected content is published. |
| Mobile | 428/428 tests (client 78, provider 153, API client 154, shared UI 30, shared models 7, overlay 6); all seven package analyzers with fatal infos. Exact consent uses two unchecked controls, exact document identities and fail-closed version refresh. |
| Admin | TypeScript, 32/32 contract tests, lint with 0 errors/101 warnings, and the 48-route production build with the read-only payment-authorised webhook-failure queue |

These gates were rerun from the exact local commit/source trees above. They
become staging evidence only after second-developer review, push/merge,
deployment of the exact artifacts and staging acceptance.

## 4. Staging feature exposure contract

The first integrated staging deployment must remain fail-closed for every
feature below, regardless of whether local code exists:

- automated batch payouts and provider aggregate payouts;
- promotion and loyalty-point redemption plus role-account reward mutation;
- USSD;
- Smile Identity automation;
- automated police/background-check callbacks and claims;
- general outbound WhatsApp notifications; the separately approved OTP test
  remains limited to the private two-number allowlist;
- general non-OTP SMS notifications;
- new scheduled artisan jobs; existing accepted commitments remain manageable;
- session recovery;
- deleted-role recovery at initial deployment; it may be enabled only for the
  controlled staging acceptance journey with backend
  `FF_ROLE_ACCOUNT_RECOVERY=true` and an admin artifact built with
  `NEXT_PUBLIC_FF_ROLE_ACCOUNT_RECOVERY=true`, then returned to false unless
  continuing exposure is explicitly approved;
- automatic permanent role-account purge;
- emergency audio/video recording;
- email notifications and weekly earnings-summary delivery;
- support attachments and dispute evidence uploads;
- admin provider profile-photo and document writers;
- cancellation financial/rating consequences;
- BR-53 active-trip fallback until its staging/device/load proof passes; and
- new V2 ride bookings until the controlled cutover preconditions pass.

For the emergency v1 cut, the items above may move to the next release only
while their flags and user-facing claims remain fail-closed. Core OTP/signup,
provider Go Online and vehicle/document eligibility, ride dispatch/recovery,
payment/refund/dispute authority, SOS, corrected legal publications, signed
installed-device acceptance, production Redis durability, monitoring, rollback
and a controlled canary cannot be shifted without blocking the public update.

Paid OTP delivery must use the approved exact two-number staging allowlist,
deployment-wide Redis limits, callback telemetry and kill switch. The private
numbers and every credential stay in the deployment secret store, never in Git
or this manifest.

The durable per-payment payout outbox is a controlled core cutover, not a
deferred aggregate payout. Leave `FF_INSTANT_PAYOUTS` false/unset until the
staging hold/outbox/verification/reversal journey passes. Then set exact `true`
with `INSTANT_TRANSFERS_ENABLED_AT` equal to the approved deployment cutover;
every older retained payment requires exact manual reconciliation and must not
fall back to a direct transfer.

## 5. Packaging and staging acceptance order

1. Recompute all three candidate fingerprints and stop on any mismatch.
2. Create reviewed commits on new branches from the pinned bases; record the
   three commit SHAs and replace this local-snapshot status.
3. Re-run diff/secret/artifact checks, full static/build/test gates and the
   fresh 195-migration database/E2E gate on those exact commits.
4. Run aggregate-only staging preflights, preserve a database rollback point,
   deploy the backend with all containment flags closed, and verify migration,
   health, Ghana staging geofence and Redis-prefix state.
5. Deploy the exact admin artifact and build signed client/provider artifacts
   with higher private build numbers and reviewed store URLs.
6. Execute the runbook's role/IDOR, OTP, dispatch, payment/refund, document,
   vehicle, background/locked-device, duplicate/delay/lost-response, failure,
   rollback and reconciliation journeys.
7. Keep V2 bookings closed unless the cutover rehearsal and canary both pass.
   A failed gate rolls back or leaves the affected feature disabled; it never
   becomes an undocumented waiver.

## 6. Still required before production

This snapshot does not prove production capacity, production Redis durability,
external metrics/alerts, private object-store behavior, live carrier/payment
failure handling, production-shaped multi-replica faults and scheduler scan/pool
capacity, full load/soak, physical-device behavior,
signed artifact installation, rollback or store approval. BR-63's reviewer
chain is approved and implemented locally but still needs the pinned staging
journey. BR-64 hold-release governance is approved, while exact post-closure
non-tax retention periods remain with Legal; automatic purge therefore stays
off. Exact legal-consent mechanics are implemented and owner-approved: the apps
submit the separate exact Terms/Privacy identities, OTP completion revalidates
them, the API stores immutable role-scoped evidence, legacy roles re-consent,
active work can finish, new work is fenced, and published tuples cannot be
edited. The remaining legal P0 is the actual qualified review, publication and
hash verification of client/driver/artisan Terms plus shared Privacy version
`1.4.1`; strict release preflight must report zero before public packaging.

Use the [production checklist](production-release-audit-checklist.md) as the
release authority and the [staging runbook](staging-release-candidate-runbook.md)
as the execution sequence.
